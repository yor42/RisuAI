# Investigation Ledger

Records every investigation dispatched under the tiering in `AGENTS.md` 1.3, so the
two-tier split is **measured rather than assumed**. The split was adopted as an
architecture to test, not a settled conclusion; if the mid-tier proves unable to produce
packets good enough to resolve most investigations, this ledger is the evidence that
justifies reverting to Opus as the default investigator.

Record one row per investigation, at the time it completes. Token counts come from the
subagent usage reported to the Orchestrator.

## What the numbers are for

| Metric | Why it matters |
|---|---|
| Escalation rate | If it approaches 1, the mid-tier is not doing its job and the split is only adding a hop. |
| Escalations that materially changed the conclusion | The core value question for the Opus tier. Cheap confirmations are fine; *only* confirmations means the bar is too low. |
| Escalations that merely confirmed | Not waste at low rates — it is what a correctly-calibrated bar produces. Waste if it dominates. |
| Cost per investigation, by tier | The economic claim being tested. |
| Failures traceable to bad investigation | The correctness guardrail. Any occurrence is serious and should be written up in full, not just tallied. |

**Honesty rule.** Record outcomes including the ones that argue against the current
architecture. A ledger kept to justify a decision already made is worthless.

## Log

| # | Date | Question | Tier(s) | Tokens | Escalated? | Outcome |
|---|---|---|---|---|---|---|
| 1 | 2026-09-21 | Size the module-editor per-keystroke fix; verify the two `modules` effects | Opus (pre-split; dispatched as `opus-investigator`) | ~99.8k, 43 tool uses | n/a | Accurate on all claims the Orchestrator spot-checked. Surfaced the `getModules()` load-bearing accident and the DEV-inflation risk to the headline measurement — both high-value. Understated two things the Orchestrator caught on verification: **two** reverts of the proxy-tracking approach plus two stabilization patches (it reported one revert), and the load-bearing comment at `ModuleSettings.svelte:181` explaining that the eager `push` exists to prevent a prior double-insert bug. **Its DEV-inflation doubt was tested by `perf-analyzer` and disproven** — production is 1.18x faster, not a multiple. |

| 2 | 2026-09-21 | Stage A plan gate — falsify the narrowing of `stores.svelte.ts:197` to four module fields (`Agents/Reports/09-stage-a-module-effect-narrowing-plan.md`) | `adversarial-reviewer` (Sonnet 5) | ~54.4k, 28 tool uses | No | Verdict *proceed with named amendments*. Could not falsify the three load-bearing claims: the field list (independently enumerated all 15 `RisuModule` fields and traced which reach `moduleUpdate()`), the scope boundary (traced `dbChangeEffects` wiring to `globalApi.svelte.ts:607`, separate from the changed effect), or the synchronous-subscriber leak path via `ReloadGUIPointer` → `resetScriptCache()`. Produced three real amendments, all Orchestrator-verified against source before folding in: two off-by-one line citations in the plan (`:565`→`:566`, `:568`→`:569`); a **better** R1 precedent than the plan had (`IrisModal.svelte:268`, `SegmentedControl.svelte:49` already ship the exact `void x` idiom); and a correctly-identified coverage gap — the tests exercise the extracted helper, not the wiring, so a manual smoke check is now part of acceptance. Note: the reviewer found the plan's cited precedent weaker than an alternative it located itself, which is the kind of finding a gate is for. |

| 3 | 2026-09-21 | Stage A post-implementation review — falsify the implemented narrowing, its tests, and the comments shipping with it | `adversarial-reviewer` (Sonnet 5) | ~60.4k, 25 tool uses | No | Verdict *ship with named fixes*, both cosmetic. Independently re-derived the four-field list from source and found no fifth field, including through the `ReloadGUIPointer` / `HideIconStore` / `moduleBackgroundEmbedding` subscriber fan-out. Fact-checked all 11 checkable claims in the shipped comments against source: all TRUE, with the caveat that the `:566`/`:569` citations are the *read* sites and the matching `.set()` calls are at `:577`/`:575`. Caught one real doc defect — the plan still said the import goes on line 5 of `stores.svelte.ts`, stale after its own leaf-file amendment; fixed. **Process failure worth recording: the brief told the agent it had Bash, and the profile grants only Read/Grep/Glob.** It disclosed this in its first paragraph instead of quietly substituting reasoning for execution, and separately asked that someone with Bash re-run the suite on this exact branch state before merge. The Orchestrator had already done so independently (red run 11 failed / green run 27 passed / full suite 358 passed exit 0 / `pnpm check` 0-0), so the gap was covered — but the brief was wrong and a less careful agent would have papered over it. Check the profile's tool grant before promising a tool. |

| 4 | 2026-09-21 | Stage B plan gate — falsify the module draft-copy plan (`Agents/Reports/10-stage-b-module-draft-copy-plan.md`) | `opus-reviewer` (Opus 5) | ~128.3k, 51 tool uses | n/a (gate, not investigation) | Verdict **[REJECT]**, 11 findings, and the rejection was correct. Two were REJECT-grade and both were Orchestrator-verified against source: **R1** — the plan's claim that flush-on-unmount closes the new loss surface is false (`grep -rn onCloseRequested src/` → nothing; `preload.ts:15` gates the only `beforeunload` on `isWeb`, so Tauri desktop has no unload hook; `accountStorage.ts:114-117` reloads through the deliberately un-cancellable `isAppInitiatedReload()` bypass). **R3** — duplicate module ids are reachable (`modules.ts:286-287` pushes `.risum` imports id-intact while `:304` regenerates only on the JSON path), so a bare `findIndex` clobbers the wrong module, unconditionally under commit-on-destroy. Also caught a false perf attribution the Orchestrator had written (R2: `stores.svelte.ts:197` is 0.03 ms post-Stage-A, not 29.21 ms — the two bullets summed to the *pre*-Stage-A total and implied Stage A bought nothing), a test seam placed back in `modules.ts` that would have re-run Stage A's ELIFECYCLE (R5, contradicting `moduleUpdateDeps.ts:24-27`), and a broken empty-draft predicate (`ModuleMenu.svelte:170-184` populates `trigger` merely on clicking the tab). **The reviewer was itself wrong twice, both caught on verification:** its R4 citations were off by one (`App.svelte:211/:219/:223` for what is `:212/:220/:225`), and its R7 replacement count (21/28) does not reproduce (Orchestrator measures 19 external / 12 internal; the plan's original "49" was also wrong — cite the command, not the number). Net: the gate paid for itself several times over, and the "verify before propagating" rule caught both of its own errors. |

| 5 | 2026-09-21 | Does any awaitable "pending DB writes flushed" signal exist, for a Tauri `onCloseRequested` draft flush? | `investigator` (Sonnet 5) | ~58.6k, 27 tool uses | No | **Refuted the feasibility premise behind the maintainer's chosen option.** No flush primitive exists anywhere: `dirtySinceLastSave` (`globalApi.svelte.ts:556`) and `changeTracker` (`:576`) are both declared *inside* `saveDb()` (`:552`) — function-local, unexported, unreachable. The only externally-visible signal is `saving` (`:379`, exported `$state`, read solely by `SavePopupIcon.svelte:4` for an icon), and its true→false edge is **racy**: a cycle already in flight took its `toSave` snapshot at `:786` before the draft commit, so a caller polling one edge can declare victory on a write that does not contain the draft. No existing teardown path waits on `saveDb()` at all — 8 `location.reload()` sites across 6 files either accept loss (with comments saying so) or bypass the loop by writing their own data. Flagged a genuine trap unprompted: `acquireExclusiveStorageMigrationLock()` (`:506-539`) looks like the pattern to copy but `dbWriteLock` is held only around the raw write (`:833-843`), not the encode or the debounce, so copying it would silently inherit the same gap. **The Orchestrator then found the number that reframes the whole question, following the investigator's own trail:** `debounceTime = 500` at `:591` is a *trailing* debounce that `clearTimeout`s on every keystroke (`:598-603`), so continuous typing produces **zero** disk writes today until a 500 ms pause — today's durability is not per-keystroke, and a 500 ms draft-commit debounce reproduces today's profile almost exactly, making the Tauri hook and the save-loop surgery likely unnecessary. Model tier note: Sonnet established the mechanism correctly and honestly labelled its one unresolved item (the Tauri API contract, which the Orchestrator then settled from `window.d.ts:1208-1223`); no escalation needed. |

| 6 | 2026-09-21 | Stage B plan gate round 2 — falsify revision 2, including the new save-loop seam (§3.7.4) | `opus-reviewer` (Opus 5, fresh context, explicitly unbound from round 1) | ~130.0k, 44 tool uses | n/a (gate) | Verdict **[REJECT]**, 11 findings, 6 REJECT-grade — and sharper than round 1. Best finding: revision 2's duplicate-id fix was **circular**. It proposed validating a stored index via `modules[hint]?.id === draft.id`, but the only index computed at open is `ModuleSettings.svelte:109` `findIndex((v) => v.id === rmodule.id)` — already a bare `findIndex` by id — so it re-derives the same wrong element and fixes nothing, while *increasing* reachability (revision 2 fires the commit on debounce/unmount/`pagehide`/close rather than only a name-gated button). Second: it read the Tauri **runtime** rather than the typings and found `onCloseRequested` already does `await handler(evt); if (!evt.isPreventDefault()) await this.destroy()`, so revision 2's `preventDefault()`-first shape opts out of the wrapper's own close and strands an **unclosable window** on any throw. Third: revision 2's flush-correlation proof was wrong about its own cited line — `globalApi.svelte.ts:786` snapshots `changeTracker` (which blocks are dirty), not the data (`:801`/`:808`), and the tracker bit is set by an *async* `$effect`; the correlation holds only on an unstated microtask-vs-macrotask ordering invariant. Also caught `sleepForever()` cited as three sites at `:709`/`:977`/`:1003` when it is **six** at `:704`/`:727`/`:739`/`:748`/`:979`/`:1005` (each citation off by two; `:709` et al. are `saving.state = false`), a **fabricated** supporting sentence about `FilesSettings.svelte` using `MigrationStorage` (`grep -rn MigrationStorage src/` matches only `accountStorage.ts`), and a retracted "49 consumers" still live in §3.2 while F7 retracted it. Confirmed sound independently: the corrected trailing-debounce baseline, `primaryCommitted` as the resolve edge, §3.3's byte-identity argument down to `clone.js:57-105` and `proxy.js:333-348`, §1's numbers, B1's additivity, and the B1/B2 split line. **Orchestrator note: across both Stage B gates, the large majority of the 22 findings were errors in the Orchestrator's own plan text — including two that would have shipped a silent-data-loss path while claiming to close one. Two consecutive rejections of successive revisions is itself the finding: plan text here is not trustworthy on first draft, and the gate is paying for itself several times over.** |

| 7 | 2026-09-21 | Benchmark the partition branch point: per-module vs whole-array snapshot, asset-heavy shape, module-count scaling, `$effect` overhead | `perf-analyzer` (Sonnet 5), two dispatches | ~91.0k + ~157.3k, 57 tool uses | No | **The measurement campaign that replaced three rejected plan revisions with numbers.** Reproduced independently by the Orchestrator on every headline figure. Established: partitioning takes a keystroke from ~29.8 ms (whole array) to ~4.2 ms (largest cjs-heavy module) / ~0.2 ms (median) in the harness; per-element sum equals the single whole-array walk (1.01x), so shape changes are not a regression; `$effect` scheduling/teardown overhead of N children vs 1 is **below measurement noise** (0.92-1.01x at N=52 and N=104) with isolation holding in every run. Flagged its own unmeasured gap unprompted (effect overhead), which the Orchestrator then commissioned. **Most valuable single result:** prompted by a maintainer detail about 10k+-image asset modules, measured that snapshot cost tracks NODE COUNT not bytes — a 1.03 MB asset-shaped module costs 35.6 ms versus 4.2 ms for a 1.65 MB cjs-heavy one, ~8x at equal size. That bounded the partition's claim honestly ("clears budget for non-asset modules") instead of shipping an overstatement. |

| 8 | 2026-09-21 | Real-app end-to-end: is the cost the effect or the DOM, and what does it cost at real module counts? | Orchestrator direct (browser pane, live dev server) | in-session | n/a | Ran against the live app rather than the harness, with the module editor open and real `input` events; every mutation restored and verified. **Effect 11.5 ms vs DOM ~2.2 ms at 52 modules — the effect is ~84% of the cost**, which answered the senior-advisor's ceiling question and cleared the last blocker to planning. Measured 104 modules in-app at **25.1 ms (150% of frame budget)**, 1.9x over 52 — linearity confirmed on live data rather than interpolated. **Also corrected the harness README's standing caveat:** it says the real figure is "this or higher, never lower", but Chromium is ~2.6x *faster* than the Node harness on identical data, so absolute harness numbers overstate real cost (the ratios remain valid). Downstream effect work beyond the raw snapshot is ~0.2 ms, i.e. negligible. |

| 9 | 2026-09-21 | Stage B partition plan gate — falsify the per-element effect partition | `opus-reviewer` (Opus 5, fresh context) | ~110.6k, 36 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS] — the first approval in this campaign's Stage B work, after two rejections of the draft-copy design.** Could not construct a missed-mutation path. Proved dependency-closure equivalence from Svelte 5.55.1 source: `$state.snapshot(array)` registers the container source, `length`, and one source per index — and notably NOT the array `version` source, since `clone.js`'s array branch uses an index loop and `i in value` rather than `ownKeys`. Worked the full mutation matrix (leaf, nested leaf, replacement, push/splice/unshift/sort, whole-array reassign, self-assign, holes, `length = 0`) and traced same-batch interleaves and effect teardown through `batch.js`/`runtime.js`/`effects.js`. All 7 findings were in the plan's *artifacts*, not the design. **Best finding (F-1): the plan's "this is the pattern already used for characters" was false AND the cited prior art is itself unsafe** — `tracker.character` is written only at `dbChangeEffects.svelte.ts:77-78` gated on `selIdState`, and `risuSave.ts:285-308` reuses the existing block otherwise, so a mutation to a non-selected character that already has a block is never re-encoded. Orchestrator verified; logged as a separate bug (plan section 8.1), not absorbed. Also caught that the existing first-run test's `toHaveBeenCalledTimes(6)` would break the equivalence suite once modules are populated (6 today vs 58 after), and that no test in the repo has ever executed a `$effect` created inside a *re-running* `$effect` — the load-bearing untested mechanic. |

| 10 | 2026-09-21 | Scope (not fix) the non-selected-character marking bug surfaced by gate 9 | `investigator` (Sonnet 5) | ~101.2k, 67 tool uses | No | **Model case for what the mid-tier is for.** Confirmed all three mechanism steps, then did the work that actually mattered: traced **67 raw candidate writes across 24 files** individually rather than grepping and inferring, and established that **~60 resolve to the live selection** and are not instances. Narrowed a scary-sounding "any write to a non-selected character is lost" to three real writers, one of which (`coldstorage.svelte.ts:621`) is **self-mitigated** by a `requiresFullEncoderReload` call someone evidently added on purpose for this exact gap. Found the decisive mitigation unprompted — selecting the character later re-captures the edit off the live proxy, so the failure mode is "never persists unless reopened before reload", not unconditional loss — which is what downgraded severity from crisis to occasional-loss. Surfaced two bonus findings the brief never asked for: the **restore-from-trash bug** (`GridCatalog.svelte:142-146`, concrete repro: restore, don't open, close app, it returns to trash) and that **`toSave.chat` is dead in the encoder** (zero matches for it or `RisuSaveType.CHAT` in `risuSave.ts`), which usefully means a fix needs no per-chat work. Honest about its own limits: flagged that it did not exhaustively sweep MCP/risuaccess write paths, so "small fix surface" is bounded-but-not-proven. Orchestrator re-verified all four headline claims against source. No escalation needed — Sonnet produced a packet good enough to disposition the whole question. |

| 11 | 2026-09-21 | Stage B post-implementation review of the partition diff | `opus-reviewer` (Opus 5, fresh) + one re-review pass | ~115.0k + ~127.2k | n/a (gate) | **[REJECT] then [APPROVE].** First pass independently derived dependency-closure equivalence from Svelte 5.55.1 source, worked the full mutation matrix including `pop`/`shift`/`fill`/`copyWithin` and a module object shared by two indices, proved `if (!m) continue` safe (the `mods[i]` read precedes the guard and `proxy.js:178` subscribes even an absent slot), traced that children created mid-flush run synchronously in the same `flush()`, and fact-checked **all six** claims in the new code comment as TRUE. **The executable change was correct on first submission.** The REJECT was entirely on the test file's comments: a flat "dbChangeEffects.svelte.ts is not modified by this change" (true when written, false once shipped), stale `:32-38` citations now landing on comment text, and **an arithmetic error that originated in the Orchestrator's own brief** — "6+N+1" first-flush `markChanged` calls, where the correct figure is 6+N and the file's own passing `toHaveBeenCalledTimes(6)` test disproved it. Comment-only fix dispatched; re-review compared every non-comment line token-by-token and confirmed nothing executable changed. One optional nit (`:54-70` vs `:54-71`) dispositioned as accepted. |

| 12 | 2026-09-21 | Empirically reproduce the restore-from-trash data-loss bug (not just source-trace it) | `test-warrior` (Sonnet 5) | ~161.0k, 77 tool uses | No | **Bug is real; reproduced end to end.** Drove the real `registerDbChangeEffects()` and real `RisuSaveEncoder` / `decodeRisuSave` and inspected decoded bytes rather than reimplementing the logic, having been briefed that a clean disproof was fully acceptable. Crux: after restoring a non-selected character exactly as `GridCatalog.svelte:142-146` does, `'char-B' in toSave.character` is **false**, and the decoded `trashTime` after that save is **still `1700000000000`**. Also empirically confirmed the claimed mitigation (selecting the character and saving again clears it). Orchestrator re-ran it independently and got identical values. Contributed a durable harness-convention finding: splitting a `vi.mock` factory for an app rune module across files via dynamic `import()` lets the real `stores.svelte.ts` / `parser.svelte.ts` fire their top-level `$effect.root` blocks against thin mocks — now documented in `Agents/Tools/README.md` with the `<name>.svelte.harness.ts` single-file workaround. Expensive for a reproduction (77 tool uses), mostly spent isolating that mock-splitting failure, which it diagnosed correctly rather than working around blindly. |

| 13 | 2026-09-21 | Phase 2 item 3 premise: how the chat list, sidebar character list, and the 3-layout character list page render and load avatars; enumerate virtualization hazards | `investigator` (Sonnet 5) | ~144.6k, 47 tool uses | No | **Premise partly confirmed, partly corrected, and the scope widened.** (1) The chat list is **already windowed**: `Chats.svelte` mounts and unmounts messages imperatively with `mount()`/`unmount()`, not an `{#each}`, capped at `loadPages` (default 30, +15 per scroll-up, `chatLoadPages.ts`). The cap only grows and never shrinks on scroll, and a screenshot sets it to `Infinity` (`DefaultChatScreen.svelte:469`). The Roadmap's "unbounded, append-only" wording is therefore imprecise: the list is bounded by scroll-back depth, not by chat length. (2) The maintainer's grid premise holds and is **wider than reported**: all three character-list layouts (grid, list/trash, and simple = `MobileCharacters.svelte`) render every matching character, with no windowing and no lazy loading. Each calls `getCharImage(...,'css')` inline in the template, so a re-render (e.g. a search keystroke) re-resolves every avatar. The sidebar `{#each charImages}` (`Sidebar.svelte:563`) is unkeyed and fully mounted, and its `<img>` has no `loading="lazy"`. (3) On the plain-browser branch of `getFileSrc` (no service worker and no account sync, which covers plain-HTTP LAN/Pi), every call **re-encodes the full asset to a base64 `data:` string even on a cache hit** (`globalApi.svelte.ts:285`). A grid of N characters therefore holds N full-size base64 avatar strings. (4) Hazards: plugin API v3 `SafeDocument` wraps the real `document.documentElement` (`v3.svelte.ts:360-364`), so plugins can query the live chat DOM; `scrollToMessage` relies on `data-chat-index` plus raising `loadPages`; the `.chat-message-container:first-of-type` CSS (`styles.css:513`) relies on the newest message being the first DOM child; there are nested `flex-col-reverse` containers; the sidebar calls `scrollIntoView` by `data-char-id`; and there is **no shared parse cache**, so a remount re-runs `ParseMarkdown`, including character display scripts. Also corrected the handoff's citation `Chat.svelte:522` to **`:538`**. Orchestrator re-verified all of the above against source. |

| 14 | 2026-09-21 | Maintainer hypothesis: chat data (heavy for asset/CSS-intensive bots) is held in RAM when not needed, causing Android OOM and Tauri slowdown | `deep-investigator` (Opus 5), escalated from Reports 01/01-deepdive (static only, one COULD NOT DETERMINE) at maintainer request | ~161.3k, 83 tool uses | Yes (from prior reports) | **Narrowed. Residency is confirmed, but the weight is somewhere other than hypothesised, and the Android premise is unobservable.** Every non-cold character and chat is decoded eagerly and stays resident. Measured in Node (pointer compression off, so the multipliers are upper bounds): proxy overhead is ~1.9-2.4 KB/message, ~3.7x plain (ASCII) and 2.4x (Hangul). At 1000 characters and 150k messages: 100.8 MB plain plus 277.6 MB of proxy overhead. **Escalation changed the conclusion.** Report 01's "recursively proxies in one pass" mechanism is wrong, because Svelte 5.55.1 proxies lazily; full materialisation is instead forced by boot's `saveDb()` → `encoder.init(getDatabase())` → `JSON.stringify(character)` through the proxy (`globalApi.svelte.ts:587`, `risuSave.ts:250-253`). It **refuted** "asset/CSS-heavy chat data": assets are paths, inlays live in a separate store, and CSS/HTML is character/module data. It found **no Android build in the repo**. It corrected three stale claims in the deep dive (`fileCache` is now 200-LRU, the translator cache is now capped at 500, and `fileSrcCache` holds full base64 on the plain-web path). New findings: whole-save `console.log` retention (CHORE-06, measured in Node, inferred in the browser); a transient cold-storage read failure that permanently orphans a chat (CHORE-07, source-traced); and the active-character chats snapshot per streaming chunk costing ~5.3 µs/message (item 2). **Process slip, disclosed by the agent:** it briefly created `.tmp-compile-probe.mjs` in the repo root, deleted it at once, and it is confirmed gone. The Orchestrator re-verified `risuSave.ts:641,824,250-253`, `coldstorage.svelte.ts:151,74-104,636-665`, `database.svelte.ts:713`, `globalApi.svelte.ts:587`, `dbChangeEffects.svelte.ts:109`, `plugins.svelte.ts:717`, `bootstrap.ts:171,284,290`, and the absence of `Cargo.toml:47` `[lib]` / `gen/android`. |

| 15 | 2026-09-21 | Empirically reproduce CHORE-07 (a transient cold-storage read failure orphans a chat) against unfixed code | `test-warrior` (Sonnet 5) | ~97.8k, 31 tool uses | No | **Reproduced on the first run, every step, including the blob deletion.** New harness `Agents/Tools/save-gen/cold-storage-orphan-repro.svelte.harness.ts`, briefed that a disproof was acceptable. It drives the real `coldstorage.svelte.ts` and `registerDbChangeEffects()`, plus a real encode/decode round trip, and mocks only the OPFS backend (`getFileHandle` throws once). The chain held: `null` from `getColdStorageItem` while the blob is intact → `preLoadChat` writes an error message → `markChanged` fires (6→7) → the decoded save holds the error → a later `preLoadChat` is a no-op → `cleanColdStorage()` deletes the intact blob. The control (genuinely missing blob) produces an identical error, so the two cases are indistinguishable to the user. It corrected three line ranges. Far cheaper than row 12's reproduction, because it reused that harness's documented mock-isolation lessons: it found and mocked two new transitive rune-module import traps (`alert`, `sionyw`). The Orchestrator re-ran it (exit 0), read the mock list, and extended the scope from source: the same swallow exists in the Node-server and Tauri branches, and the account branch returns `null` on a non-200 hub response when the data isn't local (the account case is inferred, not reproduced). `pnpm test` reported by the agent: 32 / 377 / 3, exit 0. |

| 16 | 2026-09-21 | Real-Chromium measurement of item 8's proxy multiplier and CHORE-06's console retention (the browser pane has no `gc()`, so neither could be measured there) | `perf-analyzer` (Sonnet 5) | ~152.3k, 91 tool uses | No | **Both premises measured.** Setup: headless Chrome 154, throwaway profile, `--expose-gc`, esbuild bundle of the real Svelte `proxy` plus the seed-1337 generator, with DEV=false verified statically and at runtime. Proxy overhead in Chromium is **2.44-2.49x ASCII and 1.76x Hangul, ~979-1,233 B/msg**, about half of Node's 3.6-3.75x because of pointer compression. At 1000 characters and 150k messages: 95.5 MB plain plus 140.0 MB proxy. The 3 repeats were bit-identical, and the CDP cross-check agreed within 1.3%. Console: **95.5 MB retained** after `console.log` with DevTools closed, against 0.06 MB in the control; released by `console.clear()` or about 1000 further logs. **Not propagated, unverified:** the agent's claim that V8 heap counters under-report some large or single-character strings, which was observed with RSS growth while the counters stayed flat. It is plausible but not checked by the Orchestrator, and matters only if a future measurement targets large strings. The agent validated its own CHORE-06 fixture against its expected byte count (ratio 1.002) before trusting it. The Orchestrator verified the table against `results-a.json`/`results-b.json` (multiplier = (plain+proxy)/plain, consistent with Node's) and confirmed 0 leftover headless Chrome processes. The agent did its own git-status check correctly, flagging row 15's untracked harness as not its own. |

| 17 | 2026-09-21 | Item 3 premise: count avatar lookups (`getFileSrc`) per layout × N × action in the character list, against unmodified code | `test-warrior` (Sonnet 5) | ~188.2k, 63 tool uses | No | **Premise confirmed for grid/list and partly contradicted for the default "simple" layout.** The contradiction was reported as a finding, not smoothed over. Harness `charlist-avatar-count.svelte.harness.ts` mounts the real components and the real `getCharImage`. Initial mount does one lookup per listed character in every layout. Grid and list redo them on every search keystroke (N when all match, the visible count when it narrows) and on any visible character's name or image change. Simple does 0 on search (`{#if}` inside the `{#each}`) but N on name, image, `lastInteraction` or chat-count changes (`sortChar`). A chat-message push caused 0 everywhere. Base64 at `globalApi.svelte.ts:285` costs about 8.75 ms per MB and 1.33 MB of string per MB (exactly 4/3, the base64 ratio; corrected from 1.37 at the AV-1 plan gate) (i9 best case). The agent hit a real OOM from 72 fresh 1000-character mounts, fixed it by reusing mounts, and noted and excluded an invisible-character confound. **Shipped-comment defect found at verification:** the `:683` comment says `sortChar` "never read[s] chats", but it reads `c.chats.length`. The 0 holds only because a message push doesn't change the chat count, so the comment must be corrected before any commit. The Orchestrator re-ran it (exit 0), confirmed the assertions encode the reported counts, read `MobileCharacters.svelte:53-75` to confirm the simple-layout mechanism, and checked `MobileBody.svelte:47-52` (the simple list is mounted only with no character selected, so chatting does not trigger it). `pnpm test` reported by the agent: 32 / 377 / 3, exit 0. |

| 18 | 2026-09-21 | Planning facts for avatar stages A (stop re-lookups) and C (lazy-mount): call sites, Svelte 5.55.1 equality and each-block semantics, sidebar mechanics, reusable lazy infrastructure, hazards | `investigator` (Sonnet 5), 1 `code-searcher` survey | ~131.8k, 42 tool uses | No | **Useful and mostly correct. One inference is disputed and left for the harness to settle.** Found 26 `getCharImage` call sites, 8 of them O(N characters). These include a **4th hot path the brief missed**: `AlertComp.svelte:385-388`, the `selectChar` dialog, which iterates every character with an inline `{#await getCharImage}`. The Sidebar's `charImages` `$effect` (`:83-130`) rebuilds a fresh object per character, so the lodash `isEqual` guard only skips no-op passes. Any single rename or avatar change re-resolves every sidebar avatar: the same defect as `formatChars`, one level up. `LazyPortal.svelte` has **zero call sites** and is one-shot (never unmounts), so it defers cost without bounding it. `scrollToActiveCharacter` depends on `data-char-id`, which is emitted only by a mounted `SidebarAvatar` (`:44`). `db.hideAllImages` is read inside `getCharImage` on every call, so any memo must depend on it. List/trash rows and open folders have non-uniform heights, which favours per-item IntersectionObserver over fixed-height windowing. **Disputed:** the packet concludes a child `$derived(getCharImage(image))` will not re-run when the parent item object is replaced with an equal string. The Orchestrator judges this does not follow. The prop getter reads the parent's item source, so the derived re-runs and returns a new Promise; string equality only helps behind an intermediate derived boundary. This is unresolved; the plan will settle it empirically with `charlist-avatar-count.svelte.harness.ts`, which then becomes stage A's red-before-green proof. The Orchestrator verified `equality.js:4-5,13-16,29-30`, `each.js:307,664-665` (item mode), `AlertComp.svelte:385-388`, zero `LazyPortal` users, and `SidebarAvatar.svelte:44`. |

| 19 | 2026-09-21 | Planning facts for avatar stages B (plain-HTTP encode) and D (thumbnails): `getFileSrc` callers' dependence on `data:` URLs, cache semantics, resize precedent, invisible thumbnail storage, avatar identity, Tauri cost | `investigator` (Sonnet 5), 1 `code-searcher` survey | ~117.1k, 57 tool uses | No | **Mostly correct, with one wrong claim caught at verification.** Findings:<br>• **Not safe for `blob:` URLs:** the "copy as HTML" clipboard feature has three prefix allow-lists without `blob:` (`Chat.svelte:673,713,765`). They would silently drop images, but the fix is cheap.<br>• **Revoking `blob:` URLs on LRU eviction is unsafe:** `{#await}`-bound `<img>` elements hold the resolved string, and the 200-entry cap is smaller than a 1000-character list. Precedent: `blobUrlCache` is never revoked.<br>• **Stage B does not touch `fileSrcCache`/`blobUrlCache`:** both are chat-markdown and inlay paths, not avatars.<br>• **Thumbnail precedent in production:** `BotSettings.svelte:805-818` does canvas `drawImage` to a 48×48 `toDataURL`.<br>• **Invisible storage exists on every branch:** backups enumerate only `readDir('assets')` (Tauri) and `.png` keys of `forageStorage` (web), and `cleanChunks` is scoped the same way. A separate `localforage.createInstance` (like `inlayStorage`) or an AppData subdir outside `assets`/`remotes` is invisible to backup, export, cleanup and sync.<br>• **Avatar paths are content-hashed** (`saveAsset` → `hasher(data)`), so a path-keyed thumbnail cannot go stale.<br>• **Tauri uses the stock asset protocol with no custom handler**, so the full-size decode per icon is architecturally confirmed. The cost itself is unmeasured.<br>• **A pre-existing MIME anomaly exists:** everything is labelled `image/png`, including video and audio.<br>**Wrong claim:** "`oder.img` is never read back anywhere". `backuplocal.ts:270-273` reads `item.img` as an asset-map key. It is written with the `getFileSrc` result at `Sidebar.svelte:658` and persisted in `db.characterOrder`, so on plain HTTP each folder image sits in the save as a full base64 `data:` URL. Stage B must keep `blob:` URLs out of that persisted field. The Orchestrator verified the three allow-lists, `BotSettings.svelte:805-818`, `backuplocal.ts:81,108,122,270-273`, `Sidebar.svelte:641,658`, and the `saveAsset` hash and extension default. |

| 20 | 2026-09-21 | AV-1 plan gate: falsify `Agents/Reports/12-charlist-avatar-plan.md` §2 | `adversarial-reviewer` (Sonnet 5) | ~156.7k, 60 tool uses | n/a (gate) | Verdict: **approve with required changes**. It **settled the disputed mechanism empirically** by compiling and running real 5.55.1 components in a scratch vitest. `{@const}` lowers to `$.derived` (`ConstTag.js:16-36`). Calls after equal-value replacement of 20 items: single-level `{@const}` 20, single child `$derived` 20, two-level `{@const}` **0**, two-`$derived` wrapper 0. A real change gives 1, and the `hideAllImages` toggle gives 20. **This confirmed the Orchestrator's dispute of ledger 18's investigator claim.** Findings:<br>• **MAJOR:** the new suite test must reuse mounts and keep N small, or it risks the harness's OOM in shared `pnpm test`. It re-measured a ~13-17 s first import.<br>• **MINOR:** the citation `each.js:664-665` should be `:666`.<br>• **MINOR:** the base64 ratio is **1.33x (4/3), not 1.37x**, an Orchestrator arithmetic error that had propagated into the Roadmap, ledger row 17 and Report 12. All three are now corrected.<br>• **MINOR:** there was no delete-while-open test; reasoning says keys are safe, but it is untested.<br>The Orchestrator re-ran the probe (exit 0, 7/7, identical counts), checked `each.js:664-666`, and computed 1398104/1048576 = 1.3333. All four were folded into the plan. The agent's git status showed no repo writes. |

| 21 | 2026-09-21 | CHORE-07 fix-planning facts: callers of `getColdStorageItem`, plugin contract, missing-vs-error per backend, the `preLoadChat` UI, cleanup reach, tests | `investigator` (Sonnet 5), no code-searcher | ~134.1k, 51 tool uses | No | **High value, with two errors caught at verification. One correction turned a "neutralised" finding into a live asset-deletion hypothesis.**<br>**Findings:**<br>• **Plugin contract:** `pluginStorage.getItem` is documented as never throwing and returning `null` for not-found (`risuai.d.ts:1021`), so `getColdStorageItem`'s external contract must stay.<br>• **UI rethrow:** `DefaultChatScreen.svelte:817`'s `{#await preLoadChat}` has no `:catch`, and Svelte's `await.js` re-throws, so `preLoadChat` must never reject.<br>• **Node server already distinguishes missing from error one layer down:** an empty body gives `null`, a non-2xx status throws `"getItem Error"` (`nodeStorage.ts:138-158`). `coldstorage.svelte.ts:61-73` discards that.<br>• **OPFS precedent:** the `NotFoundError` check already exists (`opfsStorage.ts:24-32`).<br>• **Tauri precedent:** `exists()` is used widely.<br>• **Account branch has NO try/catch:** a network error, or a decompress/parse failure, throws uncaught today (EXTRA-1).<br>• **Write-verify sites are safe:** `:404/:500/:543` abort before the destructive step, and `changeChar` fails safe.<br>• **`cleanColdStorage()` is manual only** (`UserSettings.svelte:94-95`).<br>**Error 1 (Orchestrator correction):** it said `bootstrap.ts:572`'s gate skips asset cleanup whenever any character is cold-stored. The gate actually checks the **global setting** `db.coldstorage`. So on Tauri, anyone with the setting off (and no account sync) runs the destructive `assets/` sweep on every launch (`:575-587`). That includes users who used cold storage and then disabled it, the population of upstream's rollback. A failed cold read in `getUncleanables` (`globalApi.svelte.ts:1478-1482`, no else-branch) leaves the character a stub, so its emotion and additional images look unreferenced and are **deleted from disk**. This is **source-traced, not reproduced**, and plausibly matches the maintainer's "images going missing" reports.<br>**Error 2:** it said the send-while-cold loss "cannot happen with today's code". It can, during the pending `{#await}` window: `sendMain` has no cold-pointer guard (`DefaultChatScreen.svelte:162-172`), and `preLoadChat` replaces `chat.message` wholesale after its await (`coldstorage.svelte.ts:637-640`). That is a pre-existing race that a pointer-preserving fix would widen.<br>The Orchestrator verified `nodeStorage.ts`, `opfsStorage.ts:24-32`, `DefaultChatScreen.svelte:814-826,160-229`, `bootstrap.ts:556-587`, `globalApi.svelte.ts:1474-1486`, `drive.ts:312`, and `coldstorage.svelte.ts:625-640`. |

| 22 | 2026-09-21 | Reproduce: startup asset cleanup deletes a cold-stored character's assets after one failed cold read (hypothesis from row 21) | `test-warrior` (Sonnet 5) | ~107.5k, 37 tool uses | No | **Confirmed.** The harness `asset-gc-cold-read-repro.svelte.harness.ts` drives the real `getUncleanables` and the real cold-storage read/write, with an in-memory Tauri FS; about 30 of `globalApi.svelte.ts`'s imports are mocked, each justified. Cases:<br>• read succeeds: all 4 assets are protected;<br>• one transient `readFile` throw: `uncleanable` = main image only, and the replicated predicate (`bootstrap.ts:577-589`) deletes 2 emotion images and 1 additional asset while the blob stays intact;<br>• blob genuinely missing: identical;<br>• `db.coldstorage=true`: gated off.<br>**Limit:** `cleanChunks` is module-private, so the predicate was replicated, not driven. That is honestly labelled.<br>**Correction:** the predicate range is `:576-589`.<br>**Web branch:** the agent read that it also deletes (`:645-654`); the Orchestrator verified it in source; the harness did not exercise it. The hosted web app, the largest user group, is exposed.<br>The Orchestrator re-ran it (exit 0, 4/4) and confirmed the web-branch lines. The agent's `pnpm test` result was exit 1, solely from the deliberately red `charlistAvatarLookups.svelte.test.ts` (8 failing); every other file matched baseline. |

| 23 | 2026-09-21 | CHORE-07 plan gate, rev 1 (`Agents/Reports/13-chore07-cold-read-failure-plan.md`) | `opus-reviewer` (Opus 5) | ~139.7k, 60 tool uses | n/a (gate) | Verdict: **approve with required changes**, with 1 BLOCKER and 6 MAJOR findings. All were verified by the Orchestrator before being folded into rev 2.<br>**B1:** rev 1 claimed the merge made a send during loading safe. That is false. `sendChat` streams into `message[msgIndex]`, with the index captured before the merge (`index.svelte.ts:1593,1653`). The plugin `risuai.sendChat` bypasses `sendMain` (`v3.svelte.ts:1411-1419`). And `{#await}` tracks `preLoadChat`'s synchronous reads (`await.js:48-52`), so `:256`'s reassignment re-triggers the load mid-stream. The guard must sit at the `sendChat` entry.<br>**M1:** "keeps showing loading" is false. It resolves to an empty `<div>`, so the user sees a blank chat. The notice is mandatory, and retry needs `{#key}`.<br>**M2:** the merge lost edits to `localLore`, summaries and `scriptstate` made during the window.<br>**M3 (the Orchestrator's own propagated error, first made in ledger 21):** "`getItem` is documented never-throwing" is false. `risuai.d.ts:1019-1021` only says it resolves to the value or null. The null-wrapper enables a plugin get-default-set overwrite.<br>**M4:** Tauri `exists()` is `Path::exists` (`tauri-plugin-fs` 2.5.2 `commands.rs:1204`), false on any metadata error, so it is not a clear not-found.<br>**M5:** already-hit users' blobs are deleted by the cleanup button.<br>**M6:** a restore that lands after a character switch is never saved (CHORE-01), and the cleanup can then delete the blob.<br>**Minors:** account fallback, 404 treated as error, a bad-shape blob, `complete` needing the chaId check, only three asset-deletion sites exist, and a RED/CHAR test split.<br>**Question-2 table:** covers 19 consumers of a still-cold chat. New loss paths: `sendChat`, plugin `sendChat`, HypaV3 modal, chat-local lorebook. SUSPECTED pre-existing: multiuser sync.<br>**Lesson recorded:** the Orchestrator verified that the cited d.ts line existed but not that it said what was claimed. Verify the claim, not just the citation. Rev 2 was sent back to the same reviewer for a re-gate. |

| 24 | 2026-09-21 | AV-1: write the red-before-green suite test against unfixed code | `test-warrior` (Sonnet 5) | ~206.1k, 100 tool uses | No | **Red as predicted.** New file `src/lib/Others/charlistAvatarLookups.svelte.test.ts`, discovered by `pnpm test`, with mounts reused and N = 60. Every O(N) case **fails on unfixed code with the plan's exact counts**: grid/list search-all 60→0, search-narrow 6→0, change-image 60→1, simple rename 60→0 and change-image 60→1, **Sidebar** rename 25→0 and change-image 25→1. Six cases pass today and are labelled "preserved behaviour": click after filter, the `hideAllImages` toggle, delete-while-open, and `AlertComp` selectChar (0 lookups on rename, so it does not churn and is left unchanged per plan 2.2). Sidebar and AlertComp were feasible after all: Sidebar needed `getDatabase()` to return `DBState.db` and about 13 extra store keys. Also fixed the harness `:683` comment. Wall time is ~19.5 s standalone, almost all of it the one-time transform (tests 318 ms). The agent caught itself reading a false exit 0 through a pipe and re-checked un-piped. The Orchestrator re-ran it: exit 1, 10 failed / 6 passed, failure messages identical. Expensive (100 tool uses), mostly isolating the Sidebar and AlertComp import graphs, done safely in throwaway probes that it deleted. |

| 25 | 2026-09-21 | CHORE-07 plan re-gate, rev 2 | `opus-reviewer` (Opus 5), same instance as row 23 | ~186.3k, 25 tool uses | n/a (gate) | Verdict: **approve with required changes**. Resolved: B1, M1, M3, M4. Partly resolved: M2, M5. **M6 not resolved.**<br>**New BLOCKER R1:** rev 2 put the chat-shape check in the shared reader. That reader also serves `{character}` blobs and arbitrary plugin values, so every cold character would become unopenable and plugin data would be overwritten.<br>**R2 (from git history):** legacy array blobs, written up to `f62de63f` before the `190f3d77` object format, never reset their side fields, so "restored" would wipe real memory and lore. `hypaV2Data` chunk ids number from `lastMainChunkID`, so it must be restored, not concatenated. hypaV3 categories merge by id, and `localLore` deduplicates by id.<br>**R3:** `requiresFullEncoderReload` does not trigger a save (`globalApi.svelte.ts:762-783`) and re-encodes every character. It needs a targeted mark-for-save and a session keep-set.<br>**R4:** the `sendChat` guard must come before `doingChat.set(true)` (`index.svelte.ts:219`) and use `alertError`, not `throwError`, which writes into the chat. The `sendMain` guard must precede `processMultiCommand`, because `/cut`, `/del` and `/multisend clear` mutate directly.<br>**R5:** recovery matches the first message, not a one-message chat. The template is unchanged since `9a59d483`, which is upstream's own "fix: prevent chat data loss in cold storage" that introduced it. Recovery must refuse during a send.<br>**Minors R6-R10:** test labels, a Tauri `/\(os error [23]\)/` regex, a Retry element for recovery, a seam including the gate and the loops, and citations.<br>**Plugin rejection:** recommended, low-to-moderate risk.<br>**Orchestrator response:** verified R3 (`:762-783`), R4a (`:219`), the SHAs `f62de63f`, `190f3d77` and `9a59d483`, and the web-loop structure (`bootstrap.ts:651-654` assets, `:660-699` remotes). Two rounds each produced a new blocker, so the work was **staged** in rev 3: **7a "stop deleting"** (sweep skip plus error-text keep-list, no UI, plugin or load change) is planned in full and gated first, and 7b/7c carry every finding for their own gates. |

| 26 | 2026-09-21 | CHORE-07 stage 7a gate (rev 3 §2) | `opus-reviewer` (Opus 5), same instance | ~207.8k, 9 tool uses | n/a (gate) | Verdict: **approve with required changes.** The §2.1 sweep half is sound.<br>**F1 (MAJOR):** the error-text keep-list is blind to chats inside characters that are themselves cold-stored. Stubs record only pointer chats (`coldstorage.svelte.ts:411-418`) and keep one empty chat (`:426-435`). So in the likeliest real sequence (damaged chat left unopened, character goes cold after 10 days, user presses cleanup), the blob is still deleted. `cleanColdStorage` must read cold character blobs, and abort on any incomplete read.<br>**F2:** the plan's "safe after 7a" user advice overclaimed, because unsaved restores are still exposed until 7b.<br>**Minors:** a throw counts as incomplete; healthy-install failure behaviour stays identical (Tauri `readDir` throw, web ordering and a single `keys()` call); a red-first sequence for a seam that does not yet exist (refactor, cross-check against the harness replica, red, green); never reject keys by format; build the a7 fixture via the real `preLoadChat`.<br>It confirmed that `9a59d483` is upstream (PR #1332) and that its parent kept the pointer on a failed read.<br>The Orchestrator verified F1 at `coldstorage.svelte.ts:408-436` and `coldstorageData.ts:64-78`. All findings were folded into §2.2b and §3. |
| 27 | 2026-09-21 | AV-1 post-implementation gate | `adversarial-reviewer`, fresh | ~100.5k, 30 tool uses | n/a (gate) | Verdict: **PASS with findings, none blocking.** Cleared: key uniqueness (`formatChars` emits `index: i` from one loop; `sortChar` assigns `i` before filter/sort), click closures after delete/search, Sidebar @const legality with DnD untouched, no stale avatar possible (getFileSrc is a pure function of `loc`; avatar `saveAsset` calls never pass `customId`, so same path implies same content), plugin whole-array replacement. Ran the test (16/16) and svelte-check (0/0).<br>**Low (verified by the Orchestrator):** in dev compiles, `{@const}` is evaluated eagerly for TDZ diagnostics (ConstTag.js:97), so GridCatalog calls getCharImage once per image-less character at item creation. Harmless: getCharImage returns early on a falsy `loc` (characters.ts:65-70) before getFileSrc. Production compiles stay lazy. No change needed.<br>Info: the plan's §2.3 site list omits the folder-member loop (Sidebar.svelte:731-732), which the diff correctly covers.<br>Reviewer's own limit: red-before-green was not re-executed (the tree was shared); it was already recorded in row 24. Separately, the implementer ran `git stash`/`pop` in the shared tree. Nothing was lost; agents are now told not to. |
| 28 | 2026-09-21 | AV-2 evidence packet: scroll roots, consumers, IntersectionObserver, memory holders | `investigator` | ~108.2k, 48 tool uses | No | **Sharpened premise:** the nested MobileCharacters-inside-GridCatalog case is the DEFAULT phone path. "Simple" is GridCatalog's default tab (`GridCatalog.svelte:21` `selected = $state(3)`, `:165-166`), and the standalone MobileBody needs the opt-in Beta Mobile GUI (`bootstrap.ts:281`). BarIcon and SidebarAvatar already have same-size non-image states, so there is no layout shift; `data-char-id` is independent of the avatar. **happy-dom's IntersectionObserver is a no-op stub** (`observe`/`disconnect` are `// TODO: Implement`), so tests need an injectable fake. On the plain-browser branch only the raw bytes are cached, and the base64 string is rebuilt per call (`globalApi.svelte.ts:285`), held only by the DOM and the derived. Adjacent O(N): `AlertComp.svelte:385-388` (folded into AV-2); `CharConfig.svelte:287` and `BookmarkList.svelte:185,198` (recorded). Uncertain: which nested box scrolls (CSS reasoning, not measured), and whether Chromium frees decoded bitmaps for `data:` URLs (becomes AV-2 step 0). The Orchestrator spot-checked bootstrap.ts:281, GridCatalog :21/:165-166, the happy-dom stub and AlertComp :385-388: all as stated. Led to Report 14. |
| 29 | 2026-09-21 | CHORE-07 7a post-implementation gate | `opus-reviewer` (Opus 5), fresh instance | ~74.5k, 21 tool uses | n/a (gate) | Verdict: **REJECT, with a small fix.** The deletion guards themselves are correct.<br>**MAJOR (verified by the Orchestrator):** `resolveUncleanableChars`'s new try/catch makes `getUncleanables` swallow a throw it used to propagate. The only thrower is getColdStorageItem's account branch (`coldstorage.svelte.ts:44-60`, no catch). `drive.ts:312` (`loadDrive`, a restore) relied on that rejection to abort with "Backup Error" (`drive.ts:97-98`). Now a network failure yields a stub-based `requiredImages`, and the restore "succeeds" with that character's emotion and additional assets not downloaded. No deletion, so not data loss, but a loud failure became a quiet one, and two comments claim nothing changed. The asset sweep is unaffected: cleanChunks returns early on isAccount (`bootstrap.ts:570`).<br>MINOR: stale citations and a nonexistent `getUncleanablesChecked` in the test; a literal `§` in comments; test gaps (pointer-key branch, chaId-mismatch abort, no test reaching either catch); the abort alert names no character and is not localized; the asset-GC harness still shows the pre-fix loss.<br>Cleared: healthy-install identity, a6 remotes by reading, the cleanColdStorage union and the abort ordering, template byte-identity, backups untouched, the red tests use the real wiring, and the orphan harness flip is expected. |
| 30 | 2026-09-21 | AV-2 plan gate (Report 14 rev 1) | `adversarial-reviewer`, fresh | ~97.9k, 36 tool uses | n/a (gate) | Verdict: **approve with required changes.**<br>**BLOCKER:** the observer registry's lifecycle was unspecified. A Map keyed by root pins every detached root: AlertComp mounts a fresh `overflow-y-auto` box per dialog open (`AlertComp.svelte:189-191`, verified). Fix: a WeakMap by root, with entries deleted on empty; test v9.<br>**MAJOR:** the nested-root outcome has no automated guard (happy-dom has no layout), so it is accepted as fail-open with a dev-only log. Tab switches remount, so a brief placeholder flash is accepted in writing.<br>**MINOR:** GridCatalog list/trash lines are `:113`/`:138` after AV-1 (verified); SidebarAvatar's no-src branch is exercised only by folders today (test v10); the "ledger 18, Q7" citation in Report 12 is not traceable as written.<br>Cleared: all other citations, spec-valid rootMargin and hysteresis, SvelteSet option (b) (`has` on an absent key tracks `#version`, but the boolean value is unchanged, so AV-1's gate holds; set.js:118-143), same-size sentinels, `data-char-id`, AlertComp keying safety, plugin DOM safety. All folded into Report 14 rev 2. |
| 31 | 2026-09-21 | AV-2 step 0: does clearing a `data:` URL free the decoded bitmap? | `perf-analyzer` | ~132.3k, 45 tool uses | Yes (headless Chrome 154 over CDP, standalone probe) | 200 distinct noise PNGs (535.4 MB PNG, 713.9 MB base64), 3 reps per mode plus a 5-cycle run. **CSS background-image:** shown adds +1968 MB to the renderer working set (2.76x the base64 bytes); clear plus gc releases 99.8%. **`<img src>`:** the string is freed, but decoded bitmaps stay in a bounded engine cache (idle floor flat at about 2145 MB over cycles 3-5; not an unbounded leak). CDP `Memory.forciblyPurgeJavaScriptMemory` destroys the tab context in this build. The GPU process was not separable. Headless vs headful was reasoned, not measured. Decision: release goes into AV-2, with the `<img>` limit stated (Report 14 §2.1); AV-4 thumbnails are the fix for bitmap size. |
| 32 | 2026-09-21 | CHORE-07 7a: the recorded RED runs (the evidence the guard test file points to) | `test-warrior`; re-run by the Orchestrator | n/a | No | File `src/ts/process/tests/coldStorageDeletionGuards.svelte.test.ts`.<br>**Against the unfixed source** (HEAD at the time of the run, 947a42c8, before AV-1's 64777a34, plus the pure-refactor seams `assetSweep.ts` and `buildAssetKeepSet`): the Orchestrator re-ran it, exit 1, 7 failed / 4 passed / 1 skipped.<br>• a1 Tauri transient read: `expected false to be true` on `tauriFsHas('assets/emotion-happy.png')`. a2 web/forage: the same on `forageAssetStore.has(...)`. a3 missing blob and a4 chaId mismatch: the same as a1.<br>• a7: `expected [] to include 'a7-main-key'`. a11: `expected [ 'a11-cold-char-key' ] to include 'a11-chat-key'`. a11b: `expected [] to include 'a11b-orphan-key'`.<br>**Against the round-1 fix** (not shipped; ledger 29 REJECT): a10b `promise resolved "[ 'main-image.png' ]" instead of rejecting` (the Orchestrator re-ran it, exit 1, 1 failed / 14 passed / 1 skipped). HEAD rethrows, so a10b is a regression pin, not a RED test against HEAD.<br>After the round-2 fix: 15 passed / 1 skipped, exit 0 (Orchestrator re-run). |
| 33 | 2026-09-21 | CHORE-07 7a re-gate (round 2) | `opus-reviewer`, same instance as row 29 | ~103.9k, 12 tool uses | n/a (gate) | Verdict: **REJECT on wording only; the code is approved.** getUncleanables now throws exactly as HEAD, including rejecting before scanning later characters; buildAssetKeepSet swallows; the collector, a12, a13 (the pointer branch is load-bearing) and a14 were verified.<br>Blocking, all test-file text: the header points to git history for the RED failures, which will not exist (now ledger 32); the header calls a10b an open gap; a10b is labelled RED but passes on HEAD, and its comment misdescribes HEAD; "today" wording where "before the fix" is meant.<br>MINOR: the new "Verifying..." alertWait can stay up if `listColdStorageItems()` throws (account mode: a non-200 returns null, so `.items` throws); stale citations (`coldstorage.svelte.ts:250` and test lines); the hardcoded English 'Unknown character' leaves the lang fallback dead. |
| 34 | 2026-09-21 | CHORE-07 7a re-gate (round 3) | `opus-reviewer`, same instance | ~120.5k, 6 tool uses | n/a (gate) | Verdict: **APPROVE with findings.** The abort keeps its own message: it returns inside the try, before the generic catch. Only failures before any deletion reach the catch (`listColdStorageItems`, the key builders); `removeColdStorageItems` never throws. So "nothing else was affected" is true in en and ko. **Correction to the Orchestrator's brief:** a throw can NOT come after partial removals (removal errors are logged and the loop continues); no artifact repeats the wrong claim. Remaining MINOR: line citations drifted again (`coldstorage.svelte.ts:250`; test `:524`, `:676`, `:828`, `:879`); cite by symbol instead. Pre-existing, unchanged: failed removals are silent. |
| 35 | 2026-09-21 | AV-2 post-implementation gate | `adversarial-reviewer`, fresh | ~176.4k, 63 tool uses | Probes in scratch | Verdict: **approve with required changes.**<br>**BLOCKER (verified by the Orchestrator: no try/catch in `nearViewport.svelte.ts`):** a throwing IntersectionObserver constructor is not handled; only a missing global is (:187-198). The reviewer's probe shows an action that throws during a later reactive mount silently collapses the enclosing block. For Sidebar (mounted after `$loadedStore`), all navigation would vanish. Contradicts plan §4.<br>**MAJOR (verified):** the shared visible SvelteSets never shrink on unmount. Only the far-band callback removes an entry, and that observer is torn down on unmount (`destroy` at :222-225 only removes targets), so tab switches, folder close and search narrowing let the sets grow back toward "everything resolves".<br>MINOR: BarIcon `style={as || null}` is load-bearing for the tests' `[style]` checks; no production dependency.<br>Cleared: the three-level @const gating (a scratch probe showed no cross-item re-lookup via SvelteSet.has), registry lifecycle, real boxes at every site (no display:contents), SidebarAvatar undefined src, missing-IO fail-open, near/far hysteresis ordering (structurally order-free), AlertComp index key. 424/424 and 0/0 re-run.<br>Order for the fix: test-warrior writes RED v11/v12 first; the coder is on hold until they are recorded. |
| 36 | 2026-09-21 | AV-2 post-gate: RED tests v11/v12 before the fix | `test-warrior`; re-run by the Orchestrator | ~169.8k, 47 tool uses | No | `charlistAvatarLazy.svelte.test.ts` gained v11 (throwing constructor on a later reactive mount, grid and Sidebar) and v12 (tab switch, search narrow/restore, Sidebar folder reopen), plus two v12b CHAR ordering guards. The Orchestrator re-ran it: exit 1, 5 failed / 18 passed.<br>• v11 ×2: `Error: v11 fake: IntersectionObserver constructor always throws`. The mechanism is **corrected from the reviewer's**: not a silent collapse but an uncaught rethrow, because `mount()`'s internal boundary has no onerror (render.js:176-183, boundary.js:392-396). It also leaves a half-mounted component that polluted later tests until each test unmounted in `finally`.<br>• v12: tab switch `expected 3 to be +0`; search `expected 8 to be 1`; Sidebar folder `expected 5 to be 2`.<br>Design approved for the fix: a try/catch fail-open with registry cleanup, plus per-key ownership (`SvelteMap<key, Element>`, `onChange(visible, node)`, destroy calls `onChange(false, node)`), which is order-independent. |
| 37 | 2026-09-21 | AV-2 re-gate after the BLOCKER/MAJOR fixes | `adversarial-reviewer`, same instance as row 35 | ~235.1k, 80 tool uses | Probes in scratch | Verdict: **APPROVE.** The try/catch fail-open is confirmed live: v11 prints the real warning from real source lines. A scratch probe for near-succeeds/far-throws shows the near entry disconnected and removed and the element failing open. Per-key ownership: no leak (unobserve happens before targets.delete, so a stale queued callback is a no-op; mismatched release is a no-op). onChange in destroy runs in the action teardown (actions.js:39-41), not a derived; no state_unsafe_mutation in the logs. SvelteMap.has gating is preserved: ownership reassignment does not re-invoke the lookup (map.js:96-119, deriveds.js:386-407). 431/431 and 0/0 re-run. MINOR: add a partial-construction test (being added as v11c). Pending: the live dev-server check (plan §5). |
| 38 | 2026-09-21 | AV-2 live check (plan §5) | Orchestrator, in the browser pane (Chromium) against a dev server the maintainer started with the legal flag inline | n/a | Yes (live, i9) | Pane storage held the seed fixture (31 characters, no avatars). It was temporarily given generated 256px PNG avatars and 120 `DUP` copies (151 total), then **restored and verified**: 31 characters, 0 `DUP`, image fields back to none. The service-worker branch served `/sw/img/...`.<br>**A hidden pane runs no IntersectionObserver callbacks at all** (even a hand-made observer never fired), which is the same class as the rAF trap; the pane must be visible.<br>Results (resolved / total avatars): **Simple tab 18/151 at 1280x800 and 17/151 at 375x812**; every in-view avatar resolved. Scrolling to the middle and bottom resolved new items and **released the first ten** (0 of 10 still resolved); back at the top, 18 again. **List 13/151. Grid 151/151**, since at 1280x800 121 tiles are on screen, so this is expected at N=151. **Trash 0.** **The selectChar dialog loads avatars in a real browser** (132 on screen, 151 resolved; the display:contents fix is confirmed). Sidebar 21/29.<br>**Nested phone path settled:** the inner MobileCharacters box scrolls (client 612-628, scroll 11022) and the outer GridCatalog box does not, at both widths, confirming the ledger-28 inference.<br>Console: no nearViewport, each_key or state_unsafe errors from the fresh load. The `SvelteSet is not defined` errors in the buffer are HMR `?t=` transients from mid-edit (no SvelteSet reference remains in the source). **Pre-existing, unrelated:** `saveDbKei` reads `db.account.kei` with no guard (kei/backup.ts:86), so it throws and logs `KEI auto-backup failed` on every save when `account` is undefined. |
| 39 | 2026-09-21 | CHORE-07 7b plan gate (Report 13 §4) | `opus-reviewer`, fresh | ~79.5k, 25 tool uses | No | Verdict: **approve with findings**, for a minimal core it defined. Key finding (verified by the Orchestrator at `coldstorage.svelte.ts:736-774` and `DefaultChatScreen.svelte:162-175`): once a failed load stops writing the error text, the pointer stays on screen indefinitely, so `/cut`, `/del` and `/multisend clear` could drop it and a later clean would delete the blob. The core therefore had to include a `sendMain` guard and a notice, not only "no mutation". Deferred to 7c with no loss path left open: three-way reader and `missing` branch, side-field merges, the v3 `sendChat` guard, `markCharacterForSave` and the restored-keys keep-list, a Retry button. The maintainer approved the core and asked for the firm "delete this chat" notice to wait for a reliable `missing` in 7c. |
| 40 | 2026-09-21 | CHORE-07 7b post-implementation gate | `opus-reviewer`, fresh | ~63.7k, 21 tool uses | Ran `pnpm test`/`pnpm check` | Verdict: **REJECT on wording only**; no code defect. BLOCKER: the `formatColdStorageLoadError` doc comment claimed two production consumers that do not exist (verified by grep). MAJOR: test comments called the pre-fix code "HEAD"/"currently". MINOR: the refusal text said "still loading" after a failed load; optional null-safety on the notice's key expression (ruled no crash: Svelte tears the `{#if}` down first). RED claims R1-R6 verified by reading against the pre-7b code; R1/R2/R4/R5 fail on their mutation assertions, not only the return value. The coder fixed all four; the Orchestrator verified the fixes directly instead of a re-gate (comment/text only). Committed as `3e17c8a3`: 36 files, 445 passed, 4 skipped; check 0/0. |
| 41 | 2026-09-21 | CHORE-07 7c-1 plan gate (Report 13 §5 rev 4) | `opus-reviewer`, fresh | ~84k, 41 tool uses | Ran fflate for the `undefined` round trip | Verdict: **approve with required changes**. Orchestrator verified in source: (1) `v3.svelte.ts:1411-1415` pushes the plugin message before the 7b guard refuses, and `:1427` returns true, so the plugin `sendChat` guard is required, not redundant; (2) `preLoadChat` re-checks only the pointer after the await, and only the selected character is tracked for save (`dbChangeEffects.svelte.ts:103-117`), so a character switch mid-read can lose the blob (pre-existing); (3) `setItem(undefined)` stores 0 bytes that would read back as error forever (existing guard at `coldstorage.svelte.ts:648`); (4) `factory.ts` exists, and the Orchestrator's "not found" claim in rev 4 was wrong (bridge `:278-281`, `:846-868`). Also: keep `getColdStorageItem` byte-identical (wrapping breaks `drive.ts:312` abort); account is never `missing` (hub 204 unverified); Tauri `os error 2` only; notice must not say delete unconditionally. All folded into rev 5. |
| 42 | 2026-09-21 | CHORE-07 7c-1 post-implementation gate | `opus-reviewer`, fresh | ~71k, 24 tool uses | Ran `pnpm test`/`pnpm check` | Verdict: **REJECT on tests/comments only**; production logic judged correct against §5.2, and wiring matches `getColdStorageItem` path for path. MAJOR (verified by the Orchestrator at `v3.svelte.ts:606` and the test fixture): the plugin `sendChat` guard test was vacuous: no `plugins` in the fixture, so the pre-guard code threw a TypeError first, and the "no push" check read the raw object, not the Svelte proxy. MINOR: false comments (7b guard location, "11 callers", "0 bytes"), `risuai.d.ts` claimed upstream never rejects (its account branch can), unused import, mislabelled RED tests, OPFS `NotFoundError` from `getDirectory()` treated as missing, `??` vs the old falsy mapping check. The coder fixed all of them, with behavioural RED for the guard ("promise resolved true instead of rejecting") and the OPFS case. The Orchestrator reran: 38 files, 480 passed, 4 skipped; check 0/0. |
| 43 | 2026-09-21 | CHORE-07 7c-1 live check | Orchestrator, in the browser pane against the maintainer's dev server (HMR picked up the edits) | n/a | Yes (live) | Seed fixture. A temporary chat `LIVECHECK 7c` was added to Side Character 1, pointing at a non-existent key, then **removed and verified** (2 chats, original page, no leftover). OPFS read classified as missing: the firm notice rendered with the key; the chat kept its pointer only; send was refused via the 7b dialog, nothing was pushed, and the input text was kept. The dialog wording ("loading failed") was then generalised in `en.ts`. Stores had to be imported with the page's `?t=` query (README Trap 1). |
| 44 | 2026-09-21 | CHORE-07 7c-2 investigation and plan gate (Report 13 §5.3 rev 6) | `investigator` then `opus-reviewer`, fresh | ~92k + ~98k | Read-only (git show for pre-7b and upstream) | Investigator found F4: `makeColdDataForChat` (`coldstorage.svelte.ts:806`) cold-stores an error-text chat again, orphaning the original key, and cleanup then deletes it. Orchestrator verified; fix folded in. Gate verdict: **approve with required changes**. HIGH: the 'keep live side fields' rule would push restored messages before hypa `startIdx`, which is memo-based (`hypav3.ts:212-228`, `hypav2.ts:382-396`, verified by the Orchestrator), so they would never be summarized again; replaced with a merge (v3 concat plus stripping the error memo, v2 prefer blob, lore concat, scriptstate live-wins). MEDIUM: the 'missing' notice would advise deleting the chat or restoring a .bin, which would lose post-error messages; backups omit error keys (`:676`, verified). LOW: detached-proxy check, tail after the await, notice placement, edit loss on remount, F4 test must keep the character hot. All folded into rev 7. Recorded in §6: backups omit error-key blobs; the restore-then-cleanup window while saves fail. |
| 45 | 2026-09-22 | CHORE-07 7c-2 post-implementation gate | `opus-reviewer`, fresh | ~89k, 30 tool uses | Ran `pnpm test`/`pnpm check`; import-graph DFS | Verdict: **approve with findings**; code matches §5.3 rev 7. Verified: Svelte 5 proxy identity check works (`proxy.js:42`, `:174-190`); the new `doingChat` import adds only a back edge, since `index.svelte.ts` was already reachable; `chatMemos` is saved as `string[]`. Findings, all fixed by the coder with behavioural RED: (1) MEDIUM: comment overstated the hypa merge, so the limit is now recorded in §5.3; (2) a malformed blob could restore `message` before the merge threw, so the merge now runs first and returns `'error'` (RL18 RED: TypeError rejection); UI try/catch added; (3) RL14's raw-object assertion could never fail, now read through `DBState.db`; (4) the `preserveOrphanedMemory` exception is added to the comment; (6) a summary left with no memos is dropped (RED: 2 items vs 1). Orchestrator re-ran: 38 files, 507 passed, 4 skipped; check 0/0. |
| 46 | 2026-09-22 | CHORE-07 7c-2 live check | Orchestrator, in the browser pane against the maintainer's dev server | n/a | Yes (live) | Seed fixture, Side Character 1. Wrote an OPFS blob (3 messages, one hypaV3 summary, lore, scriptstate) and added two temporary chats holding the pre-7b error text. **ok:** the notice rendered just above the input; Retry restored b1-b3 plus the post-error message, gave summaries [blob `b1,b2`, live `p1`] with the error memo stripped, lore from the blob, and scriptstate merged; the notice disappeared. **missing:** Retry showed the new missing notice, left `message[0]` untouched, and created no file. **Cleanup verified:** 2 chats, page 0, no LIVECHECK chats, no leftover OPFS file. |
| 47 | 2026-09-22 | AV-3 investigation (plain-HTTP encode) | `investigator`, fresh | ~129k, 63 tool uses | Read-only | Refined Report 12 §4: a second, **unbounded** cache of `getFileSrc` results, `fileSrcCache` (`parser.svelte.ts:431-441`), pins about 4/3·B per chat asset on plain HTTP for the life of the tab. `fileCache` is shared with the service-worker branch, whose entries carry no bytes. There are 39 call sites and one persisted field (`oder.img`, `Sidebar.svelte:685`). The clipboard allow-lists (`Chat.svelte:673,713,765`) exclude `blob:`. No CSP is active, and DOMPurify already keeps `blob:` (`parser.svelte.ts:113-119`). There are no tests of `fileCache`. The Orchestrator verified the key citations. The maintainer chose option (a), a byte-budget string cache, folded in `fileSrcCache`, and set 64 MiB. Plan: Report 15 rev 1. |
| 48 | 2026-09-22 | AV-3 plan gate (Report 15 rev 1) | `opus-reviewer`, fresh | ~85k, 29 tool uses | Read-only; `node -e` spy probe on `Buffer.prototype.toString` | Verdict: **approve with required changes**; option (a), the fold-in and 64 MiB judged safe; predicate mirrors `getFileSrc` exactly. MEDIUM: (M1) §3's "fewer reads, less memory" was false: HEAD's 200-entry cap holds more large avatars than 64 MiB, so scroll-back can re-read more, and small assets cost 4/3× on the V8 heap; (M2) an oversized asset plus the parser bypass would be read and encoded on every streaming update (`ChatBody.svelte:156`, `parser.svelte.ts:762,773`; verified by the Orchestrator), fixed with a one-slot memo; (M3) byte tests could not catch a drifting counter, so the seam must expose a recomputed total, plus an orphaned-retry test. LOW: spy on `globalThis.Buffer` after `polyfill.ts:43` (verified), non-empty T9 mock, byte-identical missing fallback, CSS-mode holders still copy (`characters.ts:74-84`, verified), 16 mocks need the predicate, call-site count 41 not 39. All folded into rev 2. |
| 49 | 2026-09-22 | AV-3 red-before-green | `test-warrior`, then Orchestrator rerun | ~190k, 100 tool uses | Yes (vitest) | Test-only seam (`__fileCacheTestHooks`, `isPlainHttpFileSrc` stub) with no behaviour change; T1-T13 in `globalApiFileCacheAv3.svelte.test.ts` and `parser/tests/fileSrcCacheAv3.test.ts`. On HEAD plus the seam, 7 failed behaviourally: T1 (3 encodes vs 1), T2 (5 vs 1), T3 (3 reads vs 4), T4 (2 entries vs 1), T9 (1 call vs 2), T11 (3 mismatches), T13 (2 encodes vs 1). The 6 guards passed. The Orchestrator reran and confirmed. Implementation by `sonnet-coder`: the Orchestrator rejected its first draft, which stored only the base64 payload and prepended the prefix on each call (each holder would get its own copy); `src` now holds the full string, and cost excludes the 22-char prefix. |
| 50 | 2026-09-22 | AV-3 post-implementation gate | `opus-reviewer`, fresh | ~61k, 12 tool uses | Ran `pnpm test`/`pnpm check` | Verdict: **REJECT on tests/comments only**; production code judged correct: output identical to 5084e1ee on every path, all Map mutations go through the helpers, eviction terminates, an oversized orphan cannot delete a retry's entry, and the predicate mirrors `getFileSrc`. Findings: T12's `maxBytes: 1` made the retry oversized, so its "commits normally" comments were false (verified by the Orchestrator); T11 never called `getFileSrc`; stale citations and present-tense process history in the tests; "HEAD" and "every caller shares one string" overstatements in production comments; no test can observe string identity, so full-string storage rests on review (recorded in Report 15 §2.1). Fixed by `sonnet-coder` (comments only) and `test-warrior` (T12 `maxBytes: 8` plus an entry-count check; T11 now calls `getFileSrc` per combination). Mutation probes: removing the identity guard fails T12; dropping `!usingSw` from the predicate fails T11; source restored byte-identical (`cmp`). Orchestrator reran: 40 files, 520 passed, 4 skipped; check 0/0. |
| 51 | 2026-09-22 | AV-3 live check | Orchestrator, in the browser pane against the maintainer's dev server | n/a | Yes (live) | localhost uses the service worker, so the seed page was switched to the plain-HTTP branch with `setUsingSw(false)` (restored by reload). The seed fixture has no avatars, so three temporary PNGs (317,127 / 308,138 / 14,286 bytes, 512 px) were written to real storage under `assets/av3live-*`. First call 4.7 ms; repeat 0 ms with no read; 5 concurrent calls shared one read (2 reads in total); all results equal; the `data:` string decoded to 512x512 in an `<img>`; running and recomputed byte totals agreed (833,688). With a budget of about 1.5 entries, re-requesting the first key re-read it (3 reads, 1 entry). With an oversized budget, a repeat call was served from the memo (1 read, 0 entries). **Cleanup verified:** keys removed and read back as null, `getItem` restored, cache reset, page reloaded. Console showed only the known KEI noise. Heap size on a Pi 3 is not measured. |
| 52 | 2026-09-22 | AV-4 facts: list render sites, resolution path, raw-bytes readers, canvas taint, storage, keys, animation, cleanup | `investigator` (plus one follow-up) | ~156k, 88 tool uses | No | Eight list sites, all fixed at 56 px (5 `'css'` via `BarIcon`, 3 `'plain'` via `SidebarAvatar`); no flag enlarges them. `readImage` returns raw bytes on every branch. The Tauri asset protocol is a separate origin, so the generator must draw from a `blob:` URL of raw bytes. `inlayStorage` precedent: a separate localforage instance, used on Tauri too, invisible to backup, sync and sweeps, with no eviction. `saveAsset` content-hashes paths, **except** multiuser `receive-asset`, which trusts a peer-supplied id, so the `assetIntegrity.ts:19-24` comment is false. No animation detection exists in `src/`. A later-resolving `getCharImage` promise renders with no extra wiring. Recorded in Report 16 §1. |
| 53 | 2026-09-22 | AV-4 measure-first (Report 12 §5) | Orchestrator, in the browser pane (seed tab) | n/a | n/a | 40 real hub avatars (25 JPEG, 15 PNG; 512 px to 3328x4864; up to 20 MB). Full decode 258 ms and **197 MB of bitmaps** in total, against 39 ms and about 4 MB as 160 px thumbnails. The four 832x1216 NovelAI-size PNGs (the maintainer's baseline) were 1.2-1.5 MB, decoded in 6.8-10.5 ms, and took 4.0 MB of bitmap each; their WebP thumbnails were 9-10 KB. `toDataURL('image/webp')` works in Chromium; WebKit is unverified. Best-case hardware (i9); the bitmap sizes do not depend on the hardware. **Conclusion: AV-4 is justified mainly by memory, not decode time.** |
| 54 | 2026-09-22 | AV-4 plan gate (Report 16 rev 1) | `opus-reviewer`, fresh | ~108k, 49 tool uses | No | **Approve with required changes.** R1: concurrency 2 with no timeout lets two stuck reads blank every list icon, and a FIFO queue makes on-screen icons wait behind rows already scrolled past; fixed with a 15 s timeout and LIFO. R2: `cleanChunks` returns before building its keep-set for account sync and for the default cold-storage setting (verified by the Orchestrator: `bootstrap.ts:570-575,293`, `database.svelte.ts:713`), so the sweep became independent. R3: a missing `readImage` result would have been stored as a permanent skip. R4: `getCharImage` compares the type literally in its early exits (verified, `characters.ts:57-70`). R5: memoizing a failure contradicted the retry test. R6: the path must never reject. R7: F7 was wrong; multiuser sends a full loc and writes `assets/assets/...png` (verified, `multiuser.ts:101-120`, `globalApi.svelte.ts:480-509`). R8: the test kinds were relabelled and T12 re-specified. R9: a canvas readback guard. All folded into rev 2. |
| 55 | 2026-09-22 | AV-4 implementation | `sonnet-coder` | ~174k, 110 tool uses | No | Implemented Report 16 rev 2: new `src/ts/media/avatarThumb.ts`, the `getCharImage` type mapping, 8 list-site type changes, and a detached sweep call in `bootstrap.ts`. `pnpm check` 0/0. **Its test report was wrong:** it said all failures were in the two AV-1/AV-2 suites, but the Orchestrator's own run showed 7 parser suites also failing to load, because their `globalApi` mocks had no `readImage`. |
| 56 | 2026-09-22 | AV-4 tests, red proof, mutation probes | `test-warrior` | ~307k, 150 tool uses | No | Added `readImage` stubs to 7 parser mocks; T12 mocks and tests in both AV-1/AV-2 suites, with no existing assertion changed; new `avatarThumb.test.ts` (T1-T8, T11, T13) and `getCharImageThumb.svelte.test.ts` (T9, T9g, T10). Red on the HEAD versions of `characters.ts` and the list sites: T9 6/7, T12 7/7; T9g and T10 pass there as guards. Mutation probes caught: FIFO by T5c, no timeout by T5b, memoized failure and skip-on-null by T6, no `wrapType` by T9. All swaps were restored byte-identical (`cmp`). Result: 42 files, 579 passed, 4 skipped; check 0/0. |
| 57 | 2026-09-22 | AV-4 post-implementation gate | `opus-reviewer`, fresh | ~103k, 20 tool uses | Ran `pnpm test`/`pnpm check` | **REJECT** (tests and one runtime gap; no data-loss, save-format or build defect). 1: T9g mocked `isThumbEligible` with a copy of its logic, so the account invariant was never tested against the real function. 2: the T12 titles claimed all 8 sites but covered 3. 3: a timeout during `readImage` did not cancel; the late read still decoded outside the concurrency limit (verified by the Orchestrator: `setCleanup` is registered only after the read, `avatarThumb.ts:476-504`). 4: a present-tense "HEAD" claim and process history in test comments. 5: T5b's "not stored" check was never observed, and one assertion could not fail. 6: an overstated module comment. 7: store operations had no timeout. The queue, never-rejects, cleanup, generator order, byte parsing, sweep isolation, `getCharImage` identity, circular import and compatibility were all confirmed correct. |
| 58 | 2026-09-22 | AV-4 re-gate after the row-57 fixes | `opus-reviewer`, fresh | ~87k, 16 tool uses | Ran vitest (excluding `.claude/**`) and `pnpm check`; results reproduced (42 files, 595 passed, 4 skipped; 0/0) | **REJECT, comments and test strength only;** runtime code judged sound. Findings 1-7 from row 57 are resolved: the store timeout leaves nothing dangling, and rejections are still handled; there is no await between the post-read cancellation check and `setCleanup`; a cancelled task never persists or memoizes; T14 and T9g exercise the real predicate. New findings: a test comment falsely called T5e and T14 red (they are guards; only T3b, T5d and T7b are red without the hardening); the lazy suite miscounted the remaining sites (7 of 8); the first lookups grid T12 test could not fail (the spy was not cleared first); stale mock headers; a wrong citation (`:793`, not `:781`); a misleading "today"; and the unreachable `setCleanup` branch should be documented as defensive. All sent for fixing. |
| 59 | 2026-09-22 | AV-4 row-58 fixes and Orchestrator check | `test-warrior` (tests) and `sonnet-coder` (one comment); checked by the Orchestrator | ~89k combined | No | Test comments now say only T3b, T5d and T7b are red without the hardening; T5e and T14 are guards. The lazy suite now counts six other sites. The first lookups grid T12 test clears the spy first; red proof: reverting `GridCatalog.svelte:110` alone fails it (restored, `cmp`). The lazy suite's grid test has no such hole, because its fake observer never fires before `fireOn`. Mock headers updated; citation `:793` fixed; "today" reworded; the `setCleanup` branch documented as defensive. The Orchestrator grepped the changed files for process wording (only an AV-2-era region name in `charlistAvatarLazy.svelte.test.ts:1080` remains, already committed) and re-ran: 42 files, 595 passed, 4 skipped; check 0/0. **A third full gate was not run:** the row-58 reviewer said checking the edited lines would be enough, and the runtime code did not change except for one comment. |
| 60 | 2026-09-22 | AV-4 live check | Orchestrator, in the browser pane (seed tab, maintainer's dev server, service-worker branch) | n/a | Yes (live) | The seed fixture has no avatars (31 characters, 0 with `image`), so temporary files were written under `assets/av4live-*`. **Formats:** an 832x1216 PNG gave `data:image/webp` 168x246, first call 37 ms; a 400x200 JPEG with EXIF orientation 6 gave 168x336, so the orientation was honoured; a 100x100 PNG, a GIF, an APNG (crafted `acTL`) and an animated-WebP header all fell back to `/sw/img/...` (full-size path) and were stored as skip records. `'thumbcss'` is wrapped exactly like `'css'`. The readback guard passed in Chromium. **Store:** 6 records with `v:1`; after a memo reset, a store hit took 0.3 ms with no `readImage` and no `createObjectURL`. A forced PNG fallback gave `data:image/png` (48,758 chars). **40 real hub avatars:** first pass 668 ms on the i9, with at most 2 running at once, all WebP, averaging 11.8 KB as a string; a pass served from the store took 2 ms. **Sweep:** with keep {base, stale}, the stale-`v` record, the orphan and the other keys were deleted and only base kept; the real `startAvatarThumbSweep` on the live DB (keep-set size 0) emptied the store. **Not measured:** heap delta; decoded-image memory is not visible to `performance.memory`. **Cleanup verified:** 47 asset keys removed and read back as null, 4 service-worker cache entries removed, test hooks reset, and the thumbnail store empty. The console showed only the known KEI noise. |
| 61 | 2026-09-22 | CHORE-01 + item 2: effect mechanics, the non-selected writer census, encoder side, trash harness, existing tests | `investigator`, two rounds | ~243k combined | No | Mechanism confirmed end to end (`dbChangeEffects.svelte.ts:103-112` gate; `risuSave.ts:284-308` stale-block reuse). Trash harness runs (1 passed, asserting the bug); it is the red-test seed once inverted. Census closed; unmitigated writers: trash restore (`GridCatalog.svelte:171-174`), V3 `setCharacterToIndex` (`v3.svelte.ts:884-889`), V3 `setChatToIndex` (`:947-956`, new), `setDatabase`/`setDatabaseLite` (`plugins.svelte.ts:752-781`, new; V3 re-exports them at `v3.svelte.ts:758-759`), risuaccess character/chat tools (`mcp/risuaccess/characters.ts:524-1005`, `chats.ts:43`, new). Cleared: group members, Lua/CBS, plugin `setChar`, multiuser `receive-char`, import/create, `removeChar` (self-mitigated at `characters.ts:851`), backup restore. Census commands: `grep -rn "characters\[.*\]\s*=\|characterTalks\|characterActive" src/ts/process/index.svelte.ts`; `grep -n "getCharacter(\|DBState.db.characters\[" src/ts/process/mcp/risuaccess/characters.ts src/ts/process/mcp/risuaccess/chats.ts`; `grep -rn "requiresFullEncoderReload\|selectedCharID.set" src/ts/process/mcp/risuaccess/`; `grep -n "declareAPI(" src/ts/process/scriptings.ts`; `grep -n "^\s*set[A-Z][A-Za-z]*\s*:" src/ts/plugins/apiV3/v3.svelte.ts`; `grep -n "export.*function removeChar" src/ts/characters.ts`; `grep -n "characters\[\|DBState.db.characters\|requiresFullEncoderReload\|setDatabase" src/ts/sync/multiuser.ts`. Report 11's 67/24 count could not be reproduced (its command was not recorded). |
| 62 | 2026-09-22 | CHORE-01 + item 2: measure today's effect and the design options (M1-M4), then re-baseline against the real app's boot proxy walk | `perf-analyzer`, two rounds | ~468k combined | No | M1: 74-385 ms per change at 10k-50k messages, 98.8-100% the chats snapshot, ~60 MB transient per flush. M2 (selection partition): field writes ~0.002 ms; tokens 0.02-0.6 ms only with per-message children (~680 B each). M3 (watch every character): first reported +550 MB, **corrected** to ~+170 MB over the real app, because `encoder.init` already materialises every proxy at boot (`globalApi.svelte.ts:743`, `risuSave.ts:250-258`; Svelte `proxy.js:165-199` creates sources outside effects too). M4 (identity only): ~free, misses in-place writes 0/15. The first M3 baseline was wrong; the Orchestrator's premise check caught it. Node, i9, pointer compression off. Harnesses `Agents/Tools/save-gen/dbchange-*.svelte.harness.ts`; raw `Agents/Tools/output/chore01-item2-measure.md`. Maintainer chose option B. Plan: Report 17 rev 1. |
| 63 | 2026-09-22 | Gate 1 on Report 17 rev 1 (CHORE-01 + item 2, option B) | `opus-reviewer`, fresh | ~121k, 51 tool uses | No | **REJECT.** Option B sound; three blocking factual errors, each hiding a loss path: (1) the census missed generation continuing after a selection change (`index.svelte.ts:264` captures the index once; hotkeys `defaulthotkeys.ts:87-96`/`hotkey.ts:103,119`, Playground, Home and `receive-char` switch without the `doingChat` check at `characters.ts:896`); (2) V2 `getDatabase` is a live wrapper (`plugins.svelte.ts:671-712`), so V2 `setDatabase` edits in place and an identity tracker sees nothing; (3) marks before the hook would be dropped during the un-awaited, seconds-long `encoder.init` (`bootstrap.ts:292`). Also: backup loads (`globalApi.svelte.ts:2923`, `accounter.ts:134`) lose non-selected characters today; multiuser `receive-chat` would re-encode everything per message; the plan's Svelte `ownKeys` reasoning was wrong; the Stage 2 shape rebuilt children on every `chatPage` change; the equivalence assertion could not fail. The Orchestrator re-verified 1-3 in source. All folded into rev 2. Census additions for re-runs: `grep -n "selectedCharID.set" -r src/ts src/lib` then check each caller for a `doingChat` guard; `grep -rn "setDatabase(" src/ts` for whole-db replacements. |

## Reading of the log so far (n=1 — not a conclusion)

Run 1 is the only data point and it predates the split, so it cannot settle anything. Two
observations that motivated the split, recorded while they are fresh:

1. **The valuable output was concentrated, and thinner than it first appeared.** Of the two
   findings that looked like they justified the Opus tier, only one survived: the
   `getModules()` load-bearing cache accident was real and decision-changing, while the
   DEV-inflation doubt was tested and disproven (1.18x, not a multiple). Most of the 43 tool
   calls were mechanical archaeology producing no reasoning of either kind. One genuine
   load-bearing find per ~100k tokens is the bar the mid-tier now has to clear — and it is a
   lower bar than the raw report made it look.
2. **Orchestrator verification caught real gaps in an Opus packet.** Both understatements
   above came from an *Opus* investigation, not a cheap one. This is the argument for the
   standing verification duty in 1.3 being tier-independent: the failure mode is
   compression, not model capability. It also means a mid-tier packet should not be held to
   a standard the Opus tier did not meet.
3. **A flagged doubt is not a finding until it is tested.** The Opus packet correctly
   identified a real DEV-only code path and correctly labelled its magnitude UNCERTAIN. The
   Orchestrator then spent a `perf-analyzer` dispatch settling it, and the answer was "no
   material effect." That sequence was correct and should be repeated — but it means an
   investigation's value should be scored on premises *settled*, not premises *raised*.

Do not draw a conclusion about escalation rate until there are enough post-split rows to
have one.

# Investigation Ledger

Records every investigation dispatched under the tiering in `AGENTS.md` 1.3, so the
two-tier split is **measured rather than assumed**. The split was adopted as an
architecture to test, not a settled conclusion; if the mid-tier proves unable to produce
packets good enough to resolve most investigations, this ledger is the evidence that
justifies reverting to Opus as the default investigator.

Record one row per investigation, at the time it completes. Token counts come from the
subagent usage reported to the Orchestrator.

## What the numbers are for

| Metric | Why it matters |
|---|---|
| Escalation rate | If it approaches 1, the mid-tier is not doing its job and the split is only adding a hop. |
| Escalations that materially changed the conclusion | The core value question for the Opus tier. Cheap confirmations are fine; *only* confirmations means the bar is too low. |
| Escalations that merely confirmed | Not waste at low rates — it is what a correctly-calibrated bar produces. Waste if it dominates. |
| Cost per investigation, by tier | The economic claim being tested. |
| Failures traceable to bad investigation | The correctness guardrail. Any occurrence is serious and should be written up in full, not just tallied. |

**Honesty rule.** Record outcomes including the ones that argue against the current
architecture. A ledger kept to justify a decision already made is worthless.

## Log

| # | Date | Question | Tier(s) | Tokens | Escalated? | Outcome |
|---|---|---|---|---|---|---|
| 1 | 2026-09-21 | Size the module-editor per-keystroke fix; verify the two `modules` effects | Opus (pre-split; dispatched as `opus-investigator`) | ~99.8k, 43 tool uses | n/a | Accurate on all claims spot-checked. Surfaced the `getModules()` load-bearing cache accident and a DEV-inflation doubt, both high-value — the doubt was later tested by `perf-analyzer` and disproven (production 1.18x faster, not a multiple). Understated two things caught on verification: two reverts of the proxy-tracking approach (reported one), and a load-bearing comment in `ModuleSettings.svelte` guarding against a prior double-insert bug. |

| 2 | 2026-09-21 | Stage A plan gate — falsify the narrowing of `stores.svelte.ts:197` to four module fields (`Agents/Reports/09-stage-a-module-effect-narrowing-plan.md`) | `adversarial-reviewer` (Sonnet 5) | ~54.4k, 28 tool uses | No | Verdict: proceed with named amendments. Could not falsify the three load-bearing claims (field list, scope boundary, subscriber-leak path) — independently re-derived each from source. Produced three real amendments, all verified before folding in: two off-by-one line citations; a better R1 precedent than the plan cited (`IrisModal.svelte`, `SegmentedControl.svelte` already use the `void x` idiom); and a real coverage gap (tests exercised the helper, not the wiring), added as a manual smoke-check requirement. Lesson: a gate can surface a better precedent than the plan's own — that's what it's for. |

| 3 | 2026-09-21 | Stage A post-implementation review — falsify the implemented narrowing, its tests, and the comments shipping with it | `adversarial-reviewer` (Sonnet 5) | ~60.4k, 25 tool uses | No | Verdict: ship with named fixes, both cosmetic. Independently re-derived the four-field list from source (no fifth field) and fact-checked all 11 shipped-comment claims as true. Caught one real doc defect: a stale line reference in the plan, from before its own leaf-file amendment. **Process failure:** the brief wrongly promised Bash the profile didn't grant; the agent disclosed this instead of quietly substituting reasoning for execution, and separately flagged that someone with Bash should re-run the suite — which the Orchestrator had already done independently (red 11 / green 27 / full 358 passed / check 0-0). Lesson: check the tool grant before promising a tool. |

| 4 | 2026-09-21 | Stage B plan gate — falsify the module draft-copy plan (`Agents/Reports/10-stage-b-module-draft-copy-plan.md`) | `opus-reviewer` (Opus 5) | ~128.3k, 51 tool uses | n/a (gate, not investigation) | Verdict: **REJECT**, 11 findings, correctly rejected. Two REJECT-grade findings, both Orchestrator-verified: **R1** the plan's flush-on-unmount claim was false — Tauri desktop has no unload hook, and cold storage reloads bypass the un-cancellable path. **R3** duplicate module ids are reachable (`.risum` imports keep their id), so a bare `findIndex` can clobber the wrong module under commit-on-destroy. Also caught a false perf attribution the Orchestrator had written (R2: post-Stage-A cost is 0.03 ms, not 29.21 ms — the plan summed pre-Stage-A numbers), a test seam that would re-run Stage A's effect (R5), and a broken empty-draft predicate. **The reviewer was itself wrong twice**, both caught on verification: an off-by-one citation (R4) and a replacement count that didn't reproduce (R7: cite the command, not the number). Net: the gate paid for itself several times over. |

| 5 | 2026-09-21 | Does any awaitable "pending DB writes flushed" signal exist, for a Tauri `onCloseRequested` draft flush? | `investigator` (Sonnet 5) | ~58.6k, 27 tool uses | No | **Refuted the feasibility premise** behind the maintainer's chosen option: no awaitable 'writes flushed' signal exists — `dirtySinceLastSave`/`changeTracker` are function-local inside `saveDb()`, and the only exported signal (`saving`) is racy on its true→false edge. No existing teardown path waits on `saveDb()`; reload sites either accept loss or bypass the loop. Flagged unprompted that the migration-lock pattern looks copyable but only wraps the raw write, not the encode/debounce. **The Orchestrator then found the reframing fact** from the investigator's own trail: the save debounce is 500ms trailing and resets on every keystroke, so continuous typing produces zero disk writes today until a pause — today's durability is not per-keystroke, which made the planned Tauri hook and save-loop surgery likely unnecessary. No escalation needed. |

| 6 | 2026-09-21 | Stage B plan gate round 2 — falsify revision 2, including the new save-loop seam (§3.7.4) | `opus-reviewer` (Opus 5, fresh context, explicitly unbound from round 1) | ~130.0k, 44 tool uses | n/a (gate) | Verdict: **REJECT**, 11 findings, 6 REJECT-grade — sharper than round 1. Best finding: revision 2's duplicate-id fix was circular (it validated a stored index using the same bare `findIndex` that produced the wrong element, while *increasing* reachability by firing on debounce/unmount/pagehide/close). Second: reading the Tauri **runtime** (not just typings) showed `onCloseRequested` calls `destroy()` unless `preventDefault()` fires first — revision 2's shape opts out of its own close on any throw, stranding an unclosable window. Third: the flush-correlation proof cited the wrong line (a dirty-block flag, not the data) and relied on an unstated ordering invariant. Also caught a citation off by 2x (3 sites claimed, 6 actual), a fabricated supporting sentence, and a retracted claim still live in the plan. Confirmed sound: the corrected debounce baseline, the resolve-edge choice, and the byte-identity argument. **Orchestrator note: across both Stage B gates, most of the 22 findings were errors in the Orchestrator's own plan text, including two that would have shipped a silent-data-loss path while claiming to close one.** Plan text is not trustworthy on first draft; the gate paid for itself many times over. |

| 7 | 2026-09-21 | Benchmark the partition branch point: per-module vs whole-array snapshot, asset-heavy shape, module-count scaling, `$effect` overhead | `perf-analyzer` (Sonnet 5), two dispatches | ~91.0k + ~157.3k, 57 tool uses | No | **Measurement campaign that replaced three rejected plan revisions with numbers**, reproduced independently by the Orchestrator on every headline figure. Partitioning takes a keystroke from ~29.8 ms (whole-array snapshot) to ~4.2 ms (largest module) / ~0.2 ms (median). Per-element sum equals the whole-array walk (1.01x — shape changes are not a regression). `$effect` scheduling/teardown overhead of N children vs 1 is below measurement noise (0.92-1.01x at N=52/104). Flagged its own unmeasured gap unprompted (effect overhead), then commissioned. **Most valuable result:** snapshot cost tracks node count, not bytes — a 1.03 MB asset-shaped module costs 35.6 ms vs 4.2 ms for an equal-size cjs-heavy one, ~8x at equal size, which bounded the partition's claim to non-asset modules instead of overstating it. |

| 8 | 2026-09-21 | Real-app end-to-end: is the cost the effect or the DOM, and what does it cost at real module counts? | Orchestrator direct (browser pane, live dev server) | in-session | n/a | Live-app measurement (module editor open, real `input` events; all mutations restored and verified): effect 11.5 ms vs DOM ~2.2 ms at 52 modules — the effect is ~84% of the cost, answering the ceiling question and clearing the last planning blocker. 104 modules: 25.1 ms (150% of frame budget), 1.9x over 52 — linearity confirmed on live data. Corrected the harness README's caveat: Chromium is ~2.6x faster than the Node harness on identical data (ratios still valid, absolute numbers don't transfer). Downstream effect work beyond the raw snapshot is ~0.2 ms, negligible. |

| 9 | 2026-09-21 | Stage B partition plan gate — falsify the per-element effect partition | `opus-reviewer` (Opus 5, fresh context) | ~110.6k, 36 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS] — first approval in Stage B's work, after two rejections of the draft-copy design.** Could not construct a missed-mutation path; proved dependency-closure equivalence from Svelte 5.55.1 source (the array `version` source is not registered by `$state.snapshot`) and worked the full mutation matrix (leaf/nested/replace/push/splice/sort/reassign/self-assign/holes/length=0) plus same-batch interleaves. All 7 findings were in the plan's artifacts, not the design. **Best finding (F-1):** the plan's 'pattern already used for characters' precedent is itself unsafe — the existing character tracker is gated on selection, so a mutation to a non-selected character with an existing block is never re-encoded; logged as a separate bug (became row 10), not absorbed here. Also caught a test assertion that would break once modules are populated, and that no test in the repo had ever exercised an `$effect` created inside a re-running `$effect` — the load-bearing untested mechanic. |

| 10 | 2026-09-21 | Scope (not fix) the non-selected-character marking bug surfaced by gate 9 | `investigator` (Sonnet 5) | ~101.2k, 67 tool uses | No | **Model case for the mid-tier.** Confirmed the mechanism, then traced 67 raw candidate writes across 24 files individually (not grep+infer): ~60 resolve to the live selection and are not instances. Narrowed to three real writers, one self-mitigated (`coldstorage.svelte.ts` calls a full-encoder-reload). Found the decisive mitigation unprompted — selecting the affected character later re-captures the edit, so the failure mode is 'lost only if never reopened before reload', not unconditional loss. Two bonus findings: the **restore-from-trash bug** (concrete repro: restore, don't open, close app, it returns to trash) and that `toSave.chat` is dead in the encoder (so a fix needs no per-chat work). Honest limit: did not exhaustively sweep MCP/risuaccess write paths. Orchestrator re-verified all four headline claims; no escalation needed. |

| 11 | 2026-09-21 | Stage B post-implementation review of the partition diff | `opus-reviewer` (Opus 5, fresh) + one re-review pass | ~115.0k + ~127.2k | n/a (gate) | **[REJECT] then [APPROVE].** First pass independently derived dependency-closure equivalence, worked the full mutation matrix including a module object shared by two indices, and fact-checked all six claims in the new code comment as true — **the executable change was correct on first submission.** The REJECT was entirely on test-file comments: a now-false 'not modified' claim, stale citations, and an arithmetic error that originated in the Orchestrator's own brief (6+N+1 vs the correct 6+N, disproved by the file's own passing test). Comment-only fix dispatched; re-review confirmed nothing executable changed. |

| 12 | 2026-09-21 | Empirically reproduce the restore-from-trash data-loss bug (not just source-trace it) | `test-warrior` (Sonnet 5) | ~161.0k, 77 tool uses | No | **Bug is real; reproduced end to end**, driving the real save/decode pipeline rather than reimplementing it (a clean disproof was pre-authorized). After restoring a non-selected character exactly as the UI does, the character is absent from the encoded save and the decoded `trashTime` is unchanged — confirmed the claimed mitigation (reselecting and saving clears it) also holds. Orchestrator re-ran independently, identical values. Contributed a durable harness-convention finding, now documented in `Agents/Tools/README.md`: splitting a `vi.mock` factory for an app rune module across files via dynamic `import()` lets real top-level `$effect.root` blocks fire against thin mocks. Expensive (77 tool uses), mostly spent isolating that mock-splitting failure, diagnosed correctly rather than worked around blindly. |

| 13 | 2026-09-21 | Phase 2 item 3 premise: how the chat list, sidebar character list, and the 3-layout character list page render and load avatars; enumerate virtualization hazards | `investigator` (Sonnet 5) | ~144.6k, 47 tool uses | No | **Premise partly confirmed, partly corrected, scope widened** (detail: Report 12 problem statement). Chat list already windowed (imperative mount/unmount, `loadPages` cap, never shrinks) — the Roadmap's 'unbounded' wording was imprecise. Grid/list/simple character lists render every character with no windowing, resolving avatars inline. On plain-HTTP, every avatar lookup re-encodes the full asset to base64 even on a cache hit. Named hazards for a later lazy-mount: plugin `SafeDocument` exposes the live chat DOM, several DOM-order assumptions, no shared parse cache. Corrected one handoff citation. Orchestrator re-verified. |

| 14 | 2026-09-21 | Maintainer hypothesis: chat data (heavy for asset/CSS-intensive bots) is held in RAM when not needed, causing Android OOM and Tauri slowdown | `deep-investigator` (Opus 5), escalated from Reports 01/01-deepdive (static only, one COULD NOT DETERMINE) at maintainer request | ~161.3k, 83 tool uses | Yes (from prior reports) | **Residency confirmed; weight is elsewhere than hypothesised; Android premise unobservable.** Every non-cold character/chat is decoded eagerly and stays resident (detail and proxy-overhead numbers: Roadmap item 8). **Escalation changed the conclusion:** Report 01's 'proxies recursively in one pass' mechanism was wrong (Svelte proxies lazily); full materialisation is instead forced by boot's `saveDb()` → a full `JSON.stringify` through the proxy. Refuted 'asset/CSS-heavy chat data' as the driver (assets are paths; CSS/HTML is character data). No Android build exists in the repo. Corrected three stale claims from the earlier deep dive (fileCache now 200-LRU, translator cache capped at 500, fileSrcCache holds full base64 on plain-web). New findings folded into the Roadmap as CHORE-06 (console retention) and CHORE-07 (cold-storage read failure), plus a per-chunk streaming cost (item 2). Minor process slip: a throwaway probe file was created and deleted in the repo root; confirmed gone. |

| 15 | 2026-09-21 | Empirically reproduce CHORE-07 (a transient cold-storage read failure orphans a chat) against unfixed code | `test-warrior` (Sonnet 5) | ~97.8k, 31 tool uses | No | **Reproduced on the first run, including the blob deletion** (harness: `cold-storage-orphan-repro.svelte.harness.ts`; disproof pre-authorized). Drives the real cold-storage and save/decode pipeline, mocking only the OPFS backend. Chain confirmed: a transient read failure writes an error message indistinguishable from a genuinely missing blob, and a later cleanup deletes the still-intact blob. Corrected line ranges; reused row-12's mock-isolation lessons to find two new transitive import traps. Orchestrator re-ran (exit 0) and extended the scope from source: the same swallow exists in the Node-server and Tauri branches; the account branch is inferred, not reproduced, to return null the same way on a non-200 response. `pnpm test`: 32/377/3, exit 0. |

| 16 | 2026-09-21 | Real-Chromium measurement of item 8's proxy multiplier and CHORE-06's console retention (the browser pane has no `gc()`, so neither could be measured there) | `perf-analyzer` (Sonnet 5) | ~152.3k, 91 tool uses | No | **Both premises measured in real Chromium** (detail and numbers: Roadmap items 6 and 8). Proxy overhead in Chromium is about half of Node's, because of pointer compression. Console retention: ~95.5 MB stays pinned after a `console.log` of a large object graph, released by `console.clear()` or ~1000 further logs. **Not propagated, unverified:** the agent's observation that V8 heap counters may under-report some large/single-character strings (plausible, not checked, matters only for a future large-string measurement). Agent validated its own fixture and did its own git-status check correctly (flagged row 15's untracked harness as not its own). Orchestrator verified the table against the raw result files and confirmed no leftover headless Chrome processes. |

| 17 | 2026-09-21 | Item 3 premise: count avatar lookups (`getFileSrc`) per layout × N × action in the character list, against unmodified code | `test-warrior` (Sonnet 5) | ~188.2k, 63 tool uses | No | **Premise confirmed for grid/list, partly contradicted for the default 'simple' layout** (numbers: Roadmap item 3). Simple layout does 0 lookups on search (filters via `{#if}` inside the loop) but N on name/image/interaction/chat-count changes. Harness hit a real OOM at 72 fresh 1000-character mounts; fixed by reusing mounts. **Shipped-comment defect found at verification:** a comment claimed `sortChar` never reads chats, but it reads chat count — the 0-on-message-push result holds only because a push doesn't change the count; corrected before commit. Orchestrator re-ran (exit 0) and confirmed the mechanism in `MobileCharacters.svelte`/`MobileBody.svelte`. `pnpm test`: 32/377/3, exit 0. |

| 18 | 2026-09-21 | Planning facts for avatar stages A (stop re-lookups) and C (lazy-mount): call sites, Svelte 5.55.1 equality and each-block semantics, sidebar mechanics, reusable lazy infrastructure, hazards | `investigator` (Sonnet 5), 1 `code-searcher` survey | ~131.8k, 42 tool uses | No | **Useful, with one inference disputed and left for the harness to settle** (later settled empirically by the AV-1 gate, row 20 — the Orchestrator's dispute was correct). Found 26 `getCharImage` call sites, 8 of them O(N); a 4th hot path the brief missed: `AlertComp.svelte`'s `selectChar` dialog iterates every character inline. The Sidebar's avatar-rebuild effect only skips no-op passes (a lodash `isEqual` guard), so any single rename or avatar change re-resolves every sidebar avatar — the same defect as the character-list `formatChars`, one level up. `LazyPortal.svelte` has zero call sites and is one-shot, so it defers cost without bounding it. List/trash rows and open folders have non-uniform heights, favouring per-item IntersectionObserver over fixed-height windowing. Orchestrator verified the equality/each-block semantics and the zero-`LazyPortal`-users claim. |

| 19 | 2026-09-21 | Planning facts for avatar stages B (plain-HTTP encode) and D (thumbnails): `getFileSrc` callers' dependence on `data:` URLs, cache semantics, resize precedent, invisible thumbnail storage, avatar identity, Tauri cost | `investigator` (Sonnet 5), 1 `code-searcher` survey | ~117.1k, 57 tool uses | No | **Mostly correct, one wrong claim caught at verification.** Findings: the clipboard 'copy as HTML' allow-lists exclude `blob:` (would silently drop images if adopted); revoking `blob:` URLs on LRU eviction is unsafe while `<img>` elements still hold the resolved string; a canvas-thumbnail precedent already exists in production (`BotSettings.svelte`); avatar paths are content-hashed, so a path-keyed thumbnail can't go stale; Tauri uses the stock asset protocol with no custom handler, so full-size decode per icon is architecturally confirmed (cost unmeasured); a pre-existing MIME anomaly labels all assets `image/png`. **Wrong claim:** 'oder.img is never read back anywhere' — `backuplocal.ts` reads it as an asset-map key, and it holds the `getFileSrc` result in `db.characterOrder`, so on plain HTTP each folder image is persisted as a full base64 `data:` URL in the save; AV-3+ must keep `blob:` URLs out of that field. Orchestrator verified the corrected claim and the other citations. |

| 20 | 2026-09-21 | AV-1 plan gate: falsify `Agents/Reports/12-charlist-avatar-plan.md` §2 | `adversarial-reviewer` (Sonnet 5) | ~156.7k, 60 tool uses | n/a (gate) | Verdict: **approve with required changes.** Settled the disputed reactivity mechanism (row 18) empirically by compiling and running real 5.55.1 components: `{@const}` lowers to `$.derived`; two-level `{@const}`/nested-derived wrappers do NOT re-run on an equal-value replacement (0 calls), confirming the Orchestrator's dispute of row 18's claim. Findings: the new suite must reuse mounts and keep N small or it risks the harness's OOM in shared `pnpm test`; a citation off by one; and **the base64 ratio is 1.33x (4/3), not 1.37x** — an Orchestrator arithmetic error that had propagated into the Roadmap, ledger row 17 and Report 12, all three now corrected. No delete-while-open test existed (reasoning says safe, untested). Orchestrator re-ran the probe (exit 0, 7/7, identical counts). |
| 21 | 2026-09-21 | CHORE-07 fix-planning facts: callers of `getColdStorageItem`, plugin contract, missing-vs-error per backend, the `preLoadChat` UI, cleanup reach, tests | `investigator` (Sonnet 5), no code-searcher | ~134.1k, 51 tool uses | No | **High value, two errors caught at verification** (one turned a 'neutralised' finding into a live asset-deletion hypothesis). Plugin contract: `getColdStorageItem` must keep its never-throws external contract; the chat UI's `{#await}` has no `:catch`, so `preLoadChat` must never reject. Node server already distinguishes missing-vs-error one layer down; OPFS and Tauri precedents for a clean not-found check exist. Account branch has no try/catch (a network/parse failure throws uncaught). `cleanColdStorage()` is manual-only.<br>**Error 1 (Orchestrator correction):** the startup asset-sweep gate checks the **global cold-storage setting**, not per-character — so anyone with the setting off (including former cold-storage users who disabled it) runs the destructive sweep on every launch; a failed cold read then leaves a character stub whose emotion/additional images look unreferenced and are deleted from disk. Source-traced, not reproduced; plausibly matches maintainer reports of 'images going missing'.<br>**Error 2:** send-while-cold loss can happen during the pending `{#await}` window (no cold-pointer guard on send), contradicting the investigator's 'cannot happen' claim. Orchestrator verified both corrections in source. |
| 22 | 2026-09-21 | Reproduce: startup asset cleanup deletes a cold-stored character's assets after one failed cold read (hypothesis from row 21) | `test-warrior` (Sonnet 5) | ~107.5k, 37 tool uses | No | **Confirmed** (detail: Roadmap CHORE-07 second loss path). Harness `asset-gc-cold-read-repro.svelte.harness.ts` drives the real `getUncleanables` and cold-storage read/write with an in-memory Tauri FS. A single transient read failure causes the same asset deletion as a genuinely missing blob, while the setting-off gate applies. Web branch also deletes (source-read, not harness-driven — honestly labelled as a limit). Orchestrator re-ran (exit 0, 4/4) and confirmed the web-branch lines. Agent's own `pnpm test` showed exit 1 solely from an already-known deliberately-red suite; everything else matched baseline. |
| 23 | 2026-09-21 | CHORE-07 plan gate, rev 1 (`Agents/Reports/13-chore07-cold-read-failure-plan.md`) | `opus-reviewer` (Opus 5) | ~139.7k, 60 tool uses | n/a (gate) | Verdict: **approve with required changes**, 1 BLOCKER + 6 MAJOR, all Orchestrator-verified before folding into rev 2 (Report 13 §2.2b picks up the fixes). **B1:** the plan's claim that its merge made a send-during-loading safe was false — the stream writes into a captured message index and the plugin `sendChat` bypasses the guard entirely; the guard had to move to `sendChat`'s entry. **M1:** 'keeps showing loading' was false — it resolves to a blank `<div>`; a notice became mandatory. **M2:** the merge lost edits to `localLore`/summaries/`scriptstate` made during the failure window. **M3 (the Orchestrator's own propagated error, first made at row 21):** 'getItem never throws' is false — the account branch can reject. **M4:** Tauri `exists()` returns false on any metadata error, not just not-found. **M5/M6:** already-hit users' blobs get deleted by the manual cleanup button, and a restore landing after a character switch (CHORE-01) is never saved and can then be deleted. **Lesson recorded:** the Orchestrator verified a citation existed but not that it said what was claimed — verify the claim, not just the citation. |
| 24 | 2026-09-21 | AV-1: write the red-before-green suite test against unfixed code | `test-warrior` (Sonnet 5) | ~206.1k, 100 tool uses | No | **Red as predicted**, new file `charlistAvatarLookups.svelte.test.ts`, mounts reused, N=60. Every O(N) case failed on unfixed code with the plan's exact counts (grid/list search-all 60→0, search-narrow 6→0, change-image 60→1, simple rename 60→0, Sidebar rename 25→0). Six cases already passed and are labelled preserved behaviour. Sidebar and AlertComp coverage was feasible after all, needing extra store-key wiring. Also fixed a stale harness comment. The agent caught itself reading a false exit-0 through a pipe and re-checked un-piped. Orchestrator re-ran: exit 1, 10 failed/6 passed, messages identical. Expensive (100 tool uses), mostly isolating import graphs, done safely in throwaway probes it deleted. |
| 25 | 2026-09-21 | CHORE-07 plan re-gate, rev 2 | `opus-reviewer` (Opus 5), same instance as row 23 | ~186.3k, 25 tool uses | n/a (gate) | Verdict: **approve with required changes.** Resolved B1/M1/M3/M4; partly resolved M2/M5; **M6 not resolved.** **New BLOCKER R1:** rev 2 put the chat-shape check in the shared reader, which also serves `{character}` blobs and arbitrary plugin values — every cold character would become unopenable. **R2 (from git history):** legacy array-format blobs never reset their side fields, so 'restored' would wipe real memory/lore; hypaV2 chunk ids must be restored not concatenated; hypaV3 and localLore need id-based merges, not overwrite. **R3:** the reload trigger doesn't actually save and would re-encode every character; needs a targeted mark-for-save. **R4:** the send guards must run before the chat-mutating side effects they're meant to prevent. **R5:** recovery matched the wrong template line, unchanged since an upstream data-loss fix; must refuse during a send. **Plugin storage:** rejecting on failure recommended, low-to-moderate risk.<br>**Two rounds each produced a new blocker, so the work was staged**: stage 7a ('stop deleting', no UI/plugin/load change) planned in full and gated first; 7b/7c carry every other finding into their own gates. |
| 26 | 2026-09-21 | CHORE-07 stage 7a gate (rev 3 §2) | `opus-reviewer` (Opus 5), same instance | ~207.8k, 9 tool uses | n/a (gate) | Verdict: **approve with required changes** (folded into Report 13 §2.2b/§3). **F1 (MAJOR):** the error-text keep-list was blind to chats inside characters that are themselves cold-stored, since stubs record only pointer chats — so in the likely real sequence (damaged chat left unopened, character later goes cold, cleanup runs) the blob would still be deleted; `cleanColdStorage` must read cold character blobs and abort on any incomplete read. **F2:** the 'safe after 7a' user advice overclaimed, since unsaved restores stay exposed until 7b. Confirmed the upstream commit and its parent's prior behaviour. |
| 27 | 2026-09-21 | AV-1 post-implementation gate | `adversarial-reviewer`, fresh | ~100.5k, 30 tool uses | n/a (gate) | Verdict: **PASS with findings, none blocking.** Cleared key-uniqueness, click-closures after delete/search, Sidebar `@const` legality, no-stale-avatar guarantee, plugin whole-array-replacement safety. Ran tests (16/16) and svelte-check (0/0). **Low (Orchestrator-verified):** in dev compiles `{@const}` evaluates eagerly for TDZ diagnostics, so GridCatalog calls `getCharImage` once per image-less character at item creation — harmless (early return on falsy `loc`), production compiles stay lazy, no change needed. Reviewer's own limit: red-before-green was not re-executed (shared tree; already recorded at row 24). The implementer ran `git stash`/`pop` in the shared tree — nothing lost, but agents are now told not to. |
| 28 | 2026-09-21 | AV-2 evidence packet: scroll roots, consumers, IntersectionObserver, memory holders | `investigator` | ~108.2k, 48 tool uses | No | **Sharpened premise:** the nested MobileCharacters-inside-GridCatalog case is the DEFAULT phone path ('Simple' is GridCatalog's default tab; standalone MobileBody needs opt-in Beta Mobile GUI). BarIcon/SidebarAvatar already have same-size non-image states, so no layout shift from lazy resolution. **happy-dom's IntersectionObserver is a no-op stub**, so tests need an injectable fake. On plain-browser, only raw bytes are cached; the base64 string is rebuilt per call, held only by the DOM and the derived. Adjacent O(N) sites recorded for later folding. Uncertain: which nested box scrolls (CSS reasoning, not measured — became AV-2 step 0), and whether Chromium frees decoded bitmaps for `data:` URLs. Orchestrator spot-checked all four headline claims. Led to Report 14. |
| 29 | 2026-09-22 | CHORE-07 7a post-implementation gate | `opus-reviewer` (Opus 5), fresh instance | ~74.5k, 21 tool uses | n/a (gate) | Verdict: **REJECT, with a small fix.** Deletion guards themselves correct. **MAJOR (Orchestrator-verified):** the new try/catch made `getUncleanables` swallow a throw it used to propagate — the only thrower is the account branch, which `loadDrive` relied on to abort a restore with 'Backup Error'; now a network failure yields a stub-based image list and the restore 'succeeds' silently missing assets. Not deletion, but a loud failure became a quiet one. Asset sweep itself unaffected (account skips it). Minor: stale citations, a nonexistent test helper name, test gaps. Cleared: healthy-install identity, remotes-by-reading, cleanup ordering, template byte-identity, backups untouched. |
| 30 | 2026-09-21 | AV-2 plan gate (Report 14 rev 1) | `adversarial-reviewer`, fresh | ~97.9k, 36 tool uses | n/a (gate) | Verdict: **approve with required changes** (folded into Report 14 rev 2). **BLOCKER:** the observer registry's lifecycle was unspecified — a Map keyed by root pins every detached root, since AlertComp mounts a fresh box per dialog open; fixed with a WeakMap by root, deleted on empty. **MAJOR:** the nested-root outcome has no automated guard (happy-dom has no layout); accepted as fail-open with a dev-only log. Minor: stale citations after AV-1's line shifts; SidebarAvatar's no-src branch exercised only by folders; the 'ledger 18, Q7' citation in Report 12 is not traceable as written. Cleared: the SvelteSet has()-tracking argument, same-size sentinels, `data-char-id`, AlertComp keying safety, plugin DOM safety. |
| 31 | 2026-09-21 | AV-2 step 0: does clearing a `data:` URL free the decoded bitmap? | `perf-analyzer` | ~132.3k, 45 tool uses | Yes (headless Chrome 154 over CDP, standalone probe) | Measured (detail: Report 14 §2.1): CSS `background-image` releases 99.8% on clear+gc; `<img src>` frees the string but decoded bitmaps sit in a bounded engine-cache floor (not an unbounded leak). CDP forcible-purge destroyed the tab context in this build; GPU process not separable; headless vs headful reasoned, not measured. Decision: release goes into AV-2; AV-4 thumbnails fix bitmap size. |
| 32 | 2026-09-21 | CHORE-07 7a: the recorded RED runs (the evidence the guard test file points to) | `test-warrior`; re-run by the Orchestrator | n/a | No | File `coldStorageDeletionGuards.svelte.test.ts`. **Against unfixed source:** Orchestrator re-ran, exit 1, 7 failed/4 passed/1 skipped — Tauri, web/forage, missing-blob and chaId-mismatch cases all failed to protect assets; key-orphan cases failed too. **Against the round-1 fix (not shipped; row 29 REJECT):** one regression pin failed (a promise resolved instead of rejecting), confirming it as a pin, not a RED test against HEAD. **After the round-2 fix:** 15 passed/1 skipped, exit 0 (Orchestrator re-run). |
| 33 | 2026-09-21 | CHORE-07 7a re-gate (round 2) | `opus-reviewer`, same instance as row 29 | ~103.9k, 12 tool uses | n/a (gate) | Verdict: **REJECT on wording only; the code is approved.** `getUncleanables` throws exactly as HEAD; the collector and pointer-branch cases verified. Blocking: test-file text pointed to git history for RED failures that wouldn't exist there (now row 32); mislabelled an already-passing test as an open gap; 'today' wording meant 'before the fix'. Minor: an alert could stay up on account non-200; stale citations; a hardcoded English string leaves the lang fallback dead. |
| 34 | 2026-09-21 | CHORE-07 7a re-gate (round 3) | `opus-reviewer`, same instance | ~120.5k, 6 tool uses | n/a (gate) | Verdict: **APPROVE with findings.** The abort keeps its own message because it returns before the generic catch; only pre-deletion failures reach it, so 'nothing else was affected' holds in both languages. **Correction to the Orchestrator's brief:** a throw cannot come after partial removals (errors are logged, the loop continues); no artifact repeats the wrong claim. Remaining: citations drift again — cite by symbol, not line. Pre-existing, unchanged: failed removals are silent. |
| 35 | 2026-09-21 | AV-2 post-implementation gate | `adversarial-reviewer`, fresh | ~176.4k, 63 tool uses | Probes in scratch | Verdict: **approve with required changes.** **BLOCKER (Orchestrator-verified: no try/catch in `nearViewport.svelte.ts`):** a throwing IntersectionObserver constructor is unhandled on a later reactive mount — for Sidebar, all navigation could vanish; contradicts the plan. **MAJOR:** the shared visible-tracking sets never shrink on unmount except via the far-band callback, so tab switches/folder close/search narrowing let them grow back toward 'everything resolves'. Cleared: three-level `@const` gating, registry lifecycle, real boxes at every site, SidebarAvatar undefined src, missing-IO fail-open, hysteresis ordering, AlertComp index key. 424/424 and 0/0 re-run. Fix order: RED tests first, coder on hold until recorded (became rows 36-37). |
| 36 | 2026-09-21 | AV-2 post-gate: RED tests v11/v12 before the fix | `test-warrior`; re-run by the Orchestrator | ~169.8k, 47 tool uses | No | `charlistAvatarLazy.svelte.test.ts` gained v11 (throwing constructor on a later reactive mount) and v12 (tab switch, search narrow/restore, Sidebar folder reopen). Orchestrator re-ran: exit 1, 5 failed/18 passed. **Mechanism corrected from the reviewer's:** not a silent collapse but an uncaught rethrow (Svelte's `mount()` boundary has no onerror), which also left a half-mounted component polluting later tests until each test unmounted in `finally`. Design approved for the fix: a try/catch fail-open with registry cleanup, plus per-key ownership, order-independent. |
| 37 | 2026-09-21 | AV-2 re-gate after the BLOCKER/MAJOR fixes | `adversarial-reviewer`, same instance as row 35 | ~235.1k, 80 tool uses | Probes in scratch | Verdict: **APPROVE.** Try/catch fail-open confirmed live (v11 prints the real warning). A scratch probe for near-succeeds/far-throws showed correct disconnect-and-remove behaviour. Per-key ownership has no leak (unobserve precedes delete; mismatched release is a no-op); teardown runs in the action's own cleanup, not a derived (no `state_unsafe_mutation`). SvelteSet gating preserved: ownership reassignment doesn't re-invoke the lookup. 431/431 and 0/0 re-run. Minor: add a partial-construction test. Pending: the live dev-server check (became row 38). |
| 38 | 2026-09-21 | AV-2 live check (plan §5) | Orchestrator, in the browser pane (Chromium) against a dev server the maintainer started with the legal flag inline | n/a | Yes (live, i9) | Passed (detail: Report 14 §5.2). Live pane held the seed fixture, temporarily given generated avatars and duplicates, then restored and verified. Confirmed the hidden-pane rule (no IntersectionObserver fires at all when the pane is not visible — a live-check precondition, now standing policy). Grid resolved 151/151 (expected at this screen size); Trash 0; Sidebar 21/29; the selectChar dialog resolves in a real browser (display:contents fix confirmed). Nested phone path settled: the inner MobileCharacters box scrolls, the outer GridCatalog box does not, confirming row 28's inference. **Pre-existing, unrelated, found incidentally:** `saveDbKei` reads `db.account.kei` with no guard, so it throws and logs on every save when `account` is undefined. |
| 39 | 2026-09-21 | CHORE-07 7b plan gate (Report 13 §4) | `opus-reviewer`, fresh | ~79.5k, 25 tool uses | No | Verdict: **approve with findings**, for a minimal core. Key finding (Orchestrator-verified): once a failed load stops writing the error text, the pointer stays on screen indefinitely, so `/cut`/`/del`/`/multisend clear` could drop it and a later cleanup could delete the blob — the core therefore had to include a `sendMain` guard and a notice, not just 'no mutation'. Deferred to 7c: the three-way reader, side-field merges, the plugin `sendChat` guard, save-marking, a Retry button. Maintainer approved the core; the firm 'delete this chat' notice waits for a reliable missing-vs-error signal in 7c. |
| 40 | 2026-09-21 | CHORE-07 7b post-implementation gate | `opus-reviewer`, fresh | ~63.7k, 21 tool uses | Ran `pnpm test`/`pnpm check` | Verdict: **REJECT on wording only**; no code defect. BLOCKER: a doc comment claimed two production consumers that don't exist. MAJOR: test comments called the pre-fix code 'HEAD'/'currently'. Minor: stale 'still loading' text; a ruled-out null-safety nit. RED claims verified against the pre-7b code. Coder fixed all four; Orchestrator verified directly (comment/text only, no re-gate). Committed `3e17c8a3`: 36 files, 445 passed, 4 skipped; check 0/0. |
| 41 | 2026-09-21 | CHORE-07 7c-1 plan gate (Report 13 §5 rev 4) | `opus-reviewer`, fresh | ~84k, 41 tool uses | Ran fflate for the `undefined` round trip | Verdict: **approve with required changes.** Orchestrator verified in source: the plugin `sendChat` pushes its message before the 7b guard refuses, so a plugin-level guard is required, not redundant; `preLoadChat` only re-checks the pointer after the await, and a character switch mid-read can still lose the blob (pre-existing, deferred); `setItem(undefined)` would store 0 bytes read back as error forever (existing guard already covers it); a claimed-missing bridge file actually exists (rev 4's claim was wrong). Also: keep `getColdStorageItem` byte-identical (a restore's abort depends on it); account is never 'missing'; Tauri only distinguishes `os error 2`; the notice must not say delete unconditionally. Folded into rev 5. |
| 42 | 2026-09-21 | CHORE-07 7c-1 post-implementation gate | `opus-reviewer`, fresh | ~71k, 24 tool uses | Ran `pnpm test`/`pnpm check` | Verdict: **REJECT on tests/comments only**; production logic and wiring judged correct. MAJOR (Orchestrator-verified): the plugin `sendChat` guard test was vacuous — no `plugins` in the fixture, so a TypeError fired first, and the 'no push' check read the raw object instead of the Svelte proxy. Minor: false comments (guard location, caller count, byte count), a d.ts claim that upstream never rejects (its account branch can), an unused import, mislabelled RED tests, an OPFS not-found case treated as missing. Coder fixed all, with behavioural RED for the guard and the OPFS case. Orchestrator reran: 38 files, 480 passed, 4 skipped; check 0/0. |
| 43 | 2026-09-21 | CHORE-07 7c-1 live check | Orchestrator, in the browser pane against the maintainer's dev server (HMR picked up the edits) | n/a | Yes (live) | Passed. Seed fixture; a temporary chat pointing at a non-existent key was added, tested, then removed and verified. OPFS read classified as missing: the firm notice rendered with the key; the chat kept its pointer only; send was refused via the 7b dialog with the input text kept. Dialog wording was then generalised in `en.ts`. Note: stores must be imported with the page's `?t=` query when checking live (README Trap 1). |
| 44 | 2026-09-21 | CHORE-07 7c-2 investigation and plan gate (Report 13 §5.3 rev 6) | `investigator` then `opus-reviewer`, fresh | ~92k + ~98k | Read-only (git show for pre-7b and upstream) | Investigator found **F4**: `makeColdDataForChat` cold-stores an error-text chat again, orphaning the original key, so cleanup then deletes it — Orchestrator-verified, folded in. Gate verdict: **approve with required changes.** HIGH: the 'keep live side fields' rule would push restored messages before the hypa summarizer's memo-based start index, so they'd never be summarized again — replaced with an id-based merge (v3 concat + strip error memo, v2 prefer blob, lore concat, scriptstate live-wins). MEDIUM: the 'missing' notice would have advised actions that lose post-error messages; backups omit error-key blobs (recorded as a limit). Low: detached-proxy check, tail-after-await, notice placement. Folded into rev 7. |
| 45 | 2026-09-22 | CHORE-07 7c-2 post-implementation gate | `opus-reviewer`, fresh | ~89k, 30 tool uses | Ran `pnpm test`/`pnpm check`; import-graph DFS | Verdict: **approve with findings**; code matches rev 7. Verified the Svelte proxy identity check, the import back-edge, and the string-array save shape. Findings, all fixed with behavioural RED: comment overstated the hypa merge (limit now recorded); a malformed blob could restore `message` before the merge threw (merge now runs first, returns 'error', with a UI try/catch); a vacuous raw-object assertion (now reads through `DBState.db`); the orphan-memory exception documented; a summary left with no memos is now dropped. Orchestrator re-ran: 38 files, 507 passed, 4 skipped; check 0/0. |
| 46 | 2026-09-22 | CHORE-07 7c-2 live check | Orchestrator, in the browser pane against the maintainer's dev server | n/a | Yes (live) | Seed fixture. Wrote an OPFS blob (3 messages, a hypaV3 summary, lore, scriptstate) and two temporary chats holding the pre-7b error text. **ok case:** notice rendered above the input; Retry restored the messages plus the post-error message, merged summaries with the error memo stripped, merged lore and scriptstate; notice disappeared. **missing case:** Retry showed the new missing notice, left the message untouched, created no file. Cleanup verified: 2 chats, no leftovers. |
| 47 | 2026-09-22 | AV-3 investigation (plain-HTTP encode) | `investigator`, fresh | ~129k, 63 tool uses | Read-only | Refined Report 12 §4: a second, **unbounded** cache (`fileSrcCache`) pins about 4/3·bytes per chat asset on plain HTTP for the tab's life; `fileCache` is shared with the service-worker branch and carries no bytes there. 39 call sites, one persisted field (`oder.img`). Clipboard allow-lists exclude `blob:`; no CSP is active; DOMPurify already keeps `blob:`. No tests of `fileCache` existed. Orchestrator verified key citations. Maintainer chose option (a), a byte-budget string cache folding in `fileSrcCache`, set to 64 MiB. Plan: Report 15 rev 1. |
| 48 | 2026-09-22 | AV-3 plan gate (Report 15 rev 1) | `opus-reviewer`, fresh | ~85k, 29 tool uses | Read-only; `node -e` spy probe on `Buffer.prototype.toString` | Verdict: **approve with required changes**; option (a), the fold-in and 64 MiB judged safe. MEDIUM: (M1) 'fewer reads, less memory' was false — HEAD's 200-entry cap can hold more large avatars than 64 MiB; (M2) an oversized asset plus the parser bypass would re-encode on every streaming update, fixed with a one-slot memo; (M3) byte tests couldn't catch a drifting counter, so the seam must expose a recomputed total, plus an orphaned-retry test. Low: spy placement, call-site count corrected to 41. Folded into rev 2. |
| 49 | 2026-09-22 | AV-3 red-before-green | `test-warrior`, then Orchestrator rerun | ~190k, 100 tool uses | Yes (vitest) | Test-only seam, no behaviour change; T1-T13 across two suites. On HEAD plus the seam, 7 failed behaviourally with the plan's predicted mismatches (encode/read/entry counts); the 6 guards passed. Orchestrator confirmed. **Implementation note:** the Orchestrator rejected the coder's first draft, which stored only the base64 payload and prepended the prefix per call (each holder would get its own copy); `src` now holds the full string instead. |
| 50 | 2026-09-22 | AV-3 post-implementation gate | `opus-reviewer`, fresh | ~61k, 12 tool uses | Ran `pnpm test`/`pnpm check` | Verdict: **REJECT on tests/comments only**; production code correct — output identical to the pre-change baseline on every path, eviction terminates, an oversized orphan can't delete a retry's entry. Findings: a test's `maxBytes: 1` made its own 'commits normally' comment false; another test never actually called `getFileSrc`; stale citations and present-tense process history in test comments; overstated production comments. No test can observe string identity, so full-string storage rests on review (recorded as a limit in Report 15 §2.1). Fixed by coder/test-warrior; mutation probes confirmed the guards bite. Orchestrator reran: 40 files, 520 passed, 4 skipped; check 0/0. |
| 51 | 2026-09-22 | AV-3 live check | Orchestrator, in the browser pane against the maintainer's dev server | n/a | Yes (live) | localhost normally uses the service worker, so the seed page was switched to the plain-HTTP branch for the check (restored by reload). Wrote three temporary PNGs to real storage. First call 4.7 ms; repeat 0 ms (memo hit); 5 concurrent calls shared one read; recomputed byte total matched the running total. At a ~1.5-entry budget, re-requesting the first key forced a re-read; at an oversized budget, a repeat call served from the memo. Cleanup verified: keys removed and read back as null, cache reset. Heap size on a Pi 3 is not measured. |
| 52 | 2026-09-22 | AV-4 facts: list render sites, resolution path, raw-bytes readers, canvas taint, storage, keys, animation, cleanup | `investigator` (plus one follow-up) | ~156k, 88 tool uses | No | Eight list sites, all fixed at 56 px; no flag enlarges them. `readImage` returns raw bytes on every branch. Tauri's asset protocol is a separate origin, so a thumbnail generator must draw from a `blob:` URL of raw bytes. `inlayStorage` is the precedent: a separate localforage instance, invisible to backup/sync/sweeps, no eviction. `saveAsset` content-hashes paths **except** multiuser `receive-asset`, which trusts a peer-supplied id (a stated integrity-comment claim is false). No animation detection exists. A later-resolving `getCharImage` promise needs no extra wiring. Recorded in Report 16 §1. |
| 53 | 2026-09-22 | AV-4 measure-first (Report 12 §5) | Orchestrator, in the browser pane (seed tab) | n/a | n/a | 40 real hub avatars (25 JPEG, 15 PNG; 512 px to 3328x4864). Full decode 258 ms and **197 MB of bitmaps** total, against 39 ms and ~4 MB as 160 px thumbnails. The maintainer's baseline size (832x1216 NovelAI PNGs) decoded in 6.8-10.5 ms at 4.0 MB of bitmap each; WebP thumbnails were 9-10 KB. `toDataURL('image/webp')` works in Chromium; WebKit unverified. Best-case hardware (i9); bitmap sizes don't depend on hardware. **Conclusion: AV-4 is justified mainly by memory, not decode time.** |
| 54 | 2026-09-22 | AV-4 plan gate (Report 16 rev 1) | `opus-reviewer`, fresh | ~108k, 49 tool uses | No | **Approve with required changes.** R1: concurrency-2 with no timeout let two stuck reads blank every list icon, and FIFO made on-screen icons wait behind scrolled-past rows; fixed with a 15s timeout and LIFO. R2: the asset sweep was independently confirmed unaffected (account/cold-storage-setting gates verified). R3: a missing read result would have been memoized as a permanent skip. R4/R5: type-comparison and failure-memoization issues, fixed. R7: a citation about multiuser's write path was wrong (verified: it writes `assets/assets/...png`). Folded into rev 2. |
| 55 | 2026-09-22 | AV-4 implementation | `sonnet-coder` | ~174k, 110 tool uses | No | Implemented Report 16 rev 2: new `avatarThumb.ts`, the `getCharImage` type mapping, 8 list-site changes, a detached sweep call. `pnpm check` 0/0. **Its test report was wrong:** it attributed all failures to two known suites, but the Orchestrator's own run showed 7 parser suites also failing to load, because their `globalApi` mocks lacked `readImage`. |
| 56 | 2026-09-22 | AV-4 tests, red proof, mutation probes | `test-warrior` | ~307k, 150 tool uses | No | Added `readImage` stubs to 7 parser mocks; new tests across two files. Red on HEAD's `characters.ts` and list sites, with guards passing there as expected. Mutation probes caught: FIFO-not-LIFO, no-timeout, memoized-failure, no-type-wrapping — each swap restored byte-identical. Result: 42 files, 579 passed, 4 skipped; check 0/0. |
| 57 | 2026-09-22 | AV-4 post-implementation gate | `opus-reviewer`, fresh | ~103k, 20 tool uses | Ran `pnpm test`/`pnpm check` | **REJECT** (tests and one runtime gap; no data-loss/save-format/build defect). A test mocked the real eligibility function with a copy of its own logic instead of testing the real one; a title claimed 8 sites covered, actually 3; a timeout during `readImage` didn't cancel the in-flight read, so a late read decoded outside the concurrency limit (Orchestrator-verified). Plus present-tense process wording and an overstated comment. Queue, never-rejects, cleanup, generator order, byte parsing, sweep isolation and `getCharImage` identity were all confirmed correct. |
| 58 | 2026-09-22 | AV-4 re-gate after the row-57 fixes | `opus-reviewer`, fresh | ~87k, 16 tool uses | Ran vitest (excluding `.claude/**`) and `pnpm check`; results reproduced (42 files, 595 passed, 4 skipped; 0/0) | **REJECT, comments and test strength only**; runtime code judged sound (findings 1-7 resolved: no dangling timeout, no await between cancellation check and cleanup, cancelled tasks never memoize). New findings: a test comment falsely called two tests 'red' when they're guards; the lazy suite miscounted remaining sites; a spy wasn't cleared before a test that therefore couldn't fail; stale mock headers and a wrong citation; an unreachable branch should be documented as defensive. All sent for fixing. |
| 59 | 2026-09-22 | AV-4 row-58 fixes and Orchestrator check | `test-warrior` (tests) and `sonnet-coder` (one comment); checked by the Orchestrator | ~89k combined | No | Test comments corrected (which tests are red vs guards; site count); the first-lookups grid test now clears its spy first, and reverting the underlying fix alone fails it (restored, `cmp`); mock headers and citation fixed. Orchestrator grepped for leftover process wording (found only one already-committed region name) and re-ran: 42 files, 595 passed, 4 skipped; check 0/0. **A third full gate was not run:** the row-58 reviewer judged checking the edited lines sufficient, since runtime code changed only by one comment. |
| 60 | 2026-09-22 | AV-4 live check | Orchestrator, in the browser pane (seed tab, maintainer's dev server, service-worker branch) | n/a | Yes (live) | Seed fixture had no avatars, so temporary files were written under `assets/av4live-*`. **Formats:** a PNG and an EXIF-rotated JPEG produced correct WebP thumbnails; a small PNG, a GIF, a crafted APNG and an animated-WebP header all fell back to the full-size path as skip records. `'thumbcss'` wraps exactly like `'css'`; the readback guard passed in Chromium. **Store:** a cache hit took 0.3 ms with no `readImage`/`createObjectURL`; a forced PNG fallback worked. **40 real hub avatars:** first pass 668 ms (i9, concurrency 2, all WebP, ~11.8 KB average); a cached pass took 2 ms. **Sweep:** the real sweep on the live DB correctly emptied the store to just the keep-set. Not measured: heap delta (not visible to `performance.memory`). Cleanup verified: all test keys/cache entries removed, store empty. |
| 61 | 2026-09-22 | CHORE-01 + item 2: effect mechanics, the non-selected writer census, encoder side, trash harness, existing tests | `investigator`, two rounds | ~243k combined | No | Mechanism confirmed end to end (character-tracker gate; stale-block reuse in the encoder). Trash harness runs (1 passed, asserting the bug) — the red-test seed once inverted. Census closed; unmitigated writers: trash restore, V3 `setCharacterToIndex`, V3 `setChatToIndex` (new), plugin `setDatabase`/`setDatabaseLite` (new; V2 and V3 paths), risuaccess character/chat tools (new). Cleared: group members, Lua/CBS, plugin `setChar`, multiuser `receive-char`, import/create, `removeChar` (self-mitigated). Detail: Report 17 §1.2. Census commands (kept verbatim; cited by Report 17 §1.2): `grep -rn "characters\[.*\]\s*=\|characterTalks\|characterActive" src/ts/process/index.svelte.ts`; `grep -n "getCharacter(\|DBState.db.characters\[" src/ts/process/mcp/risuaccess/characters.ts src/ts/process/mcp/risuaccess/chats.ts`; `grep -rn "requiresFullEncoderReload\|selectedCharID.set" src/ts/process/mcp/risuaccess/`; `grep -n "declareAPI(" src/ts/process/scriptings.ts`; `grep -n "^\s*set[A-Z][A-Za-z]*\s*:" src/ts/plugins/apiV3/v3.svelte.ts`; `grep -n "export.*function removeChar" src/ts/characters.ts`; `grep -n "characters\[\|DBState.db.characters\|requiresFullEncoderReload\|setDatabase" src/ts/sync/multiuser.ts`. Report 11's 67/24 count could not be reproduced (its command was not recorded) — record your commands. |
| 62 | 2026-09-22 | CHORE-01 + item 2: measure today's effect and the design options (M1-M4), then re-baseline against the real app's boot proxy walk | `perf-analyzer`, two rounds | ~468k combined | No | M1: 74-385 ms per change at 10k-50k messages (98.8-100% the chats snapshot; ~60 MB transient per flush). M2 (selection partition): field writes ~0.002 ms; tokens 0.02-0.6 ms only with per-message children. M3 (watch every character): first reported +550 MB, **corrected to ~+170 MB** over the real app, because boot's `encoder.init` already materialises every proxy — the first baseline was wrong and the Orchestrator's premise check caught it. M4 (identity only): ~free, misses in-place writes. Node, i9, pointer compression off (detail: Report 17 §1.3/§2). Maintainer chose option B. |
| 63 | 2026-09-22 | Gate 1 on Report 17 rev 1 (CHORE-01 + item 2, option B) | `opus-reviewer`, fresh | ~121k, 51 tool uses | No | **REJECT.** Option B sound; three blocking factual errors, each hiding a loss path (detail: Report 17, folded into rev 2): (1) census missed generation continuing after a selection change without a `doingChat` guard (hotkeys, Playground, Home, `receive-char`); (2) V2 `getDatabase` is a live wrapper, so V2 `setDatabase` edits in place and an identity tracker sees nothing; (3) marks before the hook would be dropped during the un-awaited, seconds-long `encoder.init`. Also: backup loads lose non-selected characters today; multiuser `receive-chat` would re-encode everything per message; the equivalence assertion could not fail. Orchestrator re-verified 1-3 in source. Census additions for re-runs (kept verbatim; cited by Report 17 §1.2): `grep -n "selectedCharID.set" -r src/ts src/lib` then check each caller for a `doingChat` guard; `grep -rn "setDatabase(" src/ts` for whole-db replacements. |
| 64 | 2026-09-22 | CHORE-01 Stage 1 gate 2, pass 1 | `opus-reviewer`, fresh | not given | No | **REJECT.** Blocking: **B1** the save preparation reloaded before snapshotting, so the post-reload filter dropped in-place edits made during the reload (proven with a probe: a decode showed the original text). **B2** plugin setters marked every character, so a stale second plugin `setDatabase` could make the encoder delete a character's block permanently (proven through the real V2 API). Should-fix: a strong reference kept boot proxies alive; a mocked test artifact; untested backup-load wiring; stale citations. 5 commit-message corrections. Orchestrator re-verified B1 and B2 in source. |
| 65 | 2026-09-22 | CHORE-01 Stage 1: investigate the B2 fix premise ("every intentional removal sets the reload flag") | `investigator` | not given | No | Premise as worded **REFUTED**, in favour of the fix: the only removals without a reload are the plugin setters and a V3 re-keying call, and those must not delete. Their deletion happened only when the removed id was the tracker's front entry. Intentional removals (character deletion, backup loads) do set the reload flag. The boot trash purge runs before the first save. Cold storage keeps the character id. Orchestrator spot-checked the removal function and the boot ordering. |
| 66 | 2026-09-22 | CHORE-01 Stage 1: first B2 fix attempt | `sonnet-coder`; rejected by the Orchestrator | not given | No | The first B2 fix re-appended plugin-removed characters to the database. **Rejected**: it changes plugin-visible behaviour and covers only the setters. Tests were restructured first (a plugin-level removal test, a save-level no-reload presence filter), then the approach was reverted. The planned WeakSet fix from pass 1 was infeasible (the seed must stay iterable) and was replaced by a dedicated proxy-extraction helper. |
| 67 | 2026-09-22 | CHORE-01 Stage 1 gate 2, pass 2 | `opus-reviewer`, fresh | not given | No | **REJECT.** (1) The dirty flag was cleared before the reload, so a throwing reload looked clean, and multi-tab could auto-reload and discard edits without a prompt; fixed with a restore-confirmed flag plus an unconditional set in the outer catch. (2) The reload's seed object was retained by effect closures, pinning boot-time characters (confirmed with a WeakRef probe); fixed by clearing it. (3) misleading test/comment text. Also replaced O(N²) proxy scans (70/279 ms at 1000/2000 characters) with a per-call Map/Set, and moved the reload-flag clear before the await. Red tests written before each fix. |
| 68 | 2026-09-22 | CHORE-01 Stage 1 gate 2, pass 3 | `opus-reviewer`, fresh | not given | No | **REJECT, on text only.** No data-loss, crash or build defect found. Four false comments fixed as comment-only edits (Orchestrator verified via diff that only comments changed). Commit-message corrections applied. Non-blocking finding recorded for CHORE-03: a character removal during an in-flight save can skip a character for one write, healing on the next save. Accepted, disclosed behaviour: a V3 re-key keeps the old block, so both versions appear after a reload. |
| 69 | 2026-09-22 | CHORE-01 Stage 1 live check | Orchestrator, in the browser pane (dev server started by the maintainer, 31-character module-heavy fixture) | n/a | Yes (live) | All passed after a page reload: trash restore of a non-selected character; V3 `setCharacterToIndex`/`setChatToIndex`; a V3-style `setDatabase` with a fresh array; a V2-style in-place edit plus `setDatabaseLite` — each on a different non-selected character; generation with the selection switched mid-stream (reply persisted in the original character). Modules driven through the app's own instances. A save after a plugin `setDatabase` on 31 characters completed in ~0.8s including the debounce. **Large-fixture bench (perf-analyzer, Node, i9, best case; detail: Report 17 §6):** one plugin `setDatabase` save re-encodes every character — 165 ms median at 500 chars/~20k msgs, ~1.18s at 1000/~150k msgs (vs 14/49 ms for a normal one-id save); cost tracks message volume, not the O(N²) explanation perf-analyzer first proposed (rejected by the Orchestrator). Test totals: 663 passed, 4 skipped; check 0/0. |
| 70 | 2026-09-22 | CHORE-17: the two community plugins' DB API usage (AssetGod, Fast Character Import) | `investigator` | not given | No | Counting method and full findings duplicated in Roadmap CHORE-17 (real plugin evidence). Summary: AssetGod calls `setDatabase` 7 times (4 pass the whole array for one changed character), never the narrow APIs; Fast Character Import uses the cheap append API and falls back to a full-array setter only when empty. Neither plugin calls a setter per chat turn, on a timer, or at load — a quality item, not per-turn stabilisation. |
| 71 | 2026-09-22 | CHORE-17: recommend a re-encode-only-what-changed strategy for the shared plugin setters | `senior-advisor` | not given | No | Endorsed the maintainer's goal, relocated to the setter layer, not the encoder (full strategy: Roadmap CHORE-17). Two layers: a boundary reconcile at the `characters` key (identity/deep-compare rules), and an encoder exact-bytes skip (length+CRC32+byte-compare before writing). Ruled out: hash-only skip, snapshot-diff, API-version branching, 'reload instead of mark' (a stale plugin snapshot would become a permanent-deletion path). **Corrected the Orchestrator's own premise:** remote saving is opt-in, not default, on Node/Tauri. Orchestrator verified four dossier corrections in source. |
| 72 | 2026-09-22 | CHORE-01 Stage 2: equivalence suite, implementation, rebuild-count bench; the for...in entanglement | `test-warrior` (equivalence suite written first, green on the old effect, each chats-dependent class shown failing under a narrowed variant; rebuild-count tests via the optional `onPartitionRun` hook; benches), `sonnet-coder` (partition) | not given | No | **The first partition used `for…in` over the character; in Svelte 5.55.1 that subscribes the running effect to every existing property**, so a chatPage switch rebuilt the whole subtree (worse than the old effect) and every later field write repeated it (numbers: Roadmap item 2). Orchestrator verified the mechanism in Svelte's proxy source. test-warrior's suggested `untrack` fix was rejected as wrong; red tests were written first, and the real fix was `Reflect.ownKeys` plus excluding `chatPage` from the field children. |
| 73 | 2026-09-22 | CHORE-01 Stage 2 gate 3, round 1 | `opus-reviewer`, fresh | not given | No | **REJECT.** (1) A false comment about cold-storage stub shape, originating in the Orchestrator's own plan and brief; fixed in source, tests and plan. (2) A chaId captured at child-creation time could re-front a stale id after an in-place rename and a save-loop trim; reviewer reproduced it; red test written first, fixed by reading the chaId untracked at fronting time. (3) Stale citations. |
| 74 | 2026-09-22 | CHORE-01 Stage 2 gate 3, round 2 | `opus-reviewer`, fresh | not given | No | **REJECT.** Leftover false comments; many stale in-repo line citations; a new throw introduced when `db.characters` is undefined (the old optional chaining had been dropped; restored). Root cause: the Orchestrator's briefs asked for line numbers in comments. Remedy: all citations converted to names, and a standing rule added (commit `04a48028`: no in-repo line numbers in code or test comments). |
| 75 | 2026-09-22 | CHORE-01 Stage 2 gate 3, round 3 | `opus-reviewer`, fresh | not given | No | **APPROVE-WITH-FINDINGS.** Low findings only, all comment/doc text (wording nits). Applied; tests 705 passed, 4 skipped; check 0/0. Note: a Gate 3 reviewer had been invited by the brief to write a harness while its profile forbade repo writes; the conflict led to the standing rule that reviewers put throwaway files in their scratchpad only (commit `675aa470`). |
| 76 | 2026-09-22 | CHORE-01 Stage 2 commit-message fact-check | `opus-reviewer`, fresh | not given | No | **REJECT, on text only.** (1) The draft claimed a top-level-only edit no longer re-encodes the selected character; false — the selected character is the tracker's sticky front, so it's still re-encoded (Orchestrator-verified against the save loop). (2) Two stale test names, renamed. (3) The heap figure overstated the new effects' cost: the reviewer's own run of the same harness on the old source showed the new effects add about 9/43 MB, not the full 23.7/119 MB total (numbers: Roadmap item 2). Fixed in the message and the plan. Lesson: verify the Orchestrator's own claims before a commit message, same as reviewer claims. |
| 77 | 2026-09-22 | CHORE-01 Stage 2 live check | Orchestrator, in the browser pane (dev server started by the maintainer; a temporary 10k-message character "Livecheck 10k (temp)" added with the maintainer's approval, then deleted with `removeChar(..., 'permanentForce')`; verified after a reload: 31 characters, max 8 messages) | n/a | Yes (live) | Timed with `flushSync` because the pane throttles rAF; modules imported via exact resource URLs to hit the app's own instances (numbers: Roadmap item 2). A temporary 10k-message character was added with the maintainer's approval and permanently deleted, verified after reload. Confirmed on live data: chatPage switch is dominated by the chat view rendering 1000 messages, not the tracker; the old effect's `snapshot(chats)` on the same data took 204 ms in the same page. Recorded option for Pi/phones: chunked or active-chat-only message children, since remaining growth is Svelte's flush walking every live effect. |
| 78 | 2026-09-22 | CHORE-17: verify the advisor's premises (shared setters, V2/V3 payloads, proxy identity, identity tracker, encoder internals) | `investigator` | ~89k, 39 tool uses | No | All premises confirmed (shared setters, V2/V3 payload shapes, proxy identity, identity tracker, encoder internals). One caveat: with remote saving on, a post-encode byte compare can't skip the stringify/hash/remote-write, which run first. Orchestrator checked the remote-encode path in source. |
| 79 | 2026-09-22 | CHORE-17 (A): phase breakdown of an all-N `set()` in real Chromium with real IndexedDB | `perf-analyzer` | ~186k, 67 tool uses | No | 1000 chars/~150k msgs, headless Chrome, i9 (best case): an all-N `set()` totals 1155 ms = stringify-through-proxy 351 ms (30%) + encode 372 ms (32%) + IndexedDB 418 ms (36%). Raw stringify without the proxy is 69 ms (~5.1x proxy cost). The encoder retains ~109 MB of blocks. A layer-1-style compare would cost 267 ms, synchronously. The earlier Node '~1.18s' figure matched only by coincidence. |
| 80 | 2026-09-22 | CHORE-17: synchronous setter cost, blocking in today's save, chunked compare | `perf-analyzer` (same agent) | ~426k, 110 tool uses | No | A V3-style `setDatabase` costs 119 ms synchronous in the host setter's per-chat loop; V2 costs 1.6 ms. Today's save yields per block, with a longest slice of 44 ms (10 blocks over 16 ms, the large characters). Adding layer 1's compare to the setter would push the call to ~386 ms and is unchunkable — this is why the maintainer put layer 1 on hold and chose layer 2 only. |
| 81 | 2026-09-22 | CHORE-17: safety facts for the exact-bytes skip | `investigator` | ~121k, 25 tool uses | No | **REFUTED** the advisor's claim that the skip helps boot, reloads and backup loads: each constructs a fresh encoder with empty blocks, so the cache is a corruption fallback only, not a boot/reload optimization. Found the latent `checkedRemoteExistence` recorded-before-write bug (fixed on the way, per Roadmap CHORE-17). Multi-tab cache staleness named as a trade-off. Orchestrator verified the fresh-encoder sites and the cache-read site. |
| 82 | 2026-09-22 | CHORE-17 Gate 1 on Report 18 rev 1 | `opus-reviewer`, fresh | ~120k, 34 tool uses | No | **APPROVE-WITH-FINDINGS.** M1: skipping `setItem` removes the save loop's only macrotask yields — fixed with a yield budget. M2: a `chatPage` switch changes the serialized character, so it can't be skipped (the Orchestrator's own error, re-verified). M3: a module-level set fires after reloads, overriding an upstream behaviour. Minors: account-storage resolve-without-persist edge case; one set not two; overstated wording. Folded into rev 2. |
| 83 | 2026-09-22 | CHORE-17 Gate 2, round 1 | `opus-reviewer`, fresh | ~109k, 30 tool uses | No | **REJECT, on text only.** Code correct; the five red tests fail on the pre-change source for the right reason. Stale 'today'/'yet' test wording, a false upstream-behaviour description, a false 'nothing is skipped on account storage' claim, a wrong hash-algorithm name, commit-message corrections. |
| 84 | 2026-09-22 | CHORE-17 Gate 2, round 2 | `opus-reviewer`, fresh | ~104k, 37 tool uses | No | **REJECT.** One surviving false account-storage claim in the tests, and a gap: a length-only comparator passed every test. Equal-length guard tests added and shown to fail against that mutant. |
| 85 | 2026-09-22 | CHORE-17 Gate 2, round 3 | `opus-reviewer`, fresh | ~98k, 49 tool uses | No | **APPROVE-WITH-FINDINGS.** 14 mutants tried in the reviewer's scratchpad. MAJOR: deleting the comparator's word-loop still passed, because both test guards happened to have lengths that were multiples of 4; guards at a non-multiple-of-4 length were added. The account branch must never read the skip set (the backend can switch to account storage mid-page) — test and comment added. Final: 732 passed, 4 skipped; check 0/0. |
| 86 | 2026-09-22 | CHORE-17: re-measure with the skip applied | `perf-analyzer` (same agent), two rounds | ~1.18M combined | No | Result numbers: Roadmap CHORE-17. **The first round's 127 ms slice figure was a method artifact** (yields inferred from resume latency); the Orchestrator rejected it as inconsistent with the code, and the second round counted yields directly. The headless `longtask` observer didn't report a deliberate 100 ms loop, so it's unusable here. Browser gzip is deterministic. |
| 87 | 2026-09-22 | CHORE-17 live check | Orchestrator, in the browser pane (dev server started by the maintainer, 31-character fixture) | n/a | Yes (live) | Confirmed live (detail duplicated in Roadmap CHORE-17 status): a V2-API `setDatabase` changing one character wrote exactly one block, where every character was written before; a preset rename didn't rewrite the selected character; a `chatPage` switch rewrote only the selected character's block. Verified with a wrapper on the IndexedDB write call. All test edits reverted through the app; after a reload, all 31 characters loaded with no leftover test marker. |

| 88 | 2026-09-22 | Chat-list slowdowns: measure the four symptoms the maintainer reports (opening, scroll-back, streaming, typing) | Orchestrator, in the browser pane (dev server started by the maintainer; a temporary character with one 3000-message chat, deleted afterwards) | n/a | Yes (live) | **The measurement that made the plan possible** (numbers: Report 19 §1). Established that the mounted window only ever grows and is not keyed by chat, so one deep scroll inflates every chat opened later in the session: reopening the 3000-message chat cost 330-622 ms at a fresh window of 30 and **4.9 s** at 600 mounted, a streamed chunk 6-11 ms vs 124 ms, a keystroke 3 ms vs 37 ms. Selection scales with total chat length at about 0.12-0.15 ms per message. Recorded honestly that `$state.snapshot` of 3000 messages is 44 ms and **what the rest of the selection cost is was not measured**, which later stopped Stage D from being planned on an assumption. Best-case hardware (i9-13900K, dev build); no Pi or phone claim. The temporary character's first deletion silently failed here and was recorded as unexplained; row 101 found the cause. |

| 89 | 2026-09-22 | Chat list windowing: mount/unmount mechanics, per-message render work, the scroll model, virtualization hazards, existing tests | `investigator` (Sonnet 5), 1 `code-searcher` survey | ~174k, 53 tool uses | No | **Wide and mostly accurate; one inconsistency reached the plan.** Confirmed the imperative hash-keyed mount loop and that `loadPages` is decreased in exactly one place in the whole codebase — then found that that place, the screenshot reset, is the last statement in its `try`, so a failed screenshot leaves every message mounted for the rest of the session (previously unflagged). Widened the premise correctly: because `DefaultChatScreen` is never keyed by character or chat, a single deep scroll anywhere inflates every later chat. New hazards: the **V2** plugin API exposes the same live chat DOM as V3 (the Roadmap named only V3); a plugin's `MutationObserver` dies silently when a message is remounted; six keyboard shortcuts act on the *first DOM match* rather than an index; a `:first-of-type` CSS rule depends on newest-first DOM order. Refuted two suspected hazards (TTS takes a string; group `saying` is never read by the chat screens). Corrected its own dispatched `code-searcher` unprompted (the copy button parses a detached document, not live DOM). **Defect:** it claimed streaming writes coalesce to at most one per animation frame while its own next paragraph described a per-chunk remount under the default `'off'` — the two cannot both hold. The claim reached the plan and was caught by Gate 1. Lesson: check a packet against *itself*, not only against source. |

| 90 | 2026-09-22 | Attribute the measured keystroke and character-selection costs to mechanisms | `investigator` (Sonnet 5) | ~148k, 44 tool uses | No | **Model mid-tier packet; its most valuable act was refusing to supply a number.** Opened by refuting its own brief's premise: the CHORE-01 Stage 2 harness does not measure mount cost (only per-edit cost and retained heap), so "treat any per-message mount time as unmeasured; do not cite one." For typing, eliminated every reactive path by tracing rather than grepping — `messageInput` is component-local and read by nothing, `updateChatBody` does not track it, `localDrafts` is a plain `Set` — leaving DOM layout, and named `updateInputSize()`'s write-then-read inside the same uncontained flex column that holds every mounted message, **explicitly labelling the browser-internals half inference** and supplying two cheap decisive tests. For selection, confirmed the CHORE-01 tracker rebuilds one effect per message of *every* chat of the character, matching the observed per-message scaling, and ruled out `updateChatBody`, `preLoadChat`, `chatFoldedState`, `BookmarkList` and any whole-array `$derived`. Bounded its own conclusion: the 44 ms snapshot is only ~10-12% of the 358-445 ms selection and the remainder could not be split from source. Two honest limits left standing (whether the fixture chat had been cold-stored; one grep-only absence). |

| 91 | 2026-09-22 | CHORE-18: long creator notes push the character-list row buttons out of reach | `sonnet-coder`, then an Orchestrator live check in the pane | not given | No | Folded in alongside the chat-list work rather than ticketed. The list and trash rows printed the whole note in a non-wrapping span inside a text column with no `min-w-0`, so a long unbroken string ran past the row and pushed the select, restore and delete buttons off it. Fixed with `min-w-0`, `wrap-break-word` and a three-line clamp; each row now calls `parseMultilangString` once instead of twice. Live check: the note's text measured 3,616 px in a 715 px row before the fix; after it the text stays in the row, clamped, and the delete button is reachable. Committed `2ee8a2a9`. |

| 92 | 2026-09-22 | Gate 1 on Report 19 rev 1 (chat message window) | `opus-reviewer`, fresh | ~140k, 47 tool uses | n/a (gate) | **[REJECT]**, 5 MAJOR plus minors (detail: Report 19 §7). Verified the load-bearing mechanism claims rather than taking them, including the single `loadPages` reduction and the screenshot `catch`. Best finding: the planned edit guard covered `editMode` only — it missed `editTranslationMode` and `PartialEditController`, "commit on unmount" is unsafe because the long-press exit deliberately discards, and `hasLocalDrafts()` is too coarse to gate on. Also **refuted a premise carried in from row 89** (streaming is not frame-coalesced under the default `'off'`, and the cost split was unmeasured), and refused an unmeasured premise for Stage D, turning it into a profile-first step. |

| 93 | 2026-09-22 | Gate 1 re-review, rev 2 | `opus-reviewer`, fresh | ~87k, 35 tool uses | n/a (gate) | **[REJECT]**, resolving four of the five MAJORs but finding a BLOCKER the plan's own framing had hidden: "a switch already unmounts every message" was false. Copy and branch keep the message hashes, so instances with open editors survive a switch *today*, and the planned change would have started destroying them — an edit-loss path introduced by a plan whose purpose was to avoid one. Also: a per-index editor registry collides when two editors are open on one message, and restoring the window to the initial count after a *failed* screenshot converts that failure into edit loss. |

| 94 | 2026-09-22 | Gate 1 re-review, rev 3 | `opus-reviewer`, fresh | ~91k, 24 tool uses | n/a (gate) | **[REJECT]**. New BLOCKER: the greeting `Chat` (index -1) is mounted by `DefaultChatScreen` *outside* `Chats` and its translation editor is reachable, so "the unmount loop is the only unmount site" was false — `Chats`' own `onDestroy` unmounts too, and the new design would have newly destroyed the greeting's editor. Also: a recorded index past the end of the current messages would throw, and a keep-outside-the-window check needs the mount loop's *exact* hash or it orphans and duplicates instances. |

| 95 | 2026-09-22 | Gate 1 re-review, rev 4 | `opus-reviewer`, fresh | ~81k, 21 tool uses | n/a (gate) | **[REJECT]**. The design had become a clamp on `loadPages`; the gate found the clamp left **zero slack**, so the very next appended message slid the window past the open editor and silently lost it — the exact failure the clamp existed to prevent. Second: on a switch the clamp mixed indices from the chat being left with the new chat's length, so it could mount almost an entire new chat. Third: "an open editor is never outside the window" was still false — appends, folds, "load more", hash changes and component unmounts all remove instances. |

| 96 | 2026-09-22 | Gate 1 re-review, rev 5 | `opus-reviewer`, fresh | ~79k, 19 tool uses | n/a (gate) | **[REJECT]** — the fifth in a row, each with new substantive findings. The range-extension design was *stricter than today* for the greeting: switching to another chat of the same character would destroy a greeting translation edit that currently survives. Two MAJORs on the same design: when the extension lifted (on save) hundreds of visible messages unmounted and the view jumped, and scroll-up stalled until `loadPages` climbed past the extension. **This row is where the Orchestrator stopped revising and escalated** (row 97). |

| 97 | 2026-09-22 | Escalation: direction for the chat message window after five rejected designs | `senior-advisor` (Fable 5.1); the maintainer asked for it | ~113k, 42 tool uses | **Yes** | **An escalation that changed the approach rather than confirming it — and the precedent for the new AGENTS.md rule.** Named the single structural fact behind all five rejections: unsaved editor text lives in a component instance whose lifetime depends on a content hash, and that instance can be unmounted from at least six sites across four components, so **any guard placed in the window policy is incomplete by construction**. Recommended ordering the work A-lite (reset on switch, gated on the existing draft registry with a kind flag) → containment → durable drafts as the foundational fix → A2/B/D → E last, with seven explicit DO-NOTs including "no sixth variant of guarding the window policy". Contributed a new fact (`changeChatTo` fans `ReloadGUIPointer` over the old window on every switch), which the Orchestrator verified in source along with the dead `reloadKeys`. The maintainer approved the order and deferred the multi-tab trade-off to the durable-drafts plan. **AGENTS.md now requires this escalation after three substantive rejections in a row rather than five** (`3c3525ee`). |

| 98 | 2026-09-23 | Gate 1 on rev 6 (Stage A / A-lite) | `opus-reviewer`, fresh | ~117k, 37 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS]** — the first approval after five rejections, and it approved a *smaller* design. Verified that the draft gate covers every message and greeting editor (registration happens on the flag, outside the `{#key}`), that the only other `registerDraft` callers fail safe, that Svelte 5.55.1 runs a parent `$effect.pre` before user effects, and that the multi-tab gate is untouched. **Best finding: it corrected the escalation tier.** The advisor's new `changeChatTo` fan-out fact does not apply to *character* switches, so it is not part of the measured 682 ms row — verified by the Orchestrator in `changeChar`. A cheaper gate catching an over-scoped fact from the most expensive tier is the argument for gating escalation output too. Also required a specified scroll-after-reset: a no-op reset must not scroll, and `scrollIntoView` would hide a tall newest message and the composer. |

| 99 | 2026-09-23 | Stage A (A-lite) implementation: the window-policy seam, draft kinds and the suite | `test-warrior` (two dispatches, contract tests first), `sonnet-coder` (three dispatches) | ~228k / ~357k, 62 + 52 tool uses | No | Implemented as a pure seam (`chatWindowPolicy.ts`) so the reset, the screenshot save/restore and the last-seen key are testable without mounting a component, with `localDrafts` gaining a draft kind so `hasMessageEditorDrafts()` gates the reset while `hasLocalDrafts()` still feeds the multi-tab reload gate. 51 tests across the two files. Expensive for the size of the diff, mostly the two follow-up rounds fixing Gate 2's findings red-first. Committed `96311c4a`. |

| 100 | 2026-09-23 | Gate 2 on the Stage A implementation | `opus-reviewer`, fresh | ~89k, 27 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS]**, all three findings in one place: the screenshot restore. A double click saved the already-full window as its "before" value and so restored to everything mounted; a restore deferred by an open editor survived a chat change and then restored the *old* chat's window; and a deferred restore moved the scroll position. Fixed with a screenshot depth counter, a skipped reset replacing a pending restore with the initial value, and no reset-token bump on a deferred restore. Five tests written first failed on the old policy; all 14 mutants killed (run in the scratchpad); suite 775 passed / 4 skipped; `pnpm check` 0 errors, 0 warnings. Lesson: the reviewer found nothing wrong with the feature and everything wrong with its least-exercised path. |

| 101 | 2026-09-23 | Stage A live check | Orchestrator, in the browser pane (dev server started by the maintainer; a temporary character with two 2000-message chats plus a short chat whose newest message is very tall, removed afterwards through `removeChar`) | n/a | Yes (live) | **[PASS]** on all six scenarios (table: Report 19 §7): a switch with no editor open resets the window to 30 and lands at the bottom in 112 ms; a switch with an editor open changes nothing; composer text does not gate the reset; a tall newest message stays anchored at its end with the composer visible at 375x812. **Also closed row 88's unexplained failed deletion:** it was the harness, not the app — a bare `db.characters.splice` does not set `requiresFullEncoderReload`, so the saver keeps the removed character's block and the character returns on reload. Reproduced twice, then deleted through `removeChar(..., 'permanentForce')` and verified across a reload (31 characters). Harnesses must delete through `removeChar`. Two limits stated rather than papered over: the screenshot restore was not exercised live (it writes a file), and the console buffer had overflowed before the check, so "no console errors" could not be claimed. |

| 102 | 2026-09-23 | Containment experiment: does `content-visibility` pay for itself, and what is a chat-to-chat switch worth? | Orchestrator, in the browser pane (dev server started by the maintainer; a temporary character with two 3000-message chats, removed afterwards through `removeChar` and verified across a reload) | n/a | Yes (live) | **Measured, and the answer is no** (numbers: Report 19 §8). A/B/C/A with N=20 per cell: `content-visibility: auto` with a tuned `contain-intrinsic-size` takes a streamed write's layout from 12.0 ms to 10.1 ms — 12% of the step — does nothing for typing, and plain `contain: layout style` is *worse* than no rule. **The finding that decides it:** 98% of the 600 mounted messages were off-screen yet only ~16% of the column's layout time went away, because containment skips each message's internal layout while the flex column still lays out 600 boxes — direct evidence for the advisor's DO-NOT that containment is not a substitute for bounding N. Also found that the intrinsic-size estimate is load-bearing: a guessed 220 px collapsed the scroll height by 25.3%, while the measured 297 px average held it to 0.2%. Scroll-up anchoring and off-screen seek both passed unchanged; the screenshot and WebKit checks were not run and are stated as such. **Separately worth more than the rule:** a chat-to-chat switch at 600 mounted costs 95.6 ms and is almost all script, of which the `ReloadGUIPointer` bump in `changeChatTo` is ~31% — a cheap deferral. **Method limits stated rather than papered over:** the DevTools Performance panel is unreachable from the pane, so script and layout were split with `flushSync` plus a forced `scrollHeight` read and **paint was not separated**; and every row came out 3-10x below §1's at the same window and DOM size, most likely because §1 timed whole user actions and §8 timed isolated mechanisms — not settled, and the two must not be mixed in one comparison. |

| 103 | 2026-09-23 | Fact-check ledger rows 88-101 against the reports, the source and git | `doc-verifier` | ~119k, 49 tool uses | No | **Clean: 29 claims verified, 0 wrong, 0 overstated.** Re-derived the load-bearing ones rather than matching wording — it read the pre-Stage-A file out of git to confirm `loadPages` had exactly one decreasing assignment and that the screenshot reset was the last statement in its `try`, and it re-ran the suite and `pnpm check` to confirm the counts. One real catch: row 101 described the live-check fixture as two 2000-message chats and omitted the third, tall-message chat that the scenario it cites actually used; fixed. Four claims were correctly reported as unverifiable from the artifacts in scope (token counts and packet-only process details), rather than waved through. |

| 104 | 2026-09-23 | Is the `ReloadGUIPointer` bump in `changeChatTo` load-bearing on a chat switch? | `investigator` | ~113k, 47 tool uses | No | **Refuted the Orchestrator's framing, which is what made the fix correct.** The brief assumed a chat switch changes every message's `chatId` and so remounts the window anyway. `chatId` is a per-message UUID, not the chat's id — true for two independently created chats, but **branch, copy and reorder preserve it**, so those switches deliberately *reuse* the mounted instances and the bump is the only thing that refreshes them. Blanket removal would have been a data-visibility bug. Also found, by reading the refactor that introduced `changeChatTo`, that five call sites still fire their own `$ReloadGUIPointer += 1` one line later — a leftover, not a safety margin — and warned that a fix confined to the helper would not reach the primary click path. That warning is what later killed the first design. Honest limits: two UNCERTAINs left standing with the observation that would settle each. |

| 105 | 2026-09-23 | Gate 1 round 1 on Report 19 §9 rev 1 (defer the bump) | `adversarial-reviewer`, fresh | ~91k, 32 tool uses | n/a (gate) | **[REJECT]**, two design BLOCKERs, and it supplied the design that shipped. **B1:** deferring the bump is defeated by the caller's own synchronous bump one line later, so the expensive fan-out over the outgoing window would still happen and the deferred bump would merely add a second one — the plan would have delivered nothing on its main path. It also identified the specific error: the Orchestrator's "the second bump is free" measurement was real but did not transfer to a design that defers the *first* bump. **B2:** the staleness risk was understated — the script cache key carries no chat identity, so a freshly mounted instance can read a stale entry from the old chat at the same index, not just a reused one. **Its alternative** — `flushSync()` between the write and the bump — dissolved both at no extra size. |

| 106 | 2026-09-23 | Gate 1 round 2 on rev 2 (the `flushSync` design) | `adversarial-reviewer`, fresh | ~131k, 65 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS]**, earned by execution rather than argument: it built a Svelte tree mirroring the imperative mount loop and the `onMount` subscription and ran it against this repo's own Svelte 5.55.1 to prove a **freshly mounted** instance subscribes *inside* `flushSync()` and so still receives the corrective bump. That was the one thing that could have made the change worse than the status quo. **MAJOR:** rev 2's "no call site needs editing" was wrong — the two drag-reorder handlers call `changeChatTo` *before* assigning the reordered array, so the new flush would render the wrong chat; and a naive statement swap is also wrong, because the chat *object* must be captured, not the index. A hazard the change itself introduced, missed by both the Orchestrator and round 1. |

| 107 | 2026-09-23 | Implement §9 rev 3: the flush, the reorder seam, and the post-implementation measurement | `test-warrior` (three dispatches), `sonnet-coder` (three dispatches), Orchestrator live measurement | ~234k / ~147k, 72 + 47 tool uses | No | Red-first throughout. The ordering test runs a real `$effect` on `chatPage` and asserts it fired **before** the bump, which only a genuine synchronous flush between the two can satisfy — a spy on "was `flushSync` called" would have been satisfied by calling it in the wrong place. **The test author proved the hard part instead of asserting it:** an end-state assertion provably cannot catch the switch-then-assign bug, since both orders end identically, so it built a mid-flush `$effect` observer and verified by mutation that the observer is what kills it. Also surfaced a durable Svelte-5 testing fact: reassigning an array into `$state` re-proxies it, so elements and arrays must be compared by id, not reference. Measured live against the real build: a chat-list click at 600 mounted went **104.4 ms to 66.2 ms**, non-overlapping ranges. Two process notes worth keeping: an implementer hit a stale `@ts-expect-error` that broke `pnpm check` once the helper existed and **reported it rather than editing the test file it was told not to touch**; and one agent briefly created a scratch file inside the repo to resolve module aliases, then deleted it — confirmed gone. |

| 108 | 2026-09-23 | Gate 2 on the implementation | `opus-reviewer`, fresh | ~140k, 65 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS]** — **could not falsify the executable change.** Walked all 17 call sites looking for a dependent write after the call, checked re-entrancy, the throw path and streaming, and re-derived the mount-ordering claim from Svelte source. Every finding was in a comment, a test or report prose. **MAJOR (tests):** two of its six mutants **survived the full 784-test suite** — reverting the reorder handlers to switch-then-assign, and computing the target against the new array. The suite pinned the function and pinned the helper in isolation but pinned nothing about their wiring at the only two sites that needed it. **MAJOR (comment):** the shipped comment prescribed the *opposite* of the correct rule, telling callers to move dependent writes before the call; the reviewer traced the concrete failure (the branch fold silently never happens) and the Orchestrator verified it in source. Ran its mutants through an in-memory Vite transform so the repo was never written. |

| 109 | 2026-09-23 | Gate 2 re-review after remediation | `opus-reviewer`, fresh | ~97k, 36 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS]**, no findings in the executable change. **All mutants dead**, including three it devised itself — notably the natural refactor slip of assigning the array before resolving the target — plus a faithful-replica **control mutant that stayed alive with all tests passing**, which is what shows the harness was not failing for incidental reasons. Instrumented the mid-flush observer rather than trusting it: exactly one sample, taken inside `changeChatTo`'s own flush, and both end-state tests confirmed passing under the mutant it is there to catch. Cleared a hazard nobody had raised — the flush now runs before Sortable's `destroy()` — by showing the list is keyed on a counter bumped only after destroy. Remaining MINORs were a stale test header, a test whose title promised a spy contrast that did not exist (deleted as dead weight, it never failed alone across six mutants), `samples` collected but never asserted, and the §9.6 latent-fragility note that the plan had promised but the comment never shipped. |

| 110 | 2026-09-23 | Realm fetch contract: `getRisuHub`'s real return shape, every consumer, existing loading/spinner conventions | `investigator` (Sonnet 5) | ~55.5k, 35 tool uses | No | **Refuted two premises the brief carried.** Found a *third* consumer (`src/LiteMain.svelte`, unreachable dead code but typechecked) where the brief said two, and established that the component the brief named `RisuHubIcon.svelte` does not exist — it is `RealmHubIcon.svelte`, aliased at both import sites. Also confirmed `getRisuHub` returns `jso.cards` (undefined on a 200 without that key) and has no timeout. |

| 111 | 2026-09-23 | Stage 2 plan gate | `adversarial-reviewer` (Sonnet 5) | ~167.4k, 44 tool uses | n/a (gate) | **[REJECT].** Compiled the plan's Tailwind classes through the repo's own `tailwindcss@4.2.2` and proved `md:col-span-2` with `lg:col-start-3` leaves `grid-column-end: span 2` live at desktop width, spilling the card into a fabricated fourth grid track. No unit test could have caught it — `happy-dom` performs no layout. Also found the test-rewrite scope understated. |

| 112 | 2026-09-23 | Stage 2 plan gate round 2 | `adversarial-reviewer` (Sonnet 5) | ~152.2k, 44 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS].** Verified the `lg:col-span-1` fix by compiling it rather than re-reading the assertion. Found the generation guard would be applied to the success path only, and that a plan section contradicted another — both folded in before implementation. |

| 113 | 2026-09-23 | Stage 2 implementation gate | `opus-reviewer` (Opus 5) | ~168.4k, 75 tool uses | n/a (gate) | **[REJECT].** Proved with a differential test that "Hide RisuRealm" had stopped being honoured on two paths — the announcement banner escaped the guard, and the `online` handler refetched for an opted-out user. Also found a timer abort during the body read misclassified as `malformed`, and that the `AbortSignal.timeout` rationale in a shipped comment was contradicted by `src/ts/kei/backup.ts`, which already uses it. |

| 114 | 2026-09-23 | Locale machine-translation artifact survey across all six non-English locales | `translator` (Sonnet 5) | ~124.7k, 41 tool uses | No | Found `zh-Hant`'s `persona` byte-identical to `userSetting` (two menu entries rendering the same label), an omitted OpenRouter caveat, `vi`'s `lorebook` as "book of legends", and six `vi` strings never translated. Reported coverage honestly as exhaustive for brand names and sampled elsewhere. |

| 115 | 2026-09-23 | Stage 3 evidence packet: every hardcoded repo/community URL, the real git remotes, and whether any accessible disclosure pattern exists to reuse | `investigator` (Sonnet 5) | ~71.4k, 35 tool uses | No | **Established the stage's central constraint.** `aria-expanded`, `aria-haspopup` and `aria-controls` appear **zero** times in all of `src/lib`, and no accessible-primitives dependency exists — so the maintainer's tap-and-keyboard requirement had to be met by building the pattern, not composing one. Also found two link surfaces the brief had not named. |

| 116 | 2026-09-23 | Stage 3 plan gate | `adversarial-reviewer` (Sonnet 5) | ~75.5k, 49 tool uses | n/a (gate) | **[REJECT].** The plan specified `role="menu"`/`role="menuitem"` for a set of links that navigate away — the wrong ARIA pattern, promising a keyboard contract the plan never specified. Also found the markup restructuring understated (links cannot nest inside the `<button>` each card's root then was) and the reveal mechanism unspecified, with both obvious choices colliding with existing constraints. |

| 117 | 2026-09-23 | Stage 3 plan gate round 2 | `adversarial-reviewer` (Sonnet 5) | ~81.7k, 39 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS].** Found the plan instructed the `translator` agent to delete keys, which `.claude/agents/translator.md` rule 1 forbids outright — an instruction with no valid owner. Resolved by reusing the keys rather than granting an exception. Also traced the grid placement to show a sibling card would reflow when the disclosure opened. |

| 118 | 2026-09-23 | Stage 3 implementation gate | `opus-reviewer` (Opus 5) | ~142.0k, 49 tool uses | n/a (gate) | **[REJECT].** Found an accessibility regression introduced *by the accessibility stage*: `aria-label` on the card button replaced its accessible name, so the description stopped being announced — and two new tests asserted the broken value, pinning it. Also flagged the Email card's personal address as unrecorded scope (the decision was real; the record was missing, now `MC-066`), and three shipped artifacts stating things that were false. |

| 119 | 2026-09-23 | Stage 3 implementation gate round 2 | `opus-reviewer` (Opus 5) | ~112.1k, 57 tool uses | n/a (gate) | **[REJECT] on artifacts only; code found sound.** Ran five mutations against the accessible-name fix through an in-memory transform and confirmed the composed name is pinned as title → description → marker. Rejected because the commit message's counts had gone stale from the previous remediation, and because its claim that a translation slip "reached three card titles" was false — it had reached six, and three still carried it. |

| 120 | 2026-09-23 | Remediate two defects found by the Stage 3 live check: `upstream` pill contrast and CJK title wrapping | `sonnet-coder` (Sonnet 5) | ~20.0k, 12 tool uses | No | Applied exactly the four class strings specified — 4 insertions, 4 deletions, two files — and reported the suite and `pnpm check` verbatim against the stated baseline rather than summarising them. Flagged a Git `LF will be replaced by CRLF` warning instead of acting on it, which was the right call: independently confirmed as autocrlf noise, since `SourceDisclosure.svelte` was already LF in `HEAD` before the edit. Both fixes re-measured in Chrome afterwards and confirmed live.

| 121 | 2026-09-23 | Move the disclosure cards' keyboard focus ring from the inset trigger to the card | `sonnet-coder` (Sonnet 5) | ~45.1k, 30 tool uses | No | Confirmed both rules are emitted by running a real `vite build` and grepping the CSS, rather than asserting it, then deleted the build output. Kept the shared card class strings untouched so the plain link cards could not change. Correctly declined to write a test `happy-dom` could not fail. Hand-verified by the maintainer. |

| 122 | 2026-09-23 | Implement `MC-067`: let a bare Space/Enter reach a keyboard-focused control | `sonnet-coder` (Sonnet 5) | ~71.5k, 44 tool uses | No | Extracted a pure seam with an injectable focus predicate and showed tests red before green, including a wiring test on the real listener that fails if the guarded call is deleted. The requested mutation (dropping the keyboard-focus condition) failed exactly the two reroll-hazard tests. Flagged its one deviation from the brief (swapping the iterable instead of wrapping the loop, to avoid reindenting ~140 lines) instead of making it silently. **Faithfully implemented a role list the brief should not have contained**, which the gate then caught. |

| 123 | 2026-09-23 | Gate for the live-check fixes, focus ring and `MC-067` | `adversarial-reviewer` (Sonnet 5) | ~128.0k, 53 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS].** Found a regression on the app's own code: the ARIA-role branch made Space do nothing on ~30 `role="button"` elements that implement Enter only or nothing, where it had previously jumped to the chat input. The branch was the Orchestrator's addition to the brief, not the maintainer's decision. Ran mutations in a scratch copy and reported them precisely, and marked one wiring mutation it could not execute as **reasoned, not executed**. Disclosed that it briefly ran `npx @tailwindcss/cli`, which auto-installed a package against its constraints, and confirmed `package.json` and the lockfile were unchanged. |

| 124 | 2026-09-23 | Remediate gate 123: narrow the `MC-067` yield to native controls | `sonnet-coder` (Sonnet 5) | ~33.3k, 20 tool uses | No | Removed the ARIA-role branch and replaced the two "role yields" tests with four "role does not yield" tests, shown red (4 failed) against the old source before the fix and green after. Suite 72 files, 993 passed, 4 skipped, confirmed independently by the Orchestrator. Reported the blank first line of `hotkeyYield.ts` as "already gone" when the Orchestrator had seen it by byte dump before dispatch; the end state is correct, but the report of how it got there is not reliable. The Orchestrator also confirmed the hamburger button in the maintainer's third hand test is a native `<button>`, so the narrowing leaves that result valid. No re-gate: the change deletes a branch the gate itself named, with the remedy the gate specified. |

| 125 | 2026-09-23 | Durable drafts: red tests for the `MC-068` marker, draft age and translation-editor capture | `test-warrior` (Sonnet 5) | ~194.6k, 52 tool uses | No | 18 red for the stated reasons plus one suite failing to load; baseline intact. Caught a contract gap in the Orchestrator's brief: for a `tr:` record the "not a restore" comparison must be against the cached translation, not `baseData`. |

| 126 | 2026-09-23 | Durable drafts: implement the marker, draft age and translation capture | `sonnet-coder` (Sonnet 5) | ~190.6k, 102 tool uses | No | All green. Two deviations reported honestly: an equals-seed delete in the capture effect (accepted) and a non-enumerable `updatedAt` shaped to keep old `toEqual` assertions passing (rejected by the Orchestrator; assertions updated instead). |

| 127 | 2026-09-23 | Translate `draftRestored` / `draftRevert` into six locales | `translator` (Sonnet 5) | ~42.2k, 36 tool uses | No | Two keys per locale, parity checked. de/es render Revert as "discard" (flagged to the maintainer). |

| 128 | 2026-09-24 | Gate 2 round 1, durable drafts | `opus-reviewer` (Opus 5) | ~225.1k, 92 tool uses | n/a (gate) | **[REJECT].** False comments and MC-068 text, age tests that never asserted the age (`Date.now` captured by reference), four surviving mutants, three real minors. Report 20 section 11. |

| 129 | 2026-09-24 | Remediate round 1 (tests, then source; sweep seam) | `test-warrior`, `sonnet-coder` (Sonnet 5) | resumed agents; cumulative contexts ~420k / ~280k | No | Mutant kills shown for j, i, g5; four reds turned green by source; `sweepDraftRegistrations` seam extracted, its loop call stated as untestable. |

| 130 | 2026-09-24 | Gate 2 round 2 | `opus-reviewer` (Opus 5) | ~204.8k, 73 tool uses | n/a (gate) | **[REJECT], substantive.** The re-stamp fix the Orchestrator's brief specified (skip while buffer equals opening text) lost return-to-origin edits. Overlapping failing saves switched capture off. |

| 131 | 2026-09-24 | Remediate round 2 (touched flag) | `test-warrior`, `sonnet-coder` (Sonnet 5) | resumed agents | No | R2-A..D red then green. |

| 132 | 2026-09-24 | Gate 2 round 3 | `opus-reviewer` (Opus 5) | ~184.8k, 78 tool uses | n/a (gate) | **[REJECT]** on one false comment; touched flag correct in every traced scenario. Found a fail-then-succeed overlapping save leaving a stale draft. |

| 133 | 2026-09-24 | Escalation: three consecutive Gate 2 rejections | `senior-advisor` (Fable 5.1) | ~114.8k, 33 tool uses | Yes (escalation) | Root cause: an `$effect` cannot tell user writes from component writes to the buffer. Redirected capture to `input` events. Process correctives now in AGENTS.md section 4. **Changed the direction.** Commissioned before the maintainer's wording-only rule existed; under that rule round 3 would not have counted. |

| 134 | 2026-09-24 | Port the round-3 scenarios; purge history prose (re-dispatched after a Windows update rebooted the machine mid-task) | `test-warrior` (Sonnet 5) | ~244.3k, 112 tool uses | No | 13 mechanism-agnostic scenario tests; G and H red on the effect design. |

| 135 | 2026-09-24 | Replace effect capture with `input`-event capture | `sonnet-coder` (Sonnet 5) | ~206.6k, 55 tool uses | No | G and H green; flags, snapshots and pre-await nulling removed; `Chat.svelte` diff about 100 lines smaller; comment lines 190 to 124. |

| 136 | 2026-09-24 | Gate 2 round 4 (full review of the new mechanism) | `opus-reviewer` (Opus 5) | ~227.9k, 88 tool uses | n/a (gate) | **[REJECT], wording only.** All five invariants held; `bind_value` premise settled from svelte source. Six stale comments passed the keyword grep; one test gap (j2) found and closed. |

| 137 | 2026-09-24 | Gate 2 round 5 | `opus-reviewer` (Opus 5) | ~192.3k, 51 tool uses | n/a (gate) | **[REJECT], wording only.** One test comment endorsed the round-2 rule the code deliberately breaks. |

| 138 | 2026-09-24 | Live check in Chrome (Orchestrator), default, cardboard and mobilechat | Orchestrator | n/a | n/a | Report 20 Path 1 reproduced and survived (type, reroll, unReroll, reopen: draft restored with marker). Revert, age ("1분 전" at 77 s), cardboard 384 px card unchanged, light-surface contrast 7.82:1. Found default-surface contrast 2.72:1 and a mid-word Korean wrap; both fixed and re-measured (6.82:1). Translation editor and `Prereroll` not live-checked. |

| 139 | 2026-09-24 | Gate 2 round 6 | `opus-reviewer` (Opus 5) | ~199.0k, 87 tool uses | n/a (gate) | **[REJECT], wording only** (a red-before-green header over four guards). Measured a CJK regression introduced by the wrap fix itself (`break-keep` on spaceless cn/zh-Hant labels); fixed with `wrap-anywhere` and re-measured live. |

| 140 | 2026-09-24 | Gate 2 round 7 | `opus-reviewer` (Opus 5) | ~182.3k, 46 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS].** One vacuous assertion (reads a raw fixture that the `$state` proxy never writes); unlabelled guards; a 320 px squeeze the maintainer chose to fix by letting the Revert button wrap. |

| 141 | 2026-09-24 | Final check: the fix-up diff after round 7, and a fact-check of the commit message | `opus-reviewer` (Opus 5) | ~157.9k, 85 tool uses | n/a (gate) | **[REJECT], wording only.** The fix-up diff was byte-verified: only the wrap classes changed in production. One guard note claimed HEAD never touches the local-drafts registry, but it does. Four commit-message claims were WRONG or OVERSTATED, including Path 7, described as an unmount when it is a prop reset. All fixed. Its optional `basis-28` suggestion was taken and re-measured live in seven locales. |

| 142 | 2026-09-24 | openURL blast radius: opener dependence, leave-page handlers, the Tauri path, headers, a test seam | `investigator` (Sonnet 5) | ~98.8k, 52 tool uses | No | Confirmed 20 non-test call sites (it said 14 files; `doc-verifier` recounted 13, the 14th being the defining module); none reads `openURL`'s return value or relies on `window.opener`. Plugin APIs, CBS and Lua do not expose `openURL`. Confirmed the MCP OAuth gap the brief asked it to check: `mcplib.ts`'s `oauthLogin` passes an MCP server's unvalidated `authorization_endpoint` straight into `openURL`, so an unparsable or dangerous scheme (`javascript:`/`data:`) could reach `window.open` on the web. Tauri's shell-plugin default open scope (2.3.6) already restricts to http(s)/mailto/tel, so the Tauri branch needed no change. No escalation; no premise in the brief was refuted. |
| 143 | 2026-09-24 | Implement `openUrlWeb.ts`, wire it into `openURL`, and add the reload-guard handoff allowance | `sonnet-coder` (Sonnet 5) | ~65.5k | No | New `src/ts/openUrlWeb.ts`: http/https opens a new tab with `noopener` (referrer unchanged, relative URLs resolve against the page); mailto:/tel: is handed to the OS from the current tab; any other or unparsable scheme opens nothing, with a warning naming only the scheme. `src/ts/reloadGuard.ts` gained a one-shot `allowNextBeforeUnload`/`consumeExternalHandoffAllowance` allowance (3s from the most recent handoff), consulted by `src/preload.ts`'s `beforeunload` handler, kept separate from the existing app-initiated-reload flag. Tauri branch unchanged. |
| 144 | 2026-09-24 | Comment-only cleanup alongside the openURL change | `sonnet-coder` (Sonnet 5) | ~68.2k | No | Comment-only edits in `src/ts/hubHtml.ts` and `src/lib/UI/SourceDisclosure.svelte`; no behavioural change. |
| 145 | 2026-09-24 | Red-before-green tests for `openUrlWeb` and the beforeunload allowance | `test-warrior` (Sonnet 5) | ~116.0k | No | New `src/ts/openUrlWeb.test.ts` (18 tests) and `src/preload.beforeUnload.test.ts` (14 tests). Against the pre-change behaviour, all 18 `openUrlWeb` tests and 6 of the 14 beforeunload tests fail for the stated reasons; the other 8 beforeunload tests are labelled guards. |
| 146 | 2026-09-24 | Gate 2 round 1: the openURL implementation | `adversarial-reviewer` (Sonnet 5) | ~145.1k, 102 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS].** One substantive finding, verified by execution: overlapping handoffs raced — two `allowNextBeforeUnload` calls inside the 3s window let the first call's timer clear the second call's allowance, so a legitimate handoff could still trigger the "Leave site?" prompt (the failure direction only adds an unwanted prompt, never suppresses a real one). Also a weak-mutation-coverage note: a raw-prefix mutant survived 14 of 15 mutants tried. |
| 147 | 2026-09-24 | Remediate the round-1 race (red test, then fix) | `test-warrior` (~135.6k) and `sonnet-coder` (~75.6k), both Sonnet 5 | ~211.2k combined | No | A test for the overlapping-handoff case was written and shown red first; `reloadGuard.ts` was then changed to keep the timer handle and clear it on re-arm and on consume, so an earlier handoff's expiry can no longer clear a later handoff's allowance. |
| 148 | 2026-09-24 | Gate 2 round 2: re-review, still independent of implementation reasoning | `adversarial-reviewer` (Sonnet 5, same instance as round 1) | ~215.6k, 160 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS], wording only:** wrong test counts, an overstatement in the draft commit message, a stale test title and wrong header counts. No further code defect found. |
| 149 | 2026-09-24 | Remediate round-2 wording | `test-warrior` (Sonnet 5) | ~163.9k | No | Fixed the wrong test counts, the stale test title and the header counts in the test files. The commit-message overstatement was corrected separately by the Orchestrator. |
| 150 | 2026-09-24 | Live check, Chrome, against the dev server | Orchestrator | n/a | Yes (live) | The Website card opened `risuai.net` in a new tab with `window.opener === null` and its referrer present. The fork Email card's address opened the OS mail client from the same tab, with no new tab, and the app page received a `beforeunload` for that same-tab handoff. The "Leave site?" guard is registered only when `isWeb` (`location.hostname === 'risuai.xyz'`, `src/ts/platform.ts`), so it is not registered on the dev host — the allowance was verified by tests only, not live. |
| 151 | 2026-09-24 | CHORE-22 scope: every same-tab navigation or reload on the web, which are marked, what `isWeb` also decides, dev/node-server/Tauri/plugin exposure | `investigator` (Sonnet 5) | ~105.9k, 42 tool uses | No | 11 same-tab navigation/reload sites in non-test `src/`. Four app-initiated reloads were unmarked (ToS decline and the `/sw/init` fallback in `bootstrap.ts`, `unMigrationAccount`, the "save data in account" toggle), so they already prompted on risuai.xyz. `drive.ts`'s OAuth navigation is a real navigation away. Vite's HMR full reload calls `location.reload()` directly, so the guard must be off in dev. V3 plugins cannot navigate the tab; V2 plugins could already. Recommended `!isTauri && !import.meta.env.DEV`, leaving `isWeb` alone. `isWeb`'s only consumer is preload itself (the guard and the `mainpage` branch). |
| 152 | 2026-09-24 | Red-first tests for the guard's build-target condition | `test-warrior` (Sonnet 5) | ~263.7k | No | 7 new tests in `src/preload.beforeUnload.test.ts` (21 total). Against the unchanged preload, 3 failed (node server, static self-host, a later leave after a mailto: handoff on the node server). Found that a second `vi.doMock` for the same specifier within one test does not reliably apply in Vitest 4.1.2, so each test mocks the platform once. |
| 153 | 2026-09-24 | Implement CHORE-22: the new registration condition and four reload marks | `sonnet-coder` (Sonnet 5) | ~98.0k | No | `preload.ts` registers the guard when `!isTauri && !import.meta.env.DEV`; `markAppInitiatedReload()` precedes the four reloads. 21/21 green; suite 1092 passed, 4 skipped. |
| 154 | 2026-09-24 | Fix test comments that described the unchanged preload as current | `test-warrior` (Sonnet 5) | ~288.3k | No | Found by the Orchestrator reading the diff before the gate. Header restated against an explicit baseline; the dev test confirmed to be the only one that fails against a guard gated on `!isTauri` alone. |
| 155 | 2026-09-24 | Gate 2: CHORE-22 and its commit message | `adversarial-reviewer` (Sonnet 5, fresh) | ~131.4k, 82 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS], wording only.** Re-ran both baselines (3 of 7 fail against `isWeb`-only; 1 against `!isTauri`-only). Confirmed the node server serves the built `dist`, so `DEV` is false there. Found no missed reload. One test comment cited a verification run; fixed (~293.0k, `test-warrior`). Noted that Google Drive sign-in on self-hosted builds now shows the leave-site dialog, by design. Every commit-message claim VERIFIED. |
| 156 | 2026-09-24 | Live check, Chrome, against a production build on the node server | Orchestrator | n/a | Yes (live) | The maintainer built with `VITE_RISU_LEGAL_CONFIGURED=TRUE` for that run only and started `pnpm run runserver` (http://localhost:6001). The page loaded one hashed bundle and `globalThis.__NODE__` was true. A navigation away was stopped by a real "Leave site?" dialog, so the guard is live on a self-hosted host (CHORE-22). Clicking the fork Email address dispatched a trusted `beforeunload` that the guard let through: no dialog, no new tab, the page stayed loaded. A `beforeunload` dispatched after it was prevented, so the allowance was consumed (the openURL fix). |
| 157 | 2026-09-24 | Composer stage: where composer state lives, whether the mis-send still holds, async writers, chat identity, registry reuse, plugin exposure | `investigator` (Sonnet 5) | ~176.2k, 76 tool uses | No | Report 20 section 4.5 holds at HEAD. One composer (`DefaultChatScreen.svelte`) serves every theme, mobile and LiteUI. Three async writers have no identity check: translate, Post File, paste. `chat.id` is optional and backfilled only at boot; `ChatList` New Chat creates an id-less chat. The composer already has a liveness-only `COMPOSER_DRAFT_KIND` registration. No plugin, CBS or script reads the composer. Missed a second loss in the same function: `sendMain` writes its captured message array to the live `chatPage` after its awaits. The Orchestrator found it by reading `sendMain` (Report 22 section 2.2). |
| 158 | 2026-09-24 | Gate 1, composer drafts plan (Report 22), rounds 1 and 2 | `opus-reviewer` (Opus 5, fresh each round) | ~152.7k and ~138.4k | n/a (gate) | **Round 1 [REJECT]**: the WeakMap key of an id-less chat changes on `characterFormatUpdate` and on the trigger-clone replacement; unmount/remount undefined (mobile unmounts on every switch); write-back to a detached chat object; trigger `setVar` writes to the live selection (CHORE-25). **Round 2 [REJECT]** on rev 2: I2 (the record is the state) sound; majors all in the send window. v2 effects `setCurrentCharacter(clone)` can overwrite a whole character on a mid-trigger switch; `sendChatMain` clears the composer (Ctrl+M wipes a draft at HEAD); the prev/next hotkeys bypass `changeChar`. Orchestrator re-checked M1, M2, M4 and M5 in source. Two consecutive substantive rejections; the maintainer chose a writer rework over a switch lock (`MC-073`). |
| 159 | 2026-09-24 | Writer rework: every write in send/generation/triggers that targets the live selection or a frozen index; switch paths; building blocks; candidate shapes | `investigator` (Sonnet 5) | ~174.4k, 49 tool uses, 2 `code-searcher` dispatches | No | Two classes. Live-resolved: trigger `setVar`/`varChanged`, 9 v2 effects (the round-2 review named 7), CBS `setChatVar`, about 20 Lua writers (which ignore the trigger's `char`), plugin v2 `setChar`, v3 `sendChat`, `sendMain`. Frozen index: `sendChatBody`'s ~40 writes, broken only by an array shift (Branch/Copy/New Chat `unshift`, reorder, permanent character delete). Memory modules write nothing themselves. `SendChatCallContext` already captures `chaId` for save-marking. Shape A (explicit origin threading, ~75-90 call sites) preferred over async-context. Orchestrator spot-checked `v2ModifyLorebook`, `v2SetAuthorNote`, `setChatVar`, Lua `setName` and the frozen-index capture. |
| 160 | 2026-09-24 | Comparison with the HaejeokRisuai fork (`C:\Projects\HaejeokRisuai`, GPL-3.0): writer, data model, server, assets, account removal | `investigator` (Sonnet 5) | ~116.7k, 45 tool uses | No | Forked from upstream at `22ea4a64` (2026-08-17); about 1,300 fork-only commits. **Writer: ADOPT-IDEA.** `src/ts/chatTarget.ts` defines `ChatTarget {characterId, chatId}` and `resolveChatTarget`, resolved at write time; `runTrigger`, `chatVar` and `sendChat` use it (32 files reference it; about 6 read). **Reference only:** relational storage (Postgres/SQLite, one row per message; `.bin` kept for import only), the fragmented native backup format, and a global revision counter (coarser than our per-key scheme). **Not relevant:** S3/RustFS (generic S3 client) and RisuAccount removal (nothing replaced it). Orchestrator read `chatTarget.ts` and the `runTrigger` target in their tree, and inferred that their trigger target, built from `char.chaId`, probably drops group-member trigger writes. |
| 161 | 2026-09-24 | Writer rework direction (MC-073 escalation, maintainer-approved) | `senior-advisor` (Fable 5.1) | ~149.4k, 59 tool uses | Yes (escalation) | **Root cause:** the UI cursor is the only address space for writes; a unit of work has no representation of its subject. **Missed insights:** the engine layers already receive their subject and ignore it; one resolver plus an in-flight registry is the single choke point, and also serves MC-075; origin-bound writes to a non-selected character hit CHORE-01's unmarked-write class, so the write side must mark; group generation needs separate member and chat-owner ids (a traced upstream bug: a member's trigger clone replaces the group slot). **Strategy:** Shape A reduced; W0 identity/resolver, W1 engine binding (closes CHORE-25), then the composer stage, then W2 generation/deletion and W3 `/` commands. **Do not:** use an async-context "current", a switch lock, index pairs, or `findCharacterbyId` as the resolver. Orchestrator verified the group trigger call, `findCharacterbyId`'s group filter and blank fallback, and the auto-continue recursion; the recursion gap before the fresh selection read is microtask-only unless multiuser is connected. |
| 162 | 2026-09-24 | Pre-W0 checks requested by `senior-advisor`: persistence of writes to a non-selected character, CBS variable writers, group-slot overwrite, request-trigger redirect | `investigator` (Sonnet 5) | ~246.2k, 170 tool uses | No | **Check 1 (run):** an in-place field or nested write to a non-selected character is lost after save and decode unless `markCharacterForSave` is called; whole-object replacement persists through the identity tracker. The write helper must mark by default. **Check 2 (traced):** the parser wires `getChatVar`/`setChatVar` to the module functions, so no per-call target exists; `chatVar` needs an explicit target. `loadLoreBookV3Prompt` resolves the selection at entry. `runScripted` already accepts `char`/`chat`/`setVar`, so only its callers change. **Check 3 (traced):** a member's `start`/`output` trigger replaces the group's slot with the member (CHORE-26); `input` is not exposed. **Check 4 (traced):** the `request` trigger reads the live selection in the retry loop and runs uncloned (CHORE-27). Orchestrator re-checked the request block, the parser wiring, `loadLoreBookV3Prompt` and upstream presence. |
| 163 | 2026-09-24 | CD-4 `updateInlayScreen`: red tests, fix, and Gate 2 (two passes) | `test-warrior`, `sonnet-coder`, `doc-writer` (Sonnet 5); Gate 2 `opus-reviewer` (Opus 5, fresh) | test-warrior ~66k + ~75k + ~80k + ~108k; sonnet-coder ~50k + ~38k; doc-writer ~38k + ~54k; opus-reviewer ~110k + ~128k | n/a (gate) | Red first: 4 of 9 tests failed against HEAD's function (Orchestrator re-ran). Fix: each `newGenData` field is resolved separately; user-authored text is kept, and missing, non-string, empty or built-in-default fields get the mode default; defaults are byte-identical to HEAD and upstream. **Gate 2 pass 1 [APPROVE-WITH-FINDINGS]:** a differential against HEAD over 144 transitions found no stuck default. MAJOR: the imggen Instructions box has two jobs across the Inlay Screen toggle, so kept text can do the wrong one (the maintainer chose keep and document, MC-077). MINOR: four surviving mutants (tests added), overstated comments, stale wiki citations. The Orchestrator separately caught two pre-gate test comments that described the old function as current, and two "coverage" comments whose reasoning was false. **Pass 2 [APPROVE-WITH-FINDINGS]:** 4 of 13 tests fail at HEAD; every mutant is killed; three wording and record minors were fixed. Suite: 78 files, 1105 passed, 4 skipped; `pnpm check` clean. |
| 164 | 2026-09-24 | W0 identity: chat and `chaId` creation sites, duplicate routes, readers of `chat.id`, cold storage and delete, building blocks for the origin module, resolver cost | `investigator` (Sonnet 5) | ~172.0k, 78 tool uses, 1 `code-searcher` dispatch (~44.2k) | No | Boot `assignIds` dedupes `chaId` and chat ids in one database-wide set and saves at once; `makeColdData` runs only at boot. Id-less chats at creation: `ChatList` New Chat, `createNewGroup`, `createBlankChar`, `characterFormatUpdate`'s fallback (backfilled later in the same call), both `characterCards.ts` literals (backfilled only if "go to character on import" is on), and the cold-storage placeholder (new; not in Report 23). Duplicate routes: HTML import keeps the file's id; `risuAllChats` v1 keeps a present id (new). Plugin v3 `setChatToIndex`/`setCharacterToIndex` install whatever they get. No character-duplicate feature exists. `markCharacterForSave` keys on `chaId` only. `localDrafts.ts` is the registry precedent. Three chat-delete handlers, not two. In passing: group turns re-read `selectedCharID` in each recursive `sendChat` (W-4); `makeColdDataForCharacter` does not skip groups (untraced). Orchestrator re-read the HTML and v1 imports, the placeholder, `ChatList`'s handlers, `createNewGroup`, `createBlankChar`, `assignIds`, `SendChatCallContext` and the recursion, `removeChar`, and the v3 install APIs. Plan: Report 24. |
| 165 | 2026-09-24 | Gate 1, W0 identity plan (Report 24) | `opus-reviewer` (Opus 5, fresh each round) | round 1 ~187.5k, 110 tool uses | n/a (gate) | **Round 1 [REJECT]** on rev 1. B1: id-less chats still reach runtime (plugin `setDatabase`/`setDatabaseLite` with `characters` in `allowedDbKeys`, the v2.1 `getDatabase` live proxy, v2 `setChar`, and three backup loads that call `setDatabase` without reloading), and rev 1 had dropped the strategy's fallback, so W1 would drop writes that land at HEAD. M1: a fresh id on a slot replacement breaks identity (inherit instead). M2: "mark the member if touched" is unimplementable. M3: red-by-missing-import proves nothing; handler wiring untested. 12 minors, including one the reviewer ran (a fill after install is invisible through the `$state` proxy). Orchestrator re-checked B1's routes in source. Rev 2: fills on every install route and after the three loads, inherit on replace, first occurrence wins, `beginWork` closes the live proxy, marks in a `finally`, callback-scoped resolution. |
| 166 | 2026-09-24 | Gate 1 round 2, W0 identity plan (Report 24 rev 2) | `opus-reviewer` (Opus 5, fresh) | ~180.1k, 80 tool uses | n/a (gate) | **[REJECT].** Route coverage closed; the install-route enumeration was found complete. BLOCKER-1: slot-index inheritance gives an existing chat's id to a new empty chat (unshift, then `setCharacterToIndex`), so branch links, drafts and in-flight writes follow the id to the wrong chat. MAJOR-1: position-first dedupe lets an incoming object take an existing object's id. MAJOR-2: "the same rule as boot" was false; boot uses one database-wide set holding `chaId`s and chat ids together. 10 minors (`beginWork` on a clone, labels, mount test, O-4 chat-write semantics). The reviewer ran a `tsc` check: typing `fn` to return `undefined` rejects an async `fn`; a `fn` typed `any` passes. Orchestrator re-checked `changeChatTo(id)`, the `branchedfrom` link and `runTrigger`'s clone. Second consecutive substantive rejection; the mechanism (heuristic identity reconstruction) was replaced in rev 3 by "never guess": fresh ids when ambiguous, existing ids win, one fill that is boot's algorithm with a seed. |
| 167 | 2026-09-24 | Gate 1 round 3, W0 identity plan (Report 24 rev 3) | `opus-reviewer` (Opus 5, fresh) | ~168.0k, 89 tool uses | n/a (gate) | **[REJECT].** "Never guess" is right, but deciding at install time which holder of an id is the real one is still a guess. F1 (run): a chat swap through two `setChatToIndex` calls re-ids one chat for good; the character swap leaves a stale duplicate save block that returns after a reload. F2: the seed for `setDatabase`/`setDatabaseLite` is undefined. F3: a copied-id duplicate inside one incoming value is settled by position. F4 (measured, i9): building the seed takes ~21 ms per call at 1000 x 20. F5: cold restore is a missing install route. F6: the fill must tolerate malformed input. Wording: `beginWork` needs objects read back through `DBState` (run). Orchestrator checked F1 against the plan's own I-5. Third consecutive substantive rejection: escalated to `senior-advisor` before rev 4. |
| 168 | 2026-09-24 | W0 identity direction after three Gate 1 rejections | `senior-advisor` (Fable 5.1) | ~111.6k, 30 tool uses | Yes (escalation) | **Root cause:** every revision tried to decide at install time which holder of an id is the real chat, with no input that can decide it (v3 setters get clones, one task per call; copy idioms carry the old id; swaps are sequences). Unifying boot's repair with the runtime fill imported repair semantics where repair is unsafe (F1, F2, F4). **Missed insight:** the id field is the plugin's declaration (id-less = positional, as upstream; ids kept = identity); a duplicate is attributable at the install call and transient in a swap. **Strategy:** never reassign or move a present id at runtime; fill missing ids before assignment (fresh; inherit only the single addressed object's id in `setChatToIndex` and the `chaId` in `setCharacterToIndex`/`setChar`); no seed or global scan on install; warn on a duplicate naming the plugin; resolution treats an id with two holders as gone (pending the maintainer); boot unchanged and kept a separate function. Verified: reran `swap.mjs` (F1 holds); v3 setters deserialise clones in `factory.ts`; cold restore already fills ids in `characterFormatUpdate`; a character swap with ids kept leaves no stale block. **Next:** measure the ambiguity scan per resolution; trace a mid-swap autosave; read `risuai.d.ts`; run the v2.1 `setDatabaseLite(getDatabase())` round trip. |
| 169 | 2026-09-24 | W0 follow-up checks requested by `senior-advisor`: ambiguity-scan cost, mid-swap autosave, plugin docs, v2.1 round trip | `investigator` (Sonnet 5) | ~256.6k, 146 tool uses (much of it scratch-environment friction) | No | **Check 1 (run, i9, Svelte dev build):** a full ambiguity scan at 1000 x 20 costs ~0.75 ms median / ~1.3 ms p95 per resolution, almost all in the `chaId` scan; the chat scan costs ~0.02 ms; position does not matter. No cache needed. **Check 2 (traced; existing tests rerun):** a mid-swap autosave cannot delete a block. The identity tracker marks only the incoming `chaId`, and `prepareSaveIteration`'s no-reload branch drops ids absent from `characters` before the encoder runs. Chat swaps mark the owner on every call. Pre-existing and out of scope: an edit already pending on the absent character is dropped for that iteration. **Check 3 (read):** the plugin docs promise positions only ("by their position", "at a specific index"), nothing about `chat.id`/`chaId`. **Check 4 (run):** v2.1 `setDatabaseLite(getDatabase())` receives the live objects, including chats inserted through the proxy, and is otherwise a no-op; an object becomes a different proxied reference once it enters `$state`. Orchestrator checked `git status` afterwards: the investigator had briefly written a file into the repo root and removed it; the tree was clean. Maintainer decided the ambiguity question (MC-078). Plan rev 4 written. |
| 170 | 2026-09-24 | Gate 1 round 4, W0 identity plan (Report 24 rev 4) | `opus-reviewer` (Opus 5, fresh) | ~209.0k, 109 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS].** The mechanism holds, with no new data-loss path relative to HEAD. MAJOR-1 (run): a duplicate `chaId` does not survive a save, since there is one block per `chaId`, so one whole character is lost and a copy can overwrite the original. It is pre-existing and upstream, and it contradicted the plan and MC-078's "boot repairs it". MINORs: the repair after a backup load could throw before the full-reload flag was set; spurious duplicate warnings on swaps; v2.1 cannot name the plugin; positional handling was overstated; the app itself makes duplicates until W1 and W2 land; the fill cost on `setDatabase*` was unstated; "once per unit of writes" could not be enforced; filling a `chaId` brings back a stale block (run); wording. Round 3's F1 to F3 and F5 to F8 are closed; F4 is partly open. Orchestrator re-checked MAJOR-1 in `risuSave.ts` and the upstream presence of CHORE-28, 30 and 31. The maintainer chose a separate save fix straight after W0 (MC-079, CHORE-28). Rev 4.1 folds in every finding. New chores: CHORE-28 to CHORE-32. |
| 171 | 2026-09-24 | Gate 2 round 1, W0 implementation (Report 24 rev 4.1), and its remediation | `opus-reviewer` (Opus 5, fresh); fixes by `test-warrior` and `sonnet-coder` (Sonnet 5) | reviewer ~267.6k, 170 tool uses; test-warrior ~395.7k; sonnet-coder ~320.7k + ~354.8k | n/a (gate) | **[REJECT].** MAJOR-1 (run): the install fill inherited a replaced `chaId` or chat id that another holder still had, and the duplicate warning skipped `newId === oldId`. A v3 swap of an id-less character therefore gave two characters one `chaId`, and one was lost at save; HEAD saves both. The plan's inherit rule was underspecified: Gate 1 round 2 asked for "held nowhere else", and the escalation dropped it by reading the scope as the incoming value. MAJOR-2 (wording): false commit claims ("boot's own algorithm, now exported as assignIds"; counts 32/20, actually 34/18) and stale test comments. MINORs: `repairDatabaseIds` overwrote a non-array `chats`; `beginWork` could fill and then refuse; 14 surviving mutants; a dead `assignIds(target)`. The origin module was confirmed sound. **Remediation:** inheritance only when no other holder has the id (about 0.56 ms at 1000 characters); `beginWork` checks everything before it writes; `chats ??= []`, and a non-array value is skipped; `assignIds()` takes no argument. The coder's first fix defeated the `$state` proxy with `defineProperty` to pass a `toBe`; the Orchestrator had it reverted and the test changed to deep-equal on `$state` plus identity on a plain object. Optional held-elsewhere parameters were made required. 13 of the 14 mutants are now killed in scratch copies. **Process lapse:** the coder ran one fill-order mutant check by editing tracked `v3.svelte.ts` in place and reverting it, against the scratch-only rule; the Orchestrator verified the file afterwards (fill before install). **Recount (Orchestrator, run):** the 124 new tests were run against a HEAD export with `chatIds.ts` stubbed as no-ops. Results: 39 fail on assertions; 7 New Chat tests fail only on the missing function; 53 specify the new modules; 25 pass. Three of the four swap tests for W0's own inheritance bug pass at HEAD, which never filled ids; the fourth fails there on its fresh-id assertion (corrected after Gate 2 round 2). Suite: 98 files, 1229 passed, 4 skipped; `pnpm check` clean. |
| 172 | 2026-09-24 | Gate 2 round 2, W0 implementation, and the rev 4.2 remediation (no inheritance) | `opus-reviewer` (Opus 5, fresh); fixes by `test-warrior` and `sonnet-coder` (Sonnet 5) | reviewer ~334.4k, 185 tool uses; test-warrior ~380.5k; sonnet-coder ~250.6k | n/a (gate) | **[REJECT].** MAJOR-A (run): the held-elsewhere check is per call, so a swap that installs the id-less side first still leaves two holders of one id. Characters lose one at save; the same holds for v2.1 `setChar` and for chats. A three-way rotation defeats any local check. MAJOR-B: false claims ("creates no such duplicate"; CHORE-28 cited as landed; a `chatIds.ts` header saying nothing reassigns a present id; stale comments). MINOR: `setDatabaseLite` at 1000 x 20 went from 16.6 to 83.3 ms with no measurement reported; 3 mutants survived; scenario 18 had no test; 19 coverage tests lacked labels. The counts 124/39/7/53/25 and the stub method were confirmed. **Mechanism question:** the second consecutive rejection on the same mechanism. Inheritance is a guess that no per-call check can make safe, so rev 4.2 drops it and every missing id is fresh. This reverses Gate 1 round 1 M1; the cost is a new identity for an object a plugin rewrites without its id (writes in flight drop, a draft is orphaned, nothing is lost at save). **Remediation:** 9 red tests (fresh id on each route, both swap orders, three-way rotations) went green once the held-elsewhere helpers were removed. The duplicate walk now skips the live-array idiom and counts only repeated ids. The coder measured 78.1 to 37.7 ms (live idiom), 66.4 to 11.4 ms (snapshot) and 57.5 to 9.2 ms (`setDatabase`), with no HEAD baseline taken on the same machine. Recount (Orchestrator, run): 133 new tests; 44 fail on assertions at HEAD; 6 New Chat tests fail only on the missing function; 54 specification; 29 pass. Suite: 98 files, 1238 passed, 4 skipped; `pnpm check` clean. Gate 2 escalation count: 2. |
| 173 | 2026-09-24 to 2026-09-25 | RisuAccount removal blast radius (MC-080): storage backend, identity/UI/hub, data compatibility, what removal unblocks, plugins/servers/tests, HaejeokRisuai reference | Workflow `risuaccount-removal-map`: 6 `investigator` lenses (Sonnet 5), each verified by `deep-investigator` (Opus 5), then an `investigator` completeness critic with targeted gap investigations. Halted once by the weekly limit and resumed from cache | ~2.0M subagent tokens, ~1,124 tool uses (first run) | Yes (verify stage by design) | **Migration:** since upstream d0548267 (2026-06-11), an account-sync user's full `.bin` on risuai.xyz is encrypted with a key from `sv.risuai.xyz/cryptokey`. The fork decrypts it with an unauthenticated fetch and fails closed (fork 155c915c; upstream falls through). A `curl` probe from this machine got 403 on every variant tried. Assets and cold storage are written before the decrypt, so the abort alert overstates. By default, upstream `skipSavingAssetsOnWebSync` (default true, toggle hidden) leaves no assets in that `.bin`. SavePartialLocalBackup is the only plaintext export. Every `.bin` drops non-png assets. **Coupling:** Kei auto-backup, the accounter restore, the Drive `useSync` shortcut and `lightningRealmImport` die with the backend. Realm upload identity, the `kei` SD provider (it writes `db.account` from `fallbackRisuToken`), the login iframes, `loadRisuAccountData` and the Drive OAuth tokens in `db.account.data` need only the token. Realm browse/download needs no account but relies on `server.cjs`'s `/hub-proxy`, which also relays account-sync traffic for self-hosted Node users. `checkAccountSync()` installs AccountStorage at runtime, so deleting the `Init()` branch alone is not enough. At least 16 files carry backend branches and at least 17 read `db.account`. The plugin API exposes only v3 `saveMethod`. **Unblocks:** duplicated branches and MC-026's constraint on `SaveLocalBackup`; the optimisations were fenced off the account path, not blocked by it. A browser probe of `/cryptokey` by the Orchestrator was denied by the permission classifier; the maintainer asked for no probing and decided MC-081. The wiki page `Settings-Account-and-Files.md` (parallel docs session) documents the account UI. **Resumed gap rounds (~2.13M subagent tokens total, 23 agents):** (1) The malformed `encryption.risudat` branch falls through to `decodeRisuSave` on ciphertext. This is inherited from upstream, and no test covers it. A fuzz run of msgpackr 1.10.1 with risuSave's config threw on 20,000 of 20,000 random inputs at 1 KB to 1 MB, but at 16 KB or less it occasionally returned a string or object. The practical risk is negligible; Report 25 invariant 1 closes it, and MC-081 was widened to "any marker entry". (2) `SavePopupIcon.svelte` (always mounted) imports `AccountWarning` and branches on `'account-conflict'`. Report 25 missed it, and so does invariant 4's `\.account\b` grep. (3) No mechanism will handle a stale `accountst` profile once the account UI is gone. `unMigrationAccount` always writes to the plain `risuai` LocalForage, even on Node and OPFS, which is a pre-existing upstream bug. (4) `plugins.md` promises that `pluginStorage` syncs between devices. That is true only through the account cold-storage path being removed; afterwards only `.bin` and Drive carry it. (5) The backup asset loops can throw for account-sync users on hub-only keys (lightning/charx dedup) or on the fork-local 303 `AccountSyncCacheMismatchError`. The global handler alerts; no fork users are affected, and the removal deletes it. (6) Realm anonymous upload is a deliberate upstream feature (f24989c9, 2024-05-31), unchanged since, and HaejeokRisuai made the same cut without reverting it. This is INFERRED, not run. (7) Test files referencing account symbols: 18 tracked, plus 5 W0 files = 23. 1 is wholly account behaviour, 6 are mixed, 16 are incidental. |
| 174 | 2026-09-25 | What PocketRisu and PocketRisu-Kei did about RisuAccount (companion to row 173) | Workflow `risuaccount-removal-pocket-forks`: one `investigator` per fork, each verified by `deep-investigator`, then an `investigator` three-fork comparison | ~564k subagent tokens, 378 tool uses | Yes (verify stage by design) | Both forks removed login, sync, backup and multiuser as part of collapsing to a Node-only server (not a clean "sync only" precedent). PocketRisu imported account-encrypted `.bin` files as ciphertext, which bricked instances until a reject guard landed (4594674b, 2026-09-03); the guard runs after assets are already written. PocketRisu-Kei decrypts server-side through `/cryptokey` with no credentials (8cb538bd). Its removal took at least 4 commits over about 6 months, and Kei and the `account` type outlived the first pass. Realm browse/download needs no account; only upload and remove use the token. HaejeokRisuai kept only the anonymous decrypt path (legacyBackupEncryption.ts, with 4 tests). This repo's Kei code is upstream (14d2558f, 2023-12-27), not from PocketRisu-Kei. "Kei" names two unrelated things. None of the three forks stopped at removing sync alone. |
| 175 | 2026-09-25 | Scope, migration guarantee and timing of the RisuAccount removal (MC-080, MC-081) | `senior-advisor` (Fable 5.1), escalation authorised by the maintainer | ~213k, 81 tool uses | Yes (escalation) | **Root cause:** RisuAccount is one credential (`db.account.token`) from one source (the hub sign-in in UserSettings and AlertComp), used by four unrelated things. "Sync only" is a different end state, not a smaller cut: it keeps the source and two `db.account` re-creators (`AccountStorage.checkAuth`, the `kei` provider). **Missed insights:** the upstream alternatives lose different data (Partial keeps cold chats and drops non-profile images; logging out first keeps assets and drops cold chats; only both together are complete, read not run). The refusal hits only official-site users, since encryption needs a risuai.xyz origin. The marker comes after the data, so "nothing written" needs the manifest before the first write. Drive is hub-coupled, not login-coupled. A malformed marker still decodes ciphertext. **Strategy:** Option B (all of RisuAccount, Realm kept), with 9 invariants: refusal precedes every write; no marker means today's behaviour; `db.account` is read nowhere; `kei` fails loudly; an `accountst` profile does not silently boot stale data; Realm and Drive keep working; `saveMethod` is never `account`; tests for removed behaviour are deleted. Timing: W0, CHORE-28, multiuser, account, then W1 (after W1 acceptable, never after W2), as two stages. Orchestrator verified the writer order (`backuplocal.ts` 101/144, 158, 168, 174) and `unMigrationAccount` (copies `keys()` only). One correction: `drive.ts:218` does read `db.account.useSync`. The maintainer chose Option B and "refuse and name alternatives", and deferred timing (MC-080, MC-081). |
| 176 | 2026-09-25 | Gate 2 round 3, W0 implementation (rev 4.2), and its remediation | Workflow `w0-gate2-round3`: 4 `opus-reviewer` lenses (data-safety, tests-mutants-counts, wording-commit, performance); deep-investigator verify stage (not triggered). Fixes by `test-warrior` and `sonnet-coder` | ~1.24M subagent tokens, 897 tool uses (review); plus test-warrior ~251k + ~272k and sonnet-coder ~278k | n/a (gate) | **No substantive finding above MINOR**, so it does not count toward escalation and the Gate 2 streak ends. Round 2 MAJOR-A is closed: 432 swap/rotation probes pass, and the inheritance mutant fails 288. The counts (133/44/6/54/29) were confirmed by two lenses. MINOR substantive: DS-1 (`beginWork` accepts a clone owner that carries its chaId); DS-3(a) (a swap is blamed for a pre-existing chat-id duplicate); F1 (id-by-position mutants on the database routes survive); F5 (plugin name unpinned on 5 v3 paths); F6 (no positive database chat-id warning test); P2 (repeated per-character count). Recorded: DS-2 (stale blocks accumulate, one per id-less install; added to CHORE-29); DS-3(b) (duplicate-chaId corner); P1 (the live-idiom fill costs about 12.7 ms prod at 1000 x 20; accepted, since the call already marks every character); v2.1 in-place duplicates not warned (added to CHORE-28). Remediation: 12 new tests (3 red); `beginWork` checks the owner is live; attribution by prior holder; identity-set skip and indexed loops; warning text; docs. Suite 98 files / 1250 passed / 4 skipped. Recount: 145 new = 51 fail on assertions at HEAD + 6 missing function + 55 spec + 33 pass. |
| 177 | 2026-09-25 | Gate 2 round 4, W0: re-check of round 3's remediation | Workflow `w0-gate2-round4`: 3 `opus-reviewer` lenses (closure-safety, mutants-counts, wording-commit); deep-investigator verify stage (not triggered) | ~806k subagent tokens, 564 tool uses | n/a (gate) | All findings MINOR; each substantive one was run. Mutants run: M03c-f, M06 (plugin name per v3 path), F6, DS-1 and the slot baseline are killed; P2n survives as behaviour-neutral. MINOR substantive: DS-3(a) fixed for one swap order only (a carrier installed second is still blamed); the prior-holder scan adds about 0.45 ms to every per-slot install; the >1-prior-holder skip is untested; wrong length-based P2 variants survive. **Mechanism question** (the second round on warning attribution): blaming a call is undecidable call by call, like identity. The Orchestrator decided the warning reports the state after the call (may be transient, may predate the call), with the count filter kept as noise reduction and an early return before the scan. Wording: universal "never blamed" claims, shallow-copy reasoning, a rotation comment, the cold-proxy cost (+23.5 ms) missing from the commit message, the detection blind spot, CHORE-29's visible consequence, "by index" for a Set, and the Kei comment. |
| 178 | 2026-09-25 | CHORE-28: how the save path handles a `chaId` held by two characters, where one guard covers every backend, marks, warning surface, cost | `investigator` (Sonnet 5) | ~134.7k, 39 tool uses | No | **Choke point:** `RisuSaveEncoder.init`/`set` in `risuSave.ts`, reached only from `saveDb`; every backend and remote blocks sit downstream, so one guard covers all. **Load-bearing risks:** (1) a full reload builds a fresh encoder from `{}`, so "not rewritten" there leaves no block at all unless the old block is carried forward; (2) `set`'s "probably deleted characters" branch deletes any id left in `toSave.character` that is not in `savedId`, which a careless skip would trigger; (3) a mark left unconsumed evaporates on a successful iteration, so resuming needs its own path. Never-saved duplicates have no last good save (the maintainer decided MC-082: write the first holder once). Warning: `alertToast` with per-episode dedupe after `conflictAlertShown`. Cost O(n) on `chaId` only (not measured). **Orchestrator verification:** re-read `init`, `set`, `prepareSaveIteration` and the reload closure; all three risks hold. The packet's claim that W0's scenario 20a was never written is wrong: it is "the save format holds one block per chaId" in `pluginIdentityFill.svelte.test.ts`. Plan: Report 26 rev 1. |
| 179 | 2026-09-25 | Gate 1 round 1, CHORE-28 plan (Report 26 rev 1) | `opus-reviewer` (Opus 5, fresh) | ~237.1k, 128 tool uses | n/a (gate) | **[REJECT].** M1 (run): `init`/`set` walk the live array and await each block write, so a copy pushed partway through `init` wins the block; a start-of-pass count goes stale. M2: `alertToast` shares the single alert slot, lasts about a second, and can cancel an open `alertConfirm`. M3: a normal delete only trashes; `GridCatalog` delete/permanent delete/restore pass `chaId` and `findCharacterIndexbyId` returns the first holder, so the warning's advice could delete the original. M4: tests supply their own reload closure, so the production hand-over is untested. MINORs: scenarios 1/3/4 green at HEAD in `[A,B]` order; no save trigger when a duplicate resolves; `cleanColdStorage` computes used keys from memory; multi-tab dirty reset; `backuplocal.ts` does not repair ids; cache overstatement; key form; a test title; the boot edge. **Orchestrator verification:** re-read `alert.ts`, `GridCatalog.svelte`, `findCharacterIndexbyId`, `removeChar`, `SavePopupIcon.svelte` and the reload closure; all four MAJORs hold. The local restore does reload the page (boot repairs), but the save loop can run once before it. Substantive rejection 1 of the streak. Rev 2 folds in every finding (G-1 snapshot, G-6 persistent indicator, G-10 grid by row, G-11, G-12, shared hand-over with a mutant). |
| 180 | 2026-09-25 | Gate 1 round 2, CHORE-28 plan (Report 26 rev 2) | `opus-reviewer` (Opus 5, fresh) | ~221.3k, 78 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS].** Round 1's M1, M2 and M4 closed; M3 mostly closed. N1 (MAJOR): "dirty while frozen" routes a frozen-only tab into the multi-tab prompt, whose "Save my changes" writes its older copy of every character over a peer's save, repeatedly for a permanent duplicate. N2 (MAJOR): no exit for a frozen key whose holders all disappear without a reload (a stuck indicator, or a save every second). MINORs: rev 2 scenarios 9, 7 (`set`), 21 and 22 green at HEAD; the CHORE-17 comment would become false; index plus check cannot tell holders apart and `removeChar` resolves after its confirms; the idle step has no seam; indicator placement and boot publishing; `cleanColdStorage` checks only at entry; carry must copy references only; `encodedCharacterProxies` for non-encoded holders. The reviewer's own HEAD probes could not run (shell-wrapper failure), so its HEAD claims are traced. **Orchestrator verification:** re-read `getMultiTabAction` and the save loop's prompt (N1 holds) and `removeChar` (N5 holds). The Orchestrator dropped the multi-tab half of G-12 (a frozen-only tab auto-reloads as at HEAD). Rev 3 folds in every finding and is ready for implementation. |
| 181 | 2026-09-25 | Gate 2 round 1, CHORE-28 implementation (Report 26 rev 3), two lenses | `opus-reviewer` x2 (Opus 5, fresh): data-safety and evidence/wording | ~212.4k, 211 tool uses; ~237.7k, 184 tool uses | n/a (gate) | **Data-safety [APPROVE-WITH-FINDINGS]:** no loss path; G-7 byte identity run at 14 checkpoints against HEAD's `risuSave.ts`. MINORs: a frozen key marked twice logged a false "Deleting character data" line (run); the report's boot-edge claim was false (run); added save-path calls unguarded against throwing; cold-storage re-check not immediately before removal; holders counted by raw `chaId` while blocks key by string. **Evidence [REJECT]:** M1 (run) the per-pass list copy is load-bearing but untested, and the commit's explanation was false. At HEAD, a character removed during an earlier character's write makes the loop skip the next one, whose mark is then read as a deletion and its block deleted; the copy fixes that too. M2 false test counts; M3 test comments described fixed bugs in the present tense; m1 to m5 wording, titles claiming wiring, an untested read-once invariant. The Orchestrator re-checked the orderings. Substantive rejection 1 of the Gate 2 streak. The data-safety lens briefly created `d1.txt` in the repo root and deleted it; the evidence lens and the round-2 reviewer each had a `cp` glob match another session's scratchpad (nothing written). |
| 182 | 2026-09-25 | Gate 2 round 2, CHORE-28 re-check of round 1's remediation | `opus-reviewer` (Opus 5, fresh) | ~203.7k, 147 tool uses | n/a (gate) | **[REJECT].** MAJOR-1 (run): the round-1 fix counted holders by `String(chaId)` but matched marks with `indexOf(key)`; `frontUnshiftSelected` pushes the raw selected `chaId`, so a selected, edited character with a numeric `chaId` had its block deleted on every pass (HEAD saved it). MINORs: test counts off by one (the HEAD swap never served `en.ts`); two `init` survivors (key read live, `previous` for single holders); the resume wording held only for two holders. **Mechanism question (second rejection):** the fix had normalised one side of a comparison; the fix is to compare every mark and key in string form, keeping the raw `name` for `encodeBlock`. Orchestrator confirmed MAJOR-1 in source. Substantive rejection 2. |
| 183 | 2026-09-25 | Gate 2 round 3, CHORE-28 re-check, with the mechanism question asked up front | `opus-reviewer` (Opus 5, fresh) | ~217.3k, 188 tool uses | n/a (gate) | **[APPROVE-WITH-FINDINGS].** The two key forms are consistent everywhere they meet; `prepareSaveIteration` compares raw to raw and agrees. G-7 identical at 14 checkpoints; the only difference from HEAD without a duplicate is a string-form mark on a non-string id, which HEAD deleted and the change writes. Counts (55: 24 loss, 2 MC-082, 4 indicator, 14 missing function, 1 wording, 10 pass) and cost confirmed. Finding 1: the deletion loop's `String()` compare is untested (mutant C loses character 5 with marks `[5,'x',5]`). MINORs: a weak `toBeDefined` assertion; spies restored after failing assertions; `unknown[]` widening; two wordings. The reviewer's simpler rule (convert `toSave.character` to strings in place at the top of `set()`) was rejected by the Orchestrator: after a failed write `mergeUnsavedChanges` folds it back into the live tracker, and the no-reload filter compares raw values, so a numeric `chaId`'s mark would be dropped. The streak ends. |
| 184 | 2026-09-25 | Fix-up review of Gate 2 round 3's remediation, CHORE-28 | `adversarial-reviewer` (Sonnet 5, fresh) | ~144.1k, 111 tool uses | n/a (gate) | **[APPROVE].** Ran: mutant C (deletion loop by raw mark) killed only by the new `[5,'x',5]` test; mutant B (`removeAllOccurrences` by identity) killed only by the strengthened numeric two-holder test, which now fails at HEAD; `try/finally` moved only the restores; the comment's `mergeUnsavedChanges` and raw no-reload filter claims hold; the commit totals (56: 25 loss, 2 MC-082, 4 indicator, 14 missing function, 1 wording, 10 pass) are consistent. Full suite 104 files / 1310 passed / 4 skipped; `pnpm check` clean. Process lapse: the Orchestrator's brief told the reviewer to use the Write tool, which `adversarial-reviewer` does not have (the feedback memory on tool grants applies); the reviewer used shell appends instead, and a Git Bash `ln -s` made a real copy of its scratch folder at `%TEMP%\qa1`, left for the maintainer to delete. |

## Reading of the log so far (n=1 — not a conclusion)

Run 1 is the only data point and it predates the split, so it cannot settle anything. Two
observations that motivated the split, recorded while they are fresh:

1. **The valuable output was concentrated, and thinner than it first appeared.** Of the two
   findings that looked like they justified the Opus tier, only one survived: the
   `getModules()` load-bearing cache accident was real and decision-changing, while the
   DEV-inflation doubt was tested and disproven (1.18x, not a multiple). Most of the 43 tool
   calls were mechanical archaeology producing no reasoning of either kind. One genuine
   load-bearing find per ~100k tokens is the bar the mid-tier now has to clear — and it is a
   lower bar than the raw report made it look.
2. **Orchestrator verification caught real gaps in an Opus packet.** Both understatements
   above came from an *Opus* investigation, not a cheap one. This is the argument for the
   standing verification duty in 1.3 being tier-independent: the failure mode is
   compression, not model capability. It also means a mid-tier packet should not be held to
   a standard the Opus tier did not meet.
3. **A flagged doubt is not a finding until it is tested.** The Opus packet correctly
   identified a real DEV-only code path and correctly labelled its magnitude UNCERTAIN. The
   Orchestrator then spent a `perf-analyzer` dispatch settling it, and the answer was "no
   material effect." That sequence was correct and should be repeated — but it means an
   investigation's value should be scored on premises *settled*, not premises *raised*.

Do not draw a conclusion about escalation rate until there are enough post-split rows to
have one.
